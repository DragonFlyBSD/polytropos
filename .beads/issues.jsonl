{"_type":"issue","comment_count":0,"created_at":"2026-09-25T10:05:50Z","created_by":"Fred [bot]","dependencies":[{"created_at":"2026-09-25T10:05:50Z","created_by":"Fred [bot]","depends_on_id":"poly-xxys","issue_id":"poly-5qtr","metadata":"{}","type":"discovered-from"}],"dependency_count":0,"dependent_count":0,"description":"LIVE INCIDENT, 2026-09-25. Job\n20260925-091002Z-2026Q3-print_qt6-pdf-905127-patch.job, attempt 1, turn 41.\ndsynth_build has shown \"running\" since 09:20:13Z and had not returned 45\nminutes later, while the build's own log says it finished.\n\nWHAT THE TRACKER SHOWS, measured rather than guessed\n\n  runner heartbeat   healthy -- sampled every 15s, 1s old each time. So the\n                     PROCESS is alive and the heartbeat thread is ticking on\n                     its 5s interval. Only the main thread is stuck.\n  tool               dsynth_build, since 2026-09-25T09:20:13Z\n  published tail     frozen at 3,388,985 bytes across five samples over two\n                     minutes, last line `SUCCEEDED 00:00:00`, preceded by\n                     `rcng_ldconfig: Operation not permitted` twice and a\n                     `Deleting files for qt6-pdf-6.11.1` counter run to 100%\n  turn               41, unmoved\n\nThe log is not growing and ends in dsynth's own success line. The tool call\noutlives the build it is waiting for.\n\nTHE CALL HAS NO BOUND\n\n  worker._dsynth_run()       p = _exec(env, \"/bin/sh\", \"-c\", cmd, \"_\", *origins)\n  worker._exec(...)          subprocess.run(..., capture_output=True,\n                                            timeout=timeout)   # timeout=None\n\n_exec TAKES a timeout parameter and NOT ONE CALLER PASSES IT -- every\nchroot-bound tool in worker.py goes through it, and `grep -c timeout` over the\ncall sites returns 0. There is no tool-timeout setting either; settings.py\nbounds llm.*.timeout and delivery.git_timeout and nothing else. So a dsynth\nthat never exits blocks the runner's main thread for as long as the process\nlives, holding the one runner and its claimed job.\n\nWHY IT PROBABLY OUTLIVED DSYNTH ITSELF. capture_output=True means\nsubprocess.run reads stdout/stderr to EOF, and EOF comes when the LAST holder\nof the pipe closes it -- not when dsynth exits. Anything dsynth left behind\ninside the chroot that inherited those descriptors keeps the read blocked\nafter dsynth is gone. `rcng_ldconfig: Operation not permitted` immediately\nbefore the success line says an rc script ran during package registration,\nwhich is the kind of thing that leaves a process behind.\n\nThat last paragraph is the HYPOTHESIS and is not yet confirmed; it needs the\nhost. Two cheap checks settle it:\n\n  ps -auxww | grep -E 'dsynth|dev-env exec'   is dsynth gone, defunct, or\n                                              still running?\n  fstat -p <runner pid>                       who else holds the pipe?\n\nCONFIRMED EITHER WAY: an unbounded wait on a build. Whether dsynth hung or its\npipe outlived it, nothing in the runner can end it.\n\nTHE FIX\n  * a timeout on the dsynth calls at least. The parameter is already there and\n    already threaded; today it is simply never given a value.\n  * do not wait on the pipe for the verdict. For the long tools, wait on the\n    PROCESS and drain the pipes separately, so a lingering descriptor holder\n    cannot outlive the exit status. The tail already gives the operator the\n    output live, so the captured stream is no longer the only way to see it.\n  * a stuck tool should be visible. Nothing on the job page distinguishes \"a\n    44-minute build is running\" from \"this returned 45 minutes ago and we are\n    still waiting\" -- the tail's own mtime is the signal, and poly-qqx9.8 put\n    \"last line N ago\" on the page for exactly this. It rendered blank here;\n    worth checking why, because it would have named this in one glance.\n\nRELATED\n  poly-q07p -- abandoning the job leaves the .job file claimable, so the\n  runner re-claims it. That constrains the recovery for this incident.\n  poly-xxys -- the same class of defect on the heartbeat thread, latent.\n","id":"poly-5qtr","issue_type":"bug","owner":"github@dragonflybsd.org","priority":1,"status":"open","title":"A dsynth build that never returns hangs the runner forever: no tool call passes _exec's timeout","updated_at":"2026-09-25T10:05:50Z"}
{"_type":"issue","close_reason":"Closed","closed_at":"2026-09-24T19:58:07Z","comment_count":0,"created_at":"2026-09-24T19:49:28Z","created_by":"Fred [bot]","dependencies":[{"created_at":"2026-09-24T19:49:28Z","created_by":"Fred [bot]","depends_on_id":"poly-qqx9","issue_id":"poly-qqx9.17","metadata":"{}","type":"parent-child"}],"dependency_count":0,"dependent_count":0,"description":"DIVERGENCE FROM THE DESIGN SOURCE, found by the operator 2026-09-24.\n\nThe epic names artifact \"Five Ways to Watch a Job\" version 9 as what the work\nis measured against. Re-read today. It carries DEDICATED TERMINAL TOKENS that\nare dark in BOTH themes:\n\n  light   --term-bg #0e161d  --term-fg #c8d4dc  --term-dim #6c7f8d\n  dark    --term-bg #05090d  --term-fg #c8d4dc  --term-dim #5d6e7b\n\n  .tt-tail { color: var(--term-fg); background: var(--term-bg); height: 132px; }\n  .tt-tail .dim { color: var(--term-dim); }\n  .tt-tailbar { color: var(--faint); background: var(--head); font: 11px mono; }\n\nWhat was built uses --code-fg on --code-bg, which in the light theme is\n#253039 on #f6f8fa: a CODE BLOCK, the same treatment as a tool stdout dump.\nThe design deliberately distinguishes the two -- .tt pre uses --code-bg and\n.tt-tail uses --term-bg, in the same specimen, ten lines apart. progress.css\nhas no --term-* tokens at all, so the divergence was introduced when the tail\nCSS was first written and survived because nobody compared.\n\nSECOND DEFECT, same element: THE TAIL MUST SIT ON THE LAST LINE unless the\nreader has scrolled up. Half-implemented in poly-pvs2: agentic-job.js pins to\nthe bottom when it SWAPS the slot on a poll, but nothing scrolls it on the\ninitial server render, so first paint shows the oldest of the last forty lines\nnext to a label reading \"last line 4s ago\".\n\nFIX\n- add --term-bg / --term-fg / --term-dim to all three token blocks in\n  progress.css (bare :root, the prefers-color-scheme guard, and\n  :root[data-theme=\"dark\"]), matching the design values\n- .tool-tail takes --term-fg on --term-bg\n- scroll the tail to the bottom on initial load as well as on swap","id":"poly-qqx9.17","issue_type":"bug","labels":["tracker-ui"],"notes":"ONE DELIBERATE DIVERGENCE FROM THE DESIGN SURVIVES, written down per the\nrepo rule rather than left to be rediscovered.\n\nThe design sets .tt-tail at font: 11.5px/1.5. This sheet has an\naccessibility floor of 12px, enforced by\ntest_design_tokens.py::test_nothing_essential_is_below_twelve_pixels, which\nwas added after a 10px uppercase label slipped in (UI-4). A running builds\nlog is precisely the essential text that floor exists to protect, so the\nrule wins and the tail is 12px.\n\nEverything else matches the source: --term-bg / --term-fg / --term-dim at the\ndesigns values in all three token blocks, and height 132px rather than the\nmax-height it had (a pane that resizes under the reader as lines arrive gives\nscroll-to-bottom no stable frame).","owner":"github@dragonflybsd.org","priority":1,"status":"closed","title":"The dsynth tail is a code block, not a terminal, and it opens on the oldest line","updated_at":"2026-09-24T19:58:07Z"}
{"_type":"issue","assignee":"Fred [bot]","close_reason":"Closed","closed_at":"2026-09-24T19:12:38Z","comment_count":0,"created_at":"2026-09-24T19:08:31Z","created_by":"Fred [bot]","dependency_count":0,"dependent_count":0,"description":"FOUND IN DEPLOYMENT 2026-09-24, and it hid a whole feature.\n\nserver.py computes static_v as the sha256 of progress.css alone -- its comment\nsays so: \"Content-hash cache-buster for progress.css\". Both the CSS and the JS\ntags then interpolate that one token:\n\n  _base.html:23          progress.css?v={{ static_v }}\n  agentic_job.html:176   agentic-job.js?v={{ static_v }}\n  agentic_job_transcript.html:51   same\n\nSo a change to any .js file does not move the token. The URL is byte-identical,\nevery warm browser cache keeps the old script, and there is no error anywhere --\nthe server sends the new payload and the stale client ignores the parts it does\nnot know about.\n\nOBSERVED: poly-pvs2 shipped a tail_html slot plus the JS that swaps it. On the\nbuilder the producer published correctly and the tracker returned tail_html on\nevery poll, verified by curl. The page showed nothing, because the browser was\nrunning agentic-job.js from before the deploy. progress.css was untouched by\nthat work -- the tail CSS had survived poly-paee -- so the token was unchanged.\n\nThere are 9 files in static/ (196K total), 5 of them .js. Any JS-only change\nhas always shipped this way.\n\nFIX: hash the whole static directory, including each file relative path so a\nrename or deletion also moves the token. One directory read at startup.","id":"poly-3o3o","issue_type":"bug","notes":"FIXED 2026-09-24. static_v is now the sha256 of every file under static/, each\nfiles relative path hashed alongside its bytes so a rename or deletion moves\nthe token too. One directory read at startup; static/ is 196K across 9 files.\n\nTest pins it as a CONTRACT rather than by re-running the loop: the token the\napp serves must equal the hash of the whole tree. Mutation-checked -- reverting\nto progress.css alone fails that test, which is the bug as it shipped. Five more\ncover a JS-only change, a CSS-only change, a rename, that every versioned tag\ncarries the token, and that the page still serves one.\n\nWHY THE OLD TEST SUITE COULD NOT HAVE CAUGHT THIS: nothing asserted on the\ntokens INPUTS, only that a token existed. The defect is invisible server-side --\ncorrect HTML, correct payload, correct headers - and shows up only in a browser\nthat already has the file. That is also why poly-pvs2 passed 31 tests and still\nshowed nothing on the builder.","owner":"github@dragonflybsd.org","priority":1,"started_at":"2026-09-24T19:08:54Z","status":"closed","title":"Static cache-buster is the CSS's hash, so a JS-only change ships invisibly","updated_at":"2026-09-24T19:12:38Z"}
{"_type":"issue","assignee":"Fred [bot]","close_reason":"Closed","closed_at":"2026-09-23T23:47:57Z","comment_count":0,"created_at":"2026-09-23T23:44:31Z","created_by":"Fred [bot]","dependencies":[{"created_at":"2026-09-23T23:44:31Z","created_by":"Fred [bot]","depends_on_id":"poly-qqx9","issue_id":"poly-qqx9.15","metadata":"{}","type":"parent-child"}],"dependency_count":0,"dependent_count":0,"description":"Reported from a running job, 2026-09-24: the attempt number and turn show\nbriefly, then vanish while the job is still live and refreshing.\n\nA contract mismatch between the fragment and its client.\n\napi_job_activity_fragment renders every body `if rows else \"\"`, where\n`rows` is only the rows NEWER than the client's cursor. The client does:\n\n  if (data.nowbar_html !== undefined && barSlot)\n      barSlot.innerHTML = data.nowbar_html;\n\nAn empty string passes `!== undefined`, so the first poll with nothing new\nassigns \"\" and ERASES the bar. startElapsedClock() then has nothing to\ntick. The cards survive the same poll only because their guard is\n`if (data.cards_html && ...)` -- truthiness, which \"\" fails.\n\nWHY IT LOOKS LIKE \"A SHORT PERIOD OF TIME\": rows arrive in bursts at turn\nboundaries and vanish in between. A long tool emits nothing for its whole\nduration -- a 41-minute dsynth build is 41 minutes of empty polls -- so the\nbar disappears exactly during the wait it exists to explain.\n\nThe client's guard was written expecting the key to be OMITTED when there\nis nothing new; the server sends \"\" instead. Fix on the server so the\ndeliberate clear still works: when a poll HAS rows and now_bar() returns\nNone (MANUAL tier, or the job just ended), \"\" is correct and must still\nempty the slot.\n\nNot a re-render problem: with no new rows the bar's content cannot have\nchanged (no new turn, no new tool), and the elapsed clock is client-side.\nOmitting the key is both correct and free.\n","id":"poly-qqx9.15","issue_type":"bug","labels":["tracker-ui"],"notes":"FIXED 2026-09-24.\n\nThe fragment now OMITS nowbar_html when no rows are new, rather than\nsending \"\". The client's guard was already written for that contract; the\nserver was the half that disagreed.\n\nDeliberately a presence check, not truthiness: on a poll that HAS rows,\nnow_bar() returning None (MANUAL tier, or the job just ended) must still\nempty the slot, and truthiness would have kept a stale bar on screen\nforever.\n\nNo client change. Hardening the JS to ignore \"\" as well would have broken\nthat deliberate clear, so the contract is stated in a comment at the\nserver instead -- one reader, one writer, one rule.\n\nNothing is lost by omitting it: with no new rows there is no new turn and\nno new tool, so the bar's content cannot have changed, and the elapsed\nclock runs client-side off a data attribute.\n\ncards_html was never affected because its client guard is truthiness\n(`if (data.cards_html && ...)`), which \"\" fails. That inconsistency is why\nonly the bar disappeared and the cards stayed -- matching the report.\n\nTest: a poll at the newest cursor returns changed:false and no\nnowbar_html key at all. 4210 pass.","owner":"github@dragonflybsd.org","priority":1,"started_at":"2026-09-23T23:45:09Z","status":"closed","title":"The now-bar disappears on a live job: the fragment sends an empty bar when nothing is new","updated_at":"2026-09-23T23:47:57Z"}
{"_type":"issue","assignee":"Fred [bot]","comment_count":0,"created_at":"2026-09-23T23:43:06Z","created_by":"Fred [bot]","dependencies":[{"created_at":"2026-09-24T15:40:29Z","created_by":"Fred [bot]","depends_on_id":"poly-fij.12","issue_id":"poly-pvs2","metadata":"{}","type":"blocks"},{"created_at":"2026-09-23T23:43:06Z","created_by":"Fred [bot]","depends_on_id":"poly-paee","issue_id":"poly-pvs2","metadata":"{}","type":"related"},{"created_at":"2026-09-23T23:44:49Z","created_by":"Fred [bot]","depends_on_id":"poly-qqx9","issue_id":"poly-pvs2","metadata":"{}","type":"parent-child"}],"dependency_count":1,"dependent_count":0,"description":"poly-qqx9.8 put a running build's last lines inside the tool row that was\nrunning it. As built it cannot work: the tracker resolves the log path\nthrough worker._dsynth_log_candidates -> env_paths() -> a root-only\n`dev-env path`, and the tracker is unprivileged. poly-paee removes the\nwiring. This bead holds the decision about what, if anything, replaces it.\n\nWHY IT IS NOT JUST \"DO WHAT THE DIFF DOES\". The diff publishes once per\nattempt; the tail's whole value is sub-minute freshness during a 41-minute\nbuild. Over the artifact channel that means the runner re-POSTing the last\n~32 KiB every N seconds -- put_blob upserts on (bundle_id, relpath) and is\ncontent-addressed, so an unchanged tail costs no new blob, but it is still\na periodic emitter inside the build wait loop, which is a change to the\nloop rather than a tracker fix.\n\nTHREE OPTIONS, in the order I would consider them:\n1. The runner publishes a rolling tail artifact every N seconds while a\n   tailable tool runs. Honest, reuses the existing channel, and the\n   dsynth_tail.read_tail windowing/offset/cap logic is reusable as the\n   producer side.\n2. Drop the feature. The full log already reaches the tracker as\n   logs/full.log.gz when the build ENDS, and the tool row already shows\n   the build running with an elapsed clock. The tail was a nicety.\n3. Leave it for poly-fij, where a remote runner needs a general answer for\n   streaming progress anyway, and a bespoke tail channel would be work\n   thrown away.\n\nWhat must NOT happen is the current state: a feature that looks built,\nrenders nothing, and says nothing about why.\n","heartbeat_at":"2026-09-24T16:38:31Z","id":"poly-pvs2","issue_type":"task","lease_expires_at":"2026-09-24T16:43:31Z","notes":"SEQUENCED AFTER poly-fij.4, 2026-09-24. Not frozen for lack of a mechanism\nany more -- frozen because the mechanism needs a channel that does not exist\nyet.\n\nTHE TIMER PROBLEM IS SOLVED, and option 1's cost estimate above was wrong.\nIt said a periodic emitter would mean a background thread or a Popen rewrite\nof the build path. The runner ALREADY has one: _heartbeat_loop, a daemon\nthread started at runner.py:2081, ticking every HEARTBEAT_INTERVAL = 5s.\nVerified live on a builder -- host clock 10:38:18Z against\nrunner_status.updated_at 10:38:16.599, two seconds old -- and it keeps\nticking through a build because it is a separate thread from the one blocked\nin subprocess.run. Nothing in _dsynth_run needs touching.\n\nIt does not need dev-env exec either. The runner is root, so it can resolve\nenv_paths(env).writable / \"work/dsynth/logs/...\" and open the file directly:\na host-side read, no chroot spawn per tick. The offset-seeking reader for\nexactly this is recoverable at commit 6275522; it was only ever wrong in the\nTRACKER, where resolving the path needed root.\n\nTHE DESTINATION IS THE PROBLEM. The obvious cheap store -- a few columns on\nrunners or runner_status, overwritten in place, which the heartbeat is\nalready writing in the same transaction -- would be a TENTH direct sqlite\nwrite from the runner, on precisely the channel poly-fij.4 exists to remove.\nStoring it as artifacts instead is not an alternative: a tail's bytes differ\nevery tick, so nothing dedupes, and 480 ticks over a 40-minute build is\n~15 MB per build and several GB across the jobs already run. A tail wants\none overwritten row, and the runner should not be writing rows.\n\nSo: heartbeat as the timer, and the tail rides the HTTP write channel\npoly-fij.4 establishes -- most likely on the heartbeat endpoint itself,\nsince that is the one call already happening at the right cadence with the\nright identity. That is option 3 of the three above, reached from the other\ndirection.\n\nALSO NOT FREE, and still unmeasured: a 32 KiB seek-read every 5s per running\nbuild. If it matters the tail can tick on a multiple of the heartbeat\nwithout changing anything else.\n\nMEANWHILE, and independent of all of it: a failed dsynth_build row could\nlink to its bundle's logs/errors.txt (21,938 bytes on one measured job) and\nlogs/full.log.gz, which already exist and are already rendered by the\nartifact viewer. That is one click from the row that failed instead of two\nclicks and knowing where to look, it needs no runner change at all, and it\nis most of the practical value. It does not solve liveness -- only a real\ntail does.\nDEPENDENCY CORRECTED 2026-09-24. Was poly-fij.4 (auth); that was wrong. Auth is not on this bead's path: the five existing /v1 routes carry no credential, the runner already POSTs to them unauthenticated over loopback, and internet exposure is deferred. When poly-fij.4 lands it adds a header to the whole channel at once, the tail included.\n\nThe real blocker is poly-fij.12, and specifically only its first two steps. The note above says the tail should ride 'the heartbeat endpoint itself, since that is the one call already happening at the right cadence with the right identity' -- there is no such endpoint. /v1 has artifacts/get, artifacts/put, bundles/upsert, jobs/transition, user-context. Creating POST /v1/runners/heartbeat is poly-fij.12 step 2, and its own description calls that step 'self-contained -- proves the path end to end at trivial volume'.\n\nSo: poly-fij.12 steps 1-2 (the seam, then the heartbeat), then this. Steps 3-5 of that bead (activity_log batching, issues, the reads) are the rest of the epic and are NOT needed here.\n\npoly-fij.7 is NOT on this path either, contrary to an earlier reading. The relay exists because the HOOKS only know 127.0.0.1. The tail's producer is the runner, which reaches the tracker directly.\n\nSTILL TRUE AND UNCHANGED: the renderer is already built and tested -- running_tailable_tool and attach_tool_tail in tracker/render/activity.py, four tests in test_render_tool_tail.py, both carrying 'Has no caller in production'. Only the wiring is missing. The offset reader is at commit 6275522. _dsynth_log_candidates (worker.py) resolves the path root-side.\n\nONE GAP MEASURED: the heartbeat can see _current_job_id and _current_stage (runner.py:292-293) but not env+origin, which the path resolver needs. Two more module globals, set where _current_stage is.\nCODE READ 2026-09-24. THE FEATURE IS SMALLER THAN THIS BEAD ASSUMES, and one part is bigger.\n\n1. THE WHOLE VIEW LAYER SURVIVED poly-paee. Not just the two render helpers:\n   _turn_cards.html         the tail block, tailbar, byte counts, log link\n   progress.css:2633        .tool-tail / .tool-tailbar\n   agentic-job.js:274       the 'last line N ago' ticker off data-mtime\n   render/activity.py       running_tailable_tool + attach_tool_tail, 4 tests\npoly-paee removed only the PRODUCER -- tracker/dsynth_tail.py, the route, and\nthe pages.py wiring. So this is a producer plus a sink plus re-attaching one\ncall, not a feature rebuild.\n\n2. THE OFFSET READER IS REUSABLE VERBATIM, and it belongs on the runner now.\ngit show 6275522:dportsv3/tracker/dsynth_tail.py -- (path, offset) -> (text,\nnext offset, eof), nothing host-specific, delegating to\nworker._dsynth_log_candidates. It was only ever wrong because it ran in the\nTRACKER, which cannot resolve a dev-env path. On the runner, which is root,\nit is correct as written. Moving it to dportsv3/agent/.\n\nNO OFFSET TRACKING, though. Incremental reads would leave the row holding only\nwhat arrived in the last tick -- zero lines on a quiet build. The page wants\nthe newest screenful every time, so the producer reads offset=-1,\nmax_lines=40 each tick and overwrites. That is a 32 KiB seek-read per tick,\nstill unmeasured, as this bead already flagged.\n\n3. THE LIVE PATH IS THE PART THIS BEAD MISSED, and it is the same defect as\npoly-qqx9.16 one surface over. In agentic_api.py the fragment computes\n    window = activity_for_job(...) if rows else []\n    cards  = group_activity_into_cards(window) if rows else []\nand returns changed=bool(rows). A dsynth build writes NO activity_log rows for\n40 minutes, so with the tail hung on a card the live view would never refresh\nit -- exactly what the attempt strip's own comment says it fixed for itself:\n'Gated on new rows it would sit frozen for the whole of a long build'.\n\nFixing it by ungating the card render would swap the whole card container every\n3s for the length of a build, which recreates the tail's <pre> and so resets\nthe scroll position of the one element a reader may be scrolled inside. So the\ntail gets ITS OWN SLOT, like strip_html and worktree_html: _tool_tail.html\nextracted as a partial, sent as tail_html, swapped in place with the scroll\npreserved.\n\nTHE SINK IS KEYED BY RUNNER, NOT BY JOB. One row per job would be unbounded\nover time at up to 32 KiB each -- the artifact-growth objection this bead\nalready makes, just slower. Keyed by runner_id it is exactly N rows forever,\nwith job_id as a column: the page reads WHERE job_id = ? and correctly gets\nnothing once that builder moved on, which is also the right semantics, since a\nfinished job's tail is superseded by logs/full.log.gz.\n\nTHE LOG LINK HAS NO TARGET WHILE RUNNING. The template's 'open the whole log'\nhref pointed at api_job_dsynth_tail, which poly-paee deleted, and full.log.gz\ndoes not exist until the build ends. Guarded in the template rather than\nrendered as an empty href.\nIMPLEMENTED 2026-09-24. Option 1 of the three, reached with poly-fij.12's carrier instead of a bespoke channel -- so the answer to 'or it goes' is that it stays.\n\n  agent/dsynth_tail.py      the offset reader, moved from tracker/ unchanged.\n                            It was never wrong -- only in the wrong process.\n  runner._tail_target       what to tail, or None. Set at tool_start for a\n                            TAILABLE tool, cleared when any call returns.\n  runner._heartbeat_tail()  offset=-1, max_lines=40, never raises.\n  presence.set_tail         one row per builder, overwritten; NULLs when\n                            nothing runs; also blanked on deregister.\n  runner_tail               new table, PK runner_id, job_id a column.\n  job_tail(conn, job_id)    WHERE job_id = ? AND text IS NOT NULL.\n  _tool_tail.html           the block extracted as a partial.\n  tail_html                 its own slot, sent on EVERY poll.\n  common/tools.py           TAILABLE_TOOLS declared once, both sides import.\n\n4267 -> 4298 tests (31 new, two files).\n\nMUTATION-CHECKED, the one that matters: gating the fragment's tail read on\n -- i.e. reintroducing the defect described above -- fails three tests\nincluding test_the_tail_refreshes_with_no_new_activity_rows. Without that the\nfeature would have looked built and been frozen for the length of every build,\nwhich is the exact failure mode this bead was filed to prevent.\n\nFOUR THINGS THE REVIEW ADDED:\n- Follow the newest line. A live tail whose <pre> is recreated every 3s renders\n  scrolled to the TOP, showing the oldest of the last forty lines beside a\n  label reading 'last line 4s ago'. It now pins to the bottom unless the reader\n  has scrolled up.\n- The swap runs AFTER the cards swap, since the slot lives inside the card\n  stream and would otherwise be overwritten on any poll that had rows.\n- deregister blanks the tail. The page's 'is a tool running' gate reads\n  activity rows, and a stopped runner leaves those looking like a live build.\n  A crash cannot run it; there the mtime ticker counting up is the tell.\n- No 'open the whole log' link. It pointed at a route poly-paee deleted, and\n  full.log.gz does not exist until the build ends -- href=\"\" reads as broken.\n\nCOVERAGE: patch and convert both, since both run through PatchAttemptStep's\ndispatcher. Triage has its own handler and nothing to tail -- it reads a log\nthat already exists rather than producing one.\n\nSTILL UNMEASURED, as this bead said: a 32 KiB seek-read every 5s per running\nbuild. If it shows up, the tail can tick on a multiple of the heartbeat without\ntouching anything else -- _heartbeat_tail() is the only place that would change.\n\nTHE MEANWHILE ITEM IS NOT DONE and is now separable: a FAILED dsynth_build row\nlinking to its bundle's logs/errors.txt and logs/full.log.gz. That is about\nfinished builds, needs no runner change, and is worth its own bead.\nCORRECTION to the note above: one word was lost to shell quoting. The mutation was \"gating the fragment tail read on ROWS\" -- that is, on new activity_log rows since the cursor, exactly as everything else in that fragment is gated.","owner":"github@dragonflybsd.org","priority":1,"started_at":"2026-09-24T15:38:45Z","status":"in_progress","title":"Freeze: the dsynth tail needs the runner to publish it, or it goes","updated_at":"2026-09-24T16:56:57Z"}
{"_type":"issue","assignee":"Fred [bot]","close_reason":"Closed","closed_at":"2026-09-24T00:02:57Z","comment_count":0,"created_at":"2026-09-23T23:42:39Z","created_by":"Fred [bot]","dependencies":[{"created_at":"2026-09-23T23:44:48Z","created_by":"Fred [bot]","depends_on_id":"poly-qqx9","issue_id":"poly-paee","metadata":"{}","type":"parent-child"}],"dependency_count":0,"dependent_count":0,"description":"Found on a live builder, 2026-09-24. The tracker must never run anything;\nthe runner may be remote (poly-fij), so anything that reaches into a build\nenvironment is wrong by construction, not merely broken today.\n\nTWO VIOLATIONS, both introduced by poly-qqx9 and the only two in the\npackage. There is no subprocess/Popen/os.system anywhere in\ndportsv3/tracker/, and every other agent import is lifecycle (pure\nconstants) or fix_chat/llm (an outbound API call):\n\n  tracker/worktree_source.py:52   from dportsv3.agent import worker\n  tracker/dsynth_tail.py:133      from dportsv3.agent import worker\n\nBoth die as the unprivileged tracker account:\n\n  from_workspace -> worker.emit_diff -> env_paths()\n                 -> `dportsv3 dev-env path` -> require_root()   cli.py:282\n                 -> _exec -> `dev-env exec`  -> require_root()   cli.py:811\n  dsynth_tail.log_path -> worker._dsynth_log_candidates -> env_paths()\n\nVerified on the host: the tracker service runs as `polytropos`, the runner\nas root; chroot as non-root is EPERM; no sudoers grant for the tracker\nuser; no DPORTS_DEV_ENV_CMD in its rc.d script. from_workspace's bare\n`except Exception` swallows all of it, so the working-tree band renders\nnothing and the tail reports \"no log\" -- silently, on every job.\n\nSECOND, INDEPENDENT CAUSE for the band being absent. The finished-job\nfallback reads analysis/rescued/<job_id>.diff, which the rescue path\nwrites only when the harness RAISES. On the live builder:\n\n  analysis/rescued/%    4 rows\n  %changes.diff       289 rows\n\nanalysis/changes.diff is the canonical artifact every patch job writes\n(runner._write_changes_diff). It was passed over because changes.diff is\nper-BUNDLE and a retried bundle's copy may belong to a later job -- which\nis true, and cost the band ~99% of the jobs it could have rendered.\n\nSCOPE HERE: stop executing, and read artifacts only. Delete the worker\ncall in worktree_source; fall back to analysis/changes.diff, labelled,\nsince it may be a sibling job's; remove the tracker's live-tail wiring.\n\nA running job then has no diff to show until the runner publishes one,\nwhich is the follow-up -- and it must SAY that rather than render nothing.\n","id":"poly-paee","issue_type":"bug","labels":["tracker-ui"],"notes":"FIXED 2026-09-24. The tracker no longer runs anything.\n\nworktree_source.from_workspace is DELETED, not repaired, and the module no\nlonger imports agent.worker. A test asserts the absence -- no\nfrom_workspace attribute, no worker import, no subprocess in the file --\nbecause the next person to want a live diff will reach for exactly this.\n\nTHE BAND NOW READS THE CANONICAL ARTIFACT. Order is rescued (per job,\nexact) then analysis/changes.diff (per bundle, 289 on a live builder\nagainst 4 rescued). The per-bundle one is LABELLED \"from the bundle's\ndiff\", with a title saying a retried bundle may share it between jobs --\nthe precision loss is stated rather than hidden, which is what made\npreferring the exact artifact and showing nothing the wrong trade.\n\nA PATCH JOB WITH NOTHING PUBLISHED SAYS SO: #worktree-pending, \"no diff\npublished yet\", and one line explaining the runner publishes when the\nattempt ends. An absent band reads as \"changed nothing\". Only for type\npatch -- on a triage or verify job it would be noise about work that job\nnever does.\n\nTHE TAIL WIRING IS GONE: tracker/dsynth_tail.py deleted, the\n/api/jobs/<id>/dsynth-tail route deleted, and the read dropped from both\nthe job page and the live fragment. The reader is recoverable at commit\n6275522 if poly-pvs2 takes the publish option.\n\nrender.running_tailable_tool and attach_tool_tail STAY, and say in their\nown docstrings that they have no producer. They are pure, they are what any\npublisher would feed, and the pairing they encode -- only a long tool gets\na tail, it hangs on the running row -- is the part worth not rediscovering.\nTheir tests moved to tests/test_render_tool_tail.py; the reader's 11 tests\nwent with the reader.\n\nWHAT IS STILL NOT VISIBLE, and needs the runner: a RUNNING job's diff.\npoly-5tgc. Until then a running patch job shows the pending note and a\nfinished one shows its diff, which is 289 jobs more than before.\n\n4203 tests pass.","owner":"github@dragonflybsd.org","priority":1,"started_at":"2026-09-23T23:51:55Z","status":"closed","title":"The tracker executes runner-side code, so the working tree and the dsynth tail are dead in deployment","updated_at":"2026-09-24T00:02:57Z"}
{"_type":"issue","assignee":"Fred [bot]","close_reason":"Closed","closed_at":"2026-09-23T23:26:04Z","comment_count":0,"created_at":"2026-09-23T23:12:23Z","created_by":"Fred [bot]","dependency_count":0,"dependent_count":0,"description":"Found against a live builder's state.db, 2026-09-24.\n\nactivity_log is a GLOBAL rolling cap: runner.activity_log_max (default\n5000), enforced by a DELETE in runner.py:1877. Observed on a live host:\none day of rows, ids 65074-70073, 5000 rows, and 3 DISTINCT JOBS out of\n1,452 in the database (364 patch, 600 triage, 187 verify, 301 confirm).\nA patch job spends roughly 1,600 rows, so the page holds about the last\nthree jobs.\n\nEvery surface poly-qqx9 added reads activity_log: the now-bar, the\nattempt strip, the turn cards, the working-tree band. On an evicted job\nthey do not degrade, they assert -- a now-bar with nothing running, a\nstrip with no attempts, no turn cards at all, on a job that really did\nrun seven attempts. The old flat table had the same hole; the epic turned\none empty table into five positive claims.\n\nThe obligation is already assigned to this page. jobs.py:421 keeps\nintra-job attempt detail OFF the occurrence aggregate for exactly this\nreason: \"a number that silently becomes 0 when it is evicted is worse\nthan no number. That detail stays on the job page, where the caveat is\nlocal.\" No job-page template states it.\n\nTHE SIGNAL IS EXACT, so this needs no heuristic. jobs and job_events are\nnever pruned (jobs.py:415). A job whose job_events show that it ran,\nwhich has no activity_log rows, and whose last transition predates\nSELECT MIN(ts) FROM activity_log, has been pruned. One extra query.\n\nThree states have to read differently, which is the whole bug:\npruned, nothing-yet (a job just claimed), and has-activity.\n\nDEEPER QUESTION, DELIBERATELY NOT A BLOCKER: the cap is global, so one\nchatty patch job (2,130 llm_turn rows in the observed window) evicts\nevery other job's history. Per-job retention, or simply a larger cap, is\na runner-side change with a disk cost. The page misreporting is a bug\nwhichever way that lands, so fix the page first.\n","id":"poly-a162","issue_type":"bug","labels":["tracker-ui"],"notes":"FIXED 2026-09-24.\n\nTHE DESCRIPTION OVERSTATED THE SYMPTOM, and the real one is narrower and\nworse. The five surfaces do NOT assert: _attempt_strip.html guards on\n{% if strip and strip.attempts %}, _job_activity.html on\n{% if activity_cards %}, now_bar returns None without turns, and the\nworking tree carries an `empty` flag. They all go quiet correctly.\n\nWhat asserted was one sentence, in the else branch nobody reads:\n\n  <div class=\"empty-state\">No activity recorded for this job.</div>\n\nFalse for every evicted job, and on a live builder that is 1,449 of 1,452.\nSaid beside a durable transitions table showing seven attempts, which is\nwhat makes it read as a tracker fault rather than a retention policy.\n\nTHREE STATES NOW READ DIFFERENTLY, which was the acceptance criterion:\n- pruned, nothing left -> \"Activity has aged out\", naming the cap, the\n  oldest row the log still holds, when this job started, and that the\n  lifecycle transitions are durable.\n- nothing yet -> the original sentence, unchanged.\n- partly pruned -> a note above the surviving cards. This is the worst\n  case and was not in the original description: the survivors look like\n  the whole story and the attempt strip counts only them.\n\nactivity_pruning(conn, job_id) asks the two durable sources (jobs and\njob_events are never pruned) and FAILS CLOSED: with an empty activity_log\nthere is no horizon, so it reports not-pruned rather than announcing\npruning it cannot demonstrate.\n\nIt says \"any activity it recorded\", never \"its activity\". Whether a job\nemitted activity at all is unknowable once the rows are gone, and a verify\nor confirm job may legitimately have emitted none -- the same uncertainty\nthat made poly-qqx9.1's Q2 unanswerable.\n\nTHE TRANSCRIPT ROUTE HAD THE SAME SENTENCE and now shares one partial,\n_activity_pruned.html, so the two wordings cannot drift. It is the surface\nthat claims to be the WHOLE transcript, so the remnant note matters more\nthere.\n\nOne defect caught before it shipped: the transcript builds its template\ncontext AFTER its `with _conn()` block closes, so the query had to move\ninside the block rather than being a call in the dict.\n\nThe global-vs-per-job cap question is untouched and still open -- see the\ndescription. The page is honest now whichever way that lands.\n\n4209 tests pass; 8 new in tests/test_activity_pruning.py.","owner":"github@dragonflybsd.org","priority":1,"started_at":"2026-09-23T23:19:26Z","status":"closed","title":"Job page presents a pruned activity log as a job where nothing happened","updated_at":"2026-09-23T23:26:04Z"}
{"_type":"issue","assignee":"Fred [bot]","close_reason":"Closed","closed_at":"2026-09-23T21:57:26Z","comment_count":0,"created_at":"2026-09-23T21:40:20Z","created_by":"Fred [bot]","dependency_count":0,"dependent_count":0,"description":"Verified in code and reproduced against the real app, 2026-09-23.\n\nPOST /v1/artifacts/put-fs records any absolute path as an artifact: the\nonly check is Path(fs_path).is_file() (tracker/routes/ingest_api.py).\nGET /v1/artifacts/get then reads that path and returns the bytes --\nArtifactStore.get_artifact returns (\"fs\", Path(row[\"fs_path\"])) and the\nroute does file_path.read_bytes().\n\nTwo unauthenticated calls read any file the tracker account can read.\nOn a deployed host that account is exactly the one SECRET_MODES grants\ndelivery.token_file (0640) and llm.chat.api_key_file (0640), so one HTTP\nrequest undoes the credential split those modes exist to make. The\ncomment on SECRET_MODES states the intent the split protects.\ntracker.bind defaults to 0.0.0.0.\n\nNeither route has a gate: forbid_anonymous appears 19 times across\ntracker/routes/ and zero times in ingest_api.py, so\ntracker.public_readonly does not cover this.\n\nput_fs_ref also does ON CONFLICT DO UPDATE, so an existing bundle's\nartifact can be repointed. The diff an operator reads before Accept need\nnot be the diff on disk.\n\nNothing in the loop writes fs refs any more. hook_pkg_failure moved the\nfull log to put-blob because a recorded path is unresolvable from\nanother host and unresolvable in a chroot; the only remaining callers\nare the artifact_store_client CLI subcommand and its own tests.\n\nFIX: delete the write path -- the endpoint, ArtifactStore.put_fs_ref and\nthe CLI subcommand -- and confine the read path, so rows already written\ninto a deployed db cannot serve from outside the logs root either. The\nread path has two resolvers that both trust fs_path today:\nArtifactStore.get_artifact for /v1, and render.resolve_artifact_path for\nthe browser routes (/api/bundles/{id}/artifacts/{relpath} and the\nartifact viewer).\n\nConfine to the logs root, not the evidence root: legitimate legacy rows\npoint at dsynth logs beside the evidence tree, not inside it.\n","id":"poly-zjtx","issue_type":"bug","notes":"FIXED 2026-09-23.\n\nWrite path gone: the /v1/artifacts/put-fs endpoint, ArtifactStore.put_fs_ref,\nand the artifact_store_client put-fs subcommand. Nothing in the product can\nwrite a backend='fs' row now, so no caller-controlled path can reach either\nresolver.\n\nRead path confined on /v1: get_artifact runs contained_fs_path(self.logs_root,\n...) and returns None when the recorded path resolves outside it, so the route\n404s. Containment is after realpath, so a symlink planted inside the tree does\nnot escape it. A legacy row naming a dsynth log still serves.\n\nVerified by re-running the reproduction: put-fs 404, a row naming a credential\n404 with an empty body, a row naming a log inside the logs root 200. 4198 tests\npass.\n\nONE HALF NOT DONE, filed as poly-szg2: render.resolve_artifact_path, which the\nbrowser routes use, still serves fs_path as recorded. The same containment\nfails 42 tests in five files whose fixtures write artifacts into tmp_path while\nleaving paths.artifact_root at its production default -- the recorded path and\nthe configured root are unrelated there, so no boundary can hold until those\nfixtures change. Not a live hole with the write path gone; the asymmetry is\nrecorded in a comment at both resolvers rather than left to be discovered.\n\nTHE BOUNDARY IS A JUDGEMENT, not a measurement. No real fs row was available to\nread -- paths.artifact_root points at the builder host. The logs root is where\nthe old hook's dsynth log sat, but a deployed database may hold rows pointing\nelsewhere, and those would now 404 on /v1. poly-szg2 carries the query to run\nagainst a real state.db before the browser side picks the same boundary.","owner":"github@dragonflybsd.org","priority":1,"started_at":"2026-09-23T21:40:46Z","status":"closed","title":"Unauthenticated arbitrary file read: put-fs records any path, /v1/artifacts/get serves it","updated_at":"2026-09-23T21:57:26Z"}
{"_type":"issue","assignee":"Fred [bot]","close_reason":"Implemented. tool_loop emits tool_start at dispatch (attempt, turn, tool, call_id) and tool_call now carries the same call_id; PatchEventDispatcher writes a 'tool_start' activity row and keeps the event in trace_events. Tests: 3 dispatcher + 1 emitter (order is tool_start -> dispatch -> tool_call). Full suite 4065 passed.\n\nROW COST, as the bead asked: exactly one extra row per tool call, so patch and convert jobs grow activity_log by the tool-call share of their rows — roughly +50% on the shapes seen while designing, since tool rows and llm_turn rows run about 1:1. triage, verify and confirm are unaffected: they emit no tool calls at all (poly-qqx9.1). The exact multiplier needs the same live query the audit left open; poly-lvyr owns the global cap.\n\nTWO NOTES FOR THE VIEW BEADS. The new stage is 'tool_start', which does NOT match the existing 'tool calls only' filter (stage LIKE 'tool:%') — deliberate, so the current filter keeps meaning completed calls; poly-qqx9.4 should decide whether the cards fold starts into the tool row. And the rows are visible on the job page NOW, before the cards land: a running tool reads as '<tool> started' in the table.","closed_at":"2026-09-23T16:00:06Z","comment_count":0,"created_at":"2026-09-23T15:21:31Z","created_by":"Fred [bot]","dependencies":[{"created_at":"2026-09-23T15:21:31Z","created_by":"Fred [bot]","depends_on_id":"poly-qqx9","issue_id":"poly-qqx9.2","metadata":"{}","type":"parent-child"},{"created_at":"2026-09-23T15:21:31Z","created_by":"Fred [bot]","depends_on_id":"poly-qqx9.1","issue_id":"poly-qqx9.2","metadata":"{}","type":"blocks"}],"dependency_count":1,"dependent_count":3,"description":"A tool row reaches activity_log only when the tool RETURNS: steps.py:176\nlogs it from an event carrying ev.result and duration_ms. dsynth_build and\ndsynth_test run 41 and 44 minutes on llvm19, so for forty minutes the page\nadds nothing while the live badge keeps reading `live · just now` — the badge\nreflects the poll (agentic-job.js, dpLive, 3s), not the job. A wedged runner\nand a working one are indistinguishable for the most expensive operation the\nloop performs.\n\nEmit a start event at dispatch: tool name, its args, ts, and whatever pairs\nit with the completion row so the end can match the start. Then the\ncompletion row stays exactly as it is.\n\nThree things in this epic depend on it and cannot be honestly built without\nit: the now-bar's \"what is running, for how long\"; the attempt strip's phase\nspans (today only end + duration_ms exist, so a bar chart would be inferring\nits own left edges); and the in-row dsynth tail, which needs to know WHICH\nlog to tail, with which env and flavor, and when to stop.\n\nWatch the cost: this doubles the row count for tool calls, and poly-lvyr is\nalready open on the activity_log global cap. Say in the close note what the\nrow-count increase measured out at.\n","id":"poly-qqx9.2","issue_type":"feature","labels":["agent","tracker-ui"],"notes":"NOTE (poly-qqx9.1, second pass): analysis/tool_trace.jsonl already holds\nevery event verbatim, written after the run. It does not replace this bead —\nit lands when the run is over, and the whole point of the now-bar, the\nattempt spans and the in-row tail is the job that is still going.","owner":"github@dragonflybsd.org","priority":1,"started_at":"2026-09-23T15:56:25Z","status":"closed","title":"Emit tool_start, so a running tool is visible while it runs","updated_at":"2026-09-23T16:00:06Z"}
{"_type":"issue","assignee":"Fred [bot]","close_reason":"Audit complete on the source reading: field table, second-pass corrections, and the per-job-type matrix are on the bead. Findings pushed into poly-qqx9.3, .4, .6, .7, .9 and filed as poly-qqx9.12 and .13. Live confirmation against a builder state.db is three queries, listed on the bead; reopen if they contradict it.","closed_at":"2026-09-23T15:56:00Z","comment_count":0,"created_at":"2026-09-23T15:21:16Z","created_by":"Fred [bot]","dependencies":[{"created_at":"2026-09-23T15:21:16Z","created_by":"Fred [bot]","depends_on_id":"poly-qqx9","issue_id":"poly-qqx9.1","metadata":"{}","type":"parent-child"}],"dependency_count":0,"dependent_count":7,"description":"Before any of the view work: go field by field through specimen 06 and say,\nfor each, whether the tracker already holds it, can derive it, or does not\nhave it at all. Write the answer into this bead as a table. Every other child\nof this epic is sized by that answer, which is why this one blocks them.\n\nThree places where the answer is already known to be NO or MAYBE, found while\nthe design was being drawn — start here, then do the rest properly:\n\n1. THE CARD'S SENTENCE HAS NO SOURCE. Each turn card leads with the model's\n   own prose (\"Hunk #2 rejected — the upstream file moved under the patch\").\n   The llm_turn activity row does not carry it: steps.py:199 logs a formatted\n   token line and sets extra to the event minus 'type', which is counts plus\n   tools_requested. _activity_row.html's `a.extra.text_only` branch confirms\n   it — the page knows a turn was text-only, not what the text was. The prose\n   lives in the session dump the artifact store holds, which\n   agentic_session.html reads as turn cards already. So the card either joins\n   activity to the session dump, or the dispatcher starts recording a short\n   assistant summary per turn. DECIDE WHICH, and say why, before UI-shaping.\n\n2. TOOL ARGS ARE PROSE, NOT FIELDS. The card renders `write_file\n   Makefile.DragonFly.@2026Q3 · 118ms · +3 −1` — name, args and timing as\n   separate cells. What is stored is one summary string per call from\n   runner.py:3896 _summarize_tool_call(tool, args, result), whose own\n   docstring says it is for an at-a-glance message column. extra_json carries\n   attempt, turn, ok, and on failure rc/stderr_tail/stdout_tail — not the\n   args. Either the dispatcher records args, or the card parses the summary,\n   which is fragile and per-tool.\n\n3. NOTHING RECORDS A TOOL STARTING. Confirmed at steps.py:176 — the row is\n   written from the event carrying ev.result and duration_ms, i.e. on return.\n   That is its own bead; note here what depends on it.\n\nTHE REST OF THE FIELD LIST, per band\n\n  Now-bar: attempt n / runner.max_patch_attempts; turn number; the running\n  tool and its start; elapsed; spend vs policy.tier_budgets[tier].max_tokens\n  — and where the job's TIER is recorded, which the budget number needs.\n  Attempt strip: per-attempt span boundaries per phase. attempt_start /\n  attempt_end rows exist; whether phases inside an attempt can be bounded\n  without tool_start is the question.\n  Turn cards: turn id, verdict (derivable from the tools' ok flags?), tokens\n  per turn (yes, extra_json), timestamp, tool rows per above.\n  Working tree: file list with status letters, per-file +/-, which attempt\n  each file arrived in, and the diff. worker.emit_diff(env, origin, relpath)\n  returns the unified diff including untracked files, byte-capped — but it\n  runs where the WORKSPACE is. Say how the tracker gets it, and whether\n  per-attempt attribution exists anywhere or has to be recorded as it happens.\n  Cost: per-turn and per-attempt exist (attempt_end carries billable_tokens\n  and tokens, steps.py:53). Per-port across sibling jobs — does anything\n  aggregate it?\n\nALL FIVE JOB TYPES. triage, patch, convert, verify and confirm share this\npage and nothing branches on type. A field present only for patch jobs is a\nfinding, not a detail — say which bands degrade and to what.\n\nDeliverable: the table, plus a one-line verdict per specimen-06 band —\nbuildable as drawn / buildable with a named runner change / needs a decision.\n","id":"poly-qqx9.1","issue_type":"task","labels":["tracker-ui"],"notes":"OPERATOR NOTES ARE A BAND TOO (poly-qqx9.11, added after this bead was\nwritten). Audit them with the rest: there is no table for them today, so this\nis a schema question rather than a 'is the field there' question. What one\nneeds: job_id, the text, who wrote it, created_ts, state (queued vs\ndelivered), and the turn it landed in — the delivered card is stamped with\nthat. Also say where the runner would read the queue from without a round\ntrip per turn.\n\npoly-pf4a (the fix chat is not persisted) is the precedent not to repeat: a\nnote that exists only in one browser is worse than no note, because the\ntranscript then cannot explain why the agent changed course.\nAUDIT, first pass (2026-09-23). Read: tool_loop.py, attempt_loop.py,\ntriage.py, steps.py, session_dump.py, settings.py, schema.py,\nworker.py, render/activity.py, agentic_queries/*.\n\nHEADLINE: THE CARD'S SENTENCE DOES NOT EXIST IN A DEFAULT DEPLOYMENT.\nThe llm_turn event (tool_loop.py:338) carries prompt/completion/total/\ncached/billable, cumulative_*, tools_requested and text_only — never the\nassistant text. The text survives only in the session dump, and\nrunner.dump_session (settings.py:327) defaults to FALSE and is described as\na debug switch (\"Large, and the single most useful thing to have when an\nattempt goes wrong\"). So on a normal builder there is nothing to render.\n\nTWO THINGS ARE BETTER THAN FEARED\n  - tool_call (tool_loop.py:450) DOES carry args in full, plus the whole\n    result dict and duration_ms. PatchEventDispatcher (steps.py:152) throws\n    the args away and stores _summarize_tool_call's one-liner\n    (runner.py:3896). The args are dropped, not missing.\n  - attempt_start (attempt_loop.py:512) carries its own `budget`, so the\n    now-bar's denominator needs no tier lookup and no settings read.\n\nFIELD TABLE   (present | derivable | MISSING)\n\n  NOW-BAR\n    attempt n, turn ................ present   extra.attempt / extra.turn\n    max attempts ................... derivable runner.max_patch_attempts at\n                                               read time; not stored per job\n    running tool + args + started .. MISSING   row is written on return\n                                               (steps.py:176)\n    elapsed ........................ MISSING   same cause\n    spend .......................... present   cumulative_billable_tokens\n    budget ......................... present   attempt_start.budget\n\n  ATTEMPT STRIP\n    boundaries ..................... present   attempt_start / attempt_end\n    outcome ........................ present   attempt_end.rebuild_ok\n    per-attempt tokens ............. present   attempt_end.billable_tokens\n                                               (render/activity.py already\n                                               folds all three)\n    phase spans within an attempt .. MISSING   only end ts + duration_ms\n\n  TURN CARDS\n    id, ts, tokens ................. present\n    verdict ........................ derivable tool extra.ok, rebuild_ok\n    the model's sentence ........... MISSING   see HEADLINE\n    tool name ...................... present   stage = 'tool:<name>'\n    tool args as fields ............ DROPPED   in the event, not in the row\n    duration, ok, rc, diagnostics .. present   extra (tails capped 2000)\n\n  WORKING TREE\n    the diff ....................... derivable worker.emit_diff(env, origin,\n                                               relpath); _current_diff\n                                               already calls it per retry and\n                                               gives it to the MODEL only\n    the env to call it with ........ MISSING   no env column on jobs; only\n                                               verify_requests.env\n    per-attempt attribution ........ MISSING   nothing records which attempt\n                                               first touched a file\n    job -> occurrence .............. present   jobs.bundle_id (patch/verify\n                                               set it; triage sets\n                                               bundle_dir — tests/\n                                               test_jobs_bundle_id_fk.py)\n\n  IN-ROW TAIL\n    path ........................... present   env_paths(env).writable/work/\n                                               dsynth/logs/<stem>[@flavor].log\n    flavor ......................... present   jobs.flavor\n    env ............................ MISSING   as above\n    byte cap ....................... present   worker.dsynth_log\n\n  OPERATOR NOTES ................... NOTHING   no table, no route\n\n  COST\n    per turn / attempt / job ....... present\n    per port across sibling jobs ... MISSING   list_port_bundles carries no\n                                               spend\n\nDEGRADATION ACROSS JOB TYPES. attempt_start/attempt_end come from\nattempt_loop, i.e. patch-shaped work. triage.py:132 emits llm_turn as well,\nso a triage job has turns but no attempts; the strip and the attempt\ngrouping collapse to one implicit group for triage/verify/confirm. Design\nthat deliberately rather than discovering it.\n\nVERDICT PER BAND\n  now-bar ......... needs tool_start; everything else is there\n  attempt strip ... needs tool_start for honest spans\n  turn cards ...... buildable minus the prose, which needs a decision\n  working tree .... needs env, and a decision on attribution\n  in-row tail ..... needs env; local-only until poly-fij.7\n  operator notes .. needs a table\n  cost ............ buildable; per-port wants a small aggregate\nAUDIT, second pass — three corrections to the table above and two\nsimplifications. Read: steps.py rescue path, runner.py _write_tool_trace,\nenv_resolver.py, render/sessions.py.\n\nMISSED\n\n1. analysis/tool_trace.jsonl ALREADY HOLDS EVERY EVENT VERBATIM.\n   PatchEventDispatcher appends each event to trace_events (steps.py:108)\n   and the runner persists them as JSONL (runner.py:4006), written in the\n   rescue path (steps.py:841) so it survives however the run ended. Its own\n   docstring: \"its put_file arguments alone let a human reconstruct the\n   edits\". render/sessions.py already loads it and indexes llm_turn events\n   into a turn->tokens map for the session viewer.\n   So ARGS ARE NOT LOST — they are unavailable LIVE (the file is written\n   after the run) and not queryable per row. That weakens the \"stop dropping\n   args\" case from data loss to live availability, which is still the case\n   the job page needs, but it is a smaller claim.\n\n2. THE TERMINAL-JOB WORKING TREE NEEDS NOTHING NEW. The same rescue path\n   writes analysis/changes.diff — whole tree vs the bundle base with the\n   --intent-to-add bracket, so never-committed files surface as additions —\n   and analysis/rescued/<job_id>.diff, the same bytes at a path no later job\n   overwrites. Both are written when non-empty even when the attempt failed.\n   So the band can be phased: terminal jobs render from the rescued diff,\n   which is already per-job and already durable; only a RUNNING job needs\n   emit_diff and therefore the env.\n\n3. WHY THE ENV IS ABSENT, precisely. env_resolver's top precedence is\n   `job.dev_env` — but that lives in the queue FILE, written as a\n   `dev_env=` line (runner.py:2840), not in the DB. get_job is\n   `SELECT * FROM jobs`, so the tracker never sees it. The column finding\n   stands; it is one column written at claim time, not a new subsystem.\n\nOVER-COMPLICATED — DROPPED\n\n4. Per-attempt file attribution does not need its own bead or its own\n   storage. Once a write tool's args are on the row, attribution is a query:\n   the first attempt whose write-tool row names that path. attempt_loop.py:109\n   already defines _WRITE_TOOLS for exactly this set. Fold it into the\n   working-tree bead.\n\n5. THE PROSE IS NOT A BLOCKER. A card without it still carries turn id,\n   verdict, tokens, duration and its tool rows — already far past the table\n   it replaces. Do not enable runner.dump_session for this: it is large by\n   its own description, and its artifacts land per attempt, so they are not\n   live within the attempt anyway. Treat a capped (~300 char) assistant\n   summary on the llm_turn event as an ENHANCEMENT, and make the card\n   degrade cleanly when it is absent — which it must do regardless, for\n   every job that already ran.\n\nNET EFFECT ON THE PLAN\n  keystone ....... tool_start (poly-qqx9.2), unchanged\n  new, small ..... dev_env on the jobs row, written at claim time\n  new, small ..... tool args (+ optional capped assistant summary) into the\n                   activity row's extra_json — a change inside\n                   PatchEventDispatcher, no new event\n  poly-qqx9.7 .... phase it: terminal from rescued/<job_id>.diff first\n                   (zero new data), live from emit_diff second\n  poly-qqx9.9 .... add the per-port aggregate\n  dropped ........ a separate attribution bead; enabling dump_session\nAUDIT, third pass — the per-job-type answer, which is the one that shapes\nthe cards. Read: triage.py, steps.py (_triage_event, PatchEventDispatcher,\nboth construction sites), verify_fix.py, runner.py activity stages.\n\nWHAT EACH JOB TYPE ACTUALLY WRITES\n\n  patch    attempt_start / attempt_end, llm_turn (attempt+turn), tool:<name>,\n           decision, attempt_workspace_reset, observations_masked.\n           Everything the design draws.\n  convert  the same — steps.py:1445 builds a second PatchEventDispatcher for\n           that flow, so it behaves like patch.\n  triage   llm_turn ONLY, through _triage_event (steps.py:358), which returns\n           early on every other event type. Its extra carries phase=\"triage\",\n           turn, snippet_round and the token fields — and NO `attempt` key.\n           No tool rows. No attempts.\n  verify   nothing of the kind. No on_event anywhere in verify_fix.py; the\n           job's activity is flat operational stages written by the runner:\n           verify_branch_checkout, verify_complete, verify_failed,\n           verify_branch_dropped, ...\n  confirm  same shape as verify — a build, not an agent loop.\n\nCONSEQUENCES, and they are design consequences rather than details\n\n  - The turn card is a PATCH/CONVERT shape. A triage job renders cards with\n    no tool rows and no attempt grouping (group_activity_by_attempt puts\n    attempt-less rows in a \"setup\" group, which is already the right\n    behaviour). A verify or confirm job has no turns AT ALL — the cards\n    band must render its operational stages instead, or say plainly that\n    this job type has none. Decide that in poly-qqx9.4; do not let it be\n    discovered by whoever opens a confirm job first.\n  - The attempt strip (poly-qqx9.6) exists only for patch and convert. For\n    the other three it should be absent, not empty.\n\nTWO CORRECTIONS TO THE FIRST-PASS TABLE\n\n  - MAX ATTEMPTS IS STORED, not only a setting. attempt_start carries\n    `iterations` and the dispatcher persists the whole event\n    (steps.py:139: extra = every field but type). So \"attempt 3 of N\" reads\n    N from the row that opened the attempt — the value the run actually\n    used — rather than from runner.max_patch_attempts at render time, which\n    would misreport any job that ran under a different setting.\n  - `ok` IS ALWAYS PRESENT as a key on a tool row, including as None:\n    steps.py:154 sets it unconditionally from the result dict. The card's\n    verdict logic can rely on the key existing and must treat None as\n    \"no verdict\", not as failure.\n\nSTILL UNVERIFIED AGAINST LIVE DATA\n\n  Everything above is read from source. It should be confirmed once against\n  a real builder's state.db — it is three queries, not an investigation:\n\n    SELECT stage, COUNT(*) FROM activity_log GROUP BY stage;\n    SELECT j.type, a.stage, COUNT(*) FROM activity_log a\n      JOIN jobs j USING (job_id) GROUP BY j.type, a.stage;\n    SELECT extra_json FROM activity_log WHERE stage='attempt_start' LIMIT 3;\n\n  The third is the one that matters most: it confirms `budget` and\n  `iterations` are really in the stored JSON and not only in the event.\n\n  This bead is closed on the source reading, which is what the view beads\n  need to be written against. If the queries contradict any line above,\n  reopen it rather than patching around it downstream.\nVERIFIED AGAINST LIVE DATA (2026-09-24). The three queries above were run\nagainst a builder's state.db. Not reopening -- see the third block.\n\nQ3 CONFIRMS WHAT IT WAS WRITTEN FOR. The denominators really are in the\nstored JSON, not only in the event:\n\n  {\"attempt\": 7, \"iterations\": 8, \"tokens_used_so_far\": 14335613,\n   \"total_tokens_so_far\": 292688032, \"cached_tokens_so_far\": 278352419,\n   \"budget\": 30000000}\n\nONE LINE IN THE FIELD TABLE ABOVE IS WRONG. \"max attempts .... derivable\nrunner.max_patch_attempts at read time; not stored per job\" -- it IS\nstored per job, as attempt_start.iterations, on every row. The live host\nruns iterations 8 with attempts 1..7 observed, so a now-bar reading the\nsetting instead (default 3) would render \"attempt 7 of 3\".\n\nNOT REOPENED, deliberately: the only consumer already diverged correctly\non its own. render/nowbar.py reads attempt_extra[\"iterations\"] and its\ndocstring argues the opposite of this table -- \"THE DENOMINATORS ARE\nSTORED, NOT SETTINGS ... reading runner.max_patch_attempts at render time\nwould misreport every job\". Reopening an audit whose sole consumer is\nalready right changes no code. The correction is recorded here so the next\nreader does not re-litigate it.\n\nTwo figures in the EPIC's description are contradicted too: the attempt\ncap is not 3, and the AUTO budget is not 30k (30,000,000 live;\nsettings.py:526 still defaults to 30000, so the host overrides it). Both\nare read from the stored event, so nothing renders wrong either way.\n\nQ1 -- 5000 rows: llm_turn 2130, tool:grep 1031, tool:get_file 531,\nobservations_masked 165, tool_start 161, attempt_start 7, attempt_end 6\n(one attempt still open). tool_start exists and 317 rows carry args in\nextra_json, so poly-qqx9.2 and poly-qqx9.13 are deployed and emitting.\n\nQ2 SAYS NOTHING ABOUT verify OR confirm, AND THIS DATABASE CANNOT.\nactivity_log is a GLOBAL rolling cap -- runner.activity_log_max, 5000,\nDELETEd in runner.py:1877. The window held one day and 3 distinct jobs out\nof 1,452 (364 patch, 600 triage, 187 verify, 301 confirm). Zero\nverify%/confirm% stages survive in it, which is eviction and not evidence\n-- an earlier reading of this same output concluded they emit nothing,\nwhich was wrong. Do not re-run the query expecting an answer: catch a\nverify job live, or raise the cap for one run.\n\nThat cap also makes the job page misreport every older job. Filed as\npoly-a162.","owner":"github@dragonflybsd.org","priority":1,"started_at":"2026-09-23T15:54:01Z","status":"closed","title":"Data audit: does the tracker hold every field the job-detail design renders?","updated_at":"2026-09-23T23:13:14Z"}
{"_type":"issue","comment_count":0,"created_at":"2026-09-23T15:20:48Z","created_by":"Fred [bot]","dependency_count":0,"dependent_count":0,"description":"The job detail page renders every kind of runner work — triage, patch,\nconvert, verify, confirm — as one reverse-chronological table inside a 75vh\nscroller. Nothing in it branches on job.type; the only fork is live vs\nterminal, and the terminal branch is the better one. An operator arrives with\nfive questions and the page answers none of them:\n\n  Q1 which attempt am I on\n  Q2 what is it doing right now\n  Q3 what has this cost\n  Q4 what has it changed\n  Q5 how do I tell it something\n\nMeasured on the page as it stands: the header says `patching` and nothing\nelse; the only \"attempt\" shown is `Recent attempts 0/3 failures in last 2h`,\nwhich is the port budget over a time window, not this job's attempt; a tool\nrow is written only when the tool RETURNS (steps.py:176 logs it with\nev.result and duration_ms), so a 41-minute dsynth build writes nothing for 41\nminutes while the badge reads `live · just now` because it reflects the poll;\nthe page never shows a diff and never links to its own occurrence, though\njobs.bundle_id exists and attempt_loop._current_diff() hands the carried\noverlay diff to the MODEL at every retry; and the only control is\n`Abandon job (mark dead)`.\n\nDESIGN SOURCE\n\n  Artifact \"Five Ways to Watch a Job\",\n  https://claude.ai/artifact/9CK76aSHpv8oF36GZUrXwV, version 9, read\n  2026-09-23. Seven specimens; SPECIMEN 06 (\"One page, all five\") is the\n  target and the thing to compare against. SPECIMEN 07 records the placement\n  decision that produced it. Re-read before starting; if the artifact has\n  moved on, correct these children rather than working from this text.\n\n  The specimens are driven by schema-shaped SAMPLE data. Field names and\n  vocabularies are the tracker's; the numbers are invented. Two in\n  particular must NOT be copied: attempt \"3 of 4\" (the real denominator is\n  runner.max_patch_attempts, default 3) and the \"400,000\" token budget (the\n  real one is policy.tier_budgets[<tier>].max_tokens — AUTO 30k, ASSIST\n  120k, CONVERT 150k, MANUAL never runs the agent).\n\nTHE SHAPE, as 06 settles it\n\n  1. Pinned now-bar — attempt n of max, turn, the tool running now with a\n     ticking elapsed, spend against the tier budget.\n  2. Attempt strip — one track per attempt, segmented by what consumed the\n     wall clock. The shape of a patch job is two dsynth builds and a\n     rounding error of thinking, and a table cannot show that.\n  3. Turn cards, full width — one per model turn, verdict carried by a\n     glyph (passed / tool failed / running / attempt boundary), tool calls\n     nested inside with their args, elapsed and outcome, diagnostics folded\n     under the call that produced them and open by default when they failed.\n     Last 5 only; the whole transcript is one click away.\n  4. Working tree band, full width — files changed with per-attempt\n     attribution and the diff beside them.\n  5. No separate tail pane: a build's output belongs to the tool row running\n     it, and goes away with it.\n\n  Placement of the tree was decided in specimen 07 against three\n  alternatives (sticky rail, inline-only, drawer). Band below won: the diff\n  gets full width, and the rail's permanent ~330px tax buys something you\n  look at intermittently. If that is revisited, 07 is where the argument is.\n\n  6. A composer at the foot: an operator note joins the transcript as its own\n     card and is delivered ON THE NEXT TURN — the moment the running tool\n     returns — not at the attempt boundary. Specimen 06 shows both states, a\n     queued note and a delivered one; the delivered one is placed where it\n     explains the turn that followed it.\n\n  Q5 is the only item that changes what the agent DOES rather than what an\n  operator can see, so it carries the most runner risk in this epic. It was\n  added to 06 deliberately after the rest was settled (version 9 of the\n  artifact); specimen 05 is the wider exploration, including the pause and\n  approval-gate variants, which are NOT in scope here.\n\nWHY A NEW EPIC AND NOT poly-x3pg\n\n  poly-x3pg exists to make the built views match the operations console mock.\n  That mock's own job detail (pipelineJobDetail) is an activity TABLE with a\n  facts rail — this work deliberately replaces it. Filing it under x3pg would\n  make that epic unclosable and blur which source measures what. Instead one\n  child here ports the agreed design INTO the console mock, so the console\n  keeps a single authoritative source afterwards.\n\nCLOSING REQUIREMENT (poly-ez41)\n\n  Render specimen 06 and the built page at the same width, side by side, and\n  look at them. Every surviving divergence is a decision and gets written\n  down in the child that made it, with its measurement or its reason.\n","id":"poly-qqx9","issue_type":"epic","labels":["tracker-ui"],"notes":"\nDESIGN SOURCE, SETTLED (poly-qqx9.10, 2026-09-23)\n\nArtifact \"Polytropos Operations Console\",\nhttps://claude.ai/artifact/6UXFSQDntZKwsrwC1rLjgM, VERSION 11, published\n2026-09-23. pipelineJobDetail is now this epic's design, ported from\nspecimen 06 into the console's own vocabulary. That page is the one\nauthoritative source for the job-detail surface from here on, and it is\nwhat poly-x3pg should measure the built page against.\n\nThe intermediate remains readable and is no longer the reference:\nartifact \"Five Ways to Watch a Job\",\nhttps://claude.ai/artifact/9CK76aSHpv8oF36GZUrXwV, version 9, read\n2026-09-23. Specimen 06 is the layout the port carries; specimen 07\nrecords the placement decision behind it.\n\nTwo sources for one surface is the state this settles. The warning above\nabout the specimens' invented numbers still applies to anyone reading\nthem: the port uses the console's own sample instead (attempt 3 of 3,\n264,110 of 400,000, the figures ACTIVITY already records for j-10442).\n\nSIDE-BY-SIDE, DONE (2026-09-23)\n\nBoth surfaces rendered at 1500px in headless Chrome and put in two\ncolumns: the design (console mock v11, pipelineJobDetail) beside the\nbuilt page (/agentic/jobs/pj-af31c2 on a seeded db with a running\ndsynth, a failed tool, a queued operator note and a rescued diff).\n\nTHEY READ AS THE SAME APPLICATION. Same order, same vocabulary, same\ncolours: pinned now-bar; attempt strip with per-attempt billable beside\neach track; turn cards newest-first with the queued note pinned above\nthem; the build's tail inside the running tool row with \"last line N\nago\"; the working-tree band; the composer; the facts and token panels;\nthe flat table collapsed under them.\n\nTHE COMPARISON FOUND A REAL DEFECT, which is what it is for. The built\npage's working-tree band was ABSENT: a live emit_diff read that returns\nan ok result whose body is not a diff counted as \"the live read worked\",\nso the rescued artifact behind it was never reached and the band hid\nitself on a job that had changes recorded. from_workspace now requires\nthe body to contain \"diff --git \" before it counts. Fixed and tested.\n\nTWO DIVERGENCES SURVIVE. Both are decisions, not accidents:\n\n1. ORDER. The built page keeps the summary card and the lifecycle-\n   transitions table between the attempt strip and the turn cards -- on\n   the seeded job that is ~600px of chrome between \"where is it\" and\n   \"what is it doing\". The design puts the facts in a panel at the foot\n   and has no transitions table at all. The facts and the transitions\n   are real records that predate this epic and nothing here asked to\n   move them, so they stayed. Filed as poly-qqx9.14 rather than changed\n   under cover of this epic.\n\n2. THE FACTS ARE COLLAPSED. The built page's Token usage and Prior\n   attempts are section-collapse disclosures at the foot; the design\n   has Job + Token cost always open beside the composer. Deliberate:\n   those two sections predate this epic in that form, the billable\n   figure an operator needs at a glance is already in the now-bar, and\n   the largest turn is named in the collapsed summary line (poly-qqx9.9)\n   so the card's headline reads without opening it.","owner":"github@dragonflybsd.org","priority":1,"started_at":"2026-09-23T15:54:00Z","status":"in_progress","title":"EPIC: Job detail — what the agent is doing, what it cost, what it changed","updated_at":"2026-09-23T19:28:16Z"}
{"_type":"issue","comment_count":0,"created_at":"2026-09-20T21:31:17Z","created_by":"Fred [bot]","dependency_count":0,"dependent_count":0,"description":"`reapply` (compose --origin) writes straight into the live output tree, with no\nscratch+reconcile step. Every file it touches gets a fresh mtime even when the\ncontent is byte-identical. dsynth's port-change detector folds\n`mtime + size + path` into a per-port CRC (`subs.c::crcDirTree`), never reading\nfile contents, so an unchanged port looks changed and its package is deleted and\nrebuilt.\n\nMEASURED, 2026-09-20, devel/libcxx22 in the 2026Q3 dev-env:\n\n  reapply devel/libcxx22   ->   ops=0 applied=0   (nothing changed)\n  3 of 10 files got new mtimes\n  content verified byte-identical against a regen snapshot\n\nThe three were the generated and materialized files -- `Makefile`,\n`files/patch-libcxx_include_____config`,\n`files/patch-libcxxabi_src_cxa__thread__atexit.cpp`. The other seven arrive via\n`shutil.copy2`, which carries the source mtime across and is stable either way.\n\nFull compose is NOT affected: two consecutive `regen` runs produced 181,019\nfiles with identical content AND zero differing mtimes.\n\nOBSERVED COST, same session: `dsynth debug www/chromium` deleted\n`llvm22-22.1.7_1.pkg` and `llvm22-lite-22.1.7_1.pkg` with \"port content changed\nCRC d23c00ed/ea1ae53d\", minutes after llvm22 had built successfully and with no\ncontent change. `apply-and-build` had reapplied it. Recovery cost 21 minutes,\nand only because ccache was warm; cold it is ~2h53m.\n\nCAUSE, `dportsv3/compose.py:102,133`:\n\n    incremental = bool(requested_origins)\n    use_scratch = not dry_run and not incremental\n\nThe scratch+`reconcile` path exists and does exactly the right thing --\n`fsutils.reconcile`'s docstring cites `subs.c::crcDirTree` by name -- but\nincremental composes are excluded deliberately, on the comment's premise that\nthey \"are explicit per-port operator actions where rebuilding the selected ports\nis expected.\"\n\nThat premise is false for the dominant caller. `apply-and-build` reapplies before\nEVERY build, including the majority where nothing changed, so the exclusion\nconverts a no-op recompose into a forced rebuild of the port and its dependents.\n\nThe premise is also recoverable: if a port genuinely changed, `reconcile`\nreplaces the file and the mtime bumps anyway, so the intended rebuild still\nhappens. Using scratch+reconcile unconditionally appears to lose nothing.\n\nRelated: poly-7ska (agent build cost, different mechanism -- dsynth test\nforce-rebuilds by design). poly-mdx (closed) was a different bug in the same\nreconcile primitive.\n","id":"poly-czcw","issue_type":"bug","owner":"github@dragonflybsd.org","priority":1,"status":"open","title":"reapply writes with fresh mtimes, so dsynth force-rebuilds ports whose content never changed","updated_at":"2026-09-20T21:31:17Z"}
{"_type":"issue","comment_count":0,"created_at":"2026-09-17T08:34:34Z","created_by":"Fred [bot]","dependencies":[{"created_at":"2026-09-17T08:34:36Z","created_by":"Fred [bot]","depends_on_id":"poly-dq5","issue_id":"poly-l18e.4","metadata":"{}","type":"relates-to"},{"created_at":"2026-09-17T08:34:34Z","created_by":"Fred [bot]","depends_on_id":"poly-l18e","issue_id":"poly-l18e.4","metadata":"{}","type":"parent-child"}],"dependency_count":0,"dependent_count":0,"description":"Within an attempt the agent has no cheap way to ask \"which patches still\nreject?\", so it asks dsynth, one patch per build.\n\nTWO GAPS\n\n1. make_extract and make_patch never clean (worker.py:3071, 3243). The\nframework's cookies make a repeat `make extract` a no-op, and after a\nfailed do-patch WRKSRC stays half-patched, so the next make_patch\nre-applies on top of it and rejects patches that are fine. make_patch's\nown docstring says \"run it once per extract\" (worker.py:3268).\npoly-dq5 fixed this between attempts only: reset_attempt_workspace\ncleans the WRKDIR when attempt_idx > 1 (worker.py:1416).\n\n2. do-patch stops at the first failing patch, and dsynth runs it that\nway, so each build surfaces exactly one stale patch.\n\nOBSERVED (www/qt6-webengine, 2026-09-17, attempt 1)\n\n6.11.1 moved or rewrote many files the dragonfly/ patches touch (the\noverlay has 114 materialize lines).\n  07:06-07:16  make_patch x4, all FAIL. Note at 07:13: the reject \"is a\n               stale-extract artifact, not a real framework failure\".\n  07:16-08:32  no further make_patch. 27 dsynth_build rounds by T396,\n               each 2-4 min, each finding one patch to re-cut or drop.\nEvery build was followed by a prompt-cache miss (14 of 14 measured at\nT289, ~100-145k billable each).\n\nTHE FRAMEWORK ALREADY HAS THE PIECE\n\nPATCH_CONTINUE_ON_FAIL=yes (bsd.port.mk:3240, Scripts/do-patch.sh:43)\nkeeps applying after a failure and prints, per directory:\n  ===> Cleanly applied <dir> patch(es) ...\n  ===> FAILED to apply cleanly <dir> patch(es) ...\nDeltaPorts' Mk diff (special/Mk/diffs/@2026Q3/Scripts_do-patch.sh.diff)\nroutes dragonfly/ through the same patch_from_directory, so one run\ncovers files/ and dragonfly/.\n\nDESIGN\n\n- make_patch runs `make clean patch PATCH_CONTINUE_ON_FAIL=yes`: a fresh\n  extract every call, like dsynth, and every reject in one call. Parse\n  the two lines above into failed/applied lists per directory.\n- Say in the result that a patch after a failed one on the same file can\n  fail only because of it, and that a failed run's WRKSRC is not a\n  baseline for dupe/genpatch; a clean run's is.\n- make_extract gets the same clean-first semantics, and the tool\n  descriptions say a call resets WRKSRC, discarding dupe'd edits.\n- Do not clean through _clean_port_workdir as is: it also drops\n  _MATERIALIZE_STATE, which makes dsynth_build refuse with\n  stale_compose (poly-dq5 notes, item 4).\n- Prompt: after a dsynth_build that fails in the patch phase, run\n  make_patch to list every reject before re-cutting any.\n\nCost: one extract per call (~70 s for qt6-webengine) against a dsynth\nround of 2-4 min plus a full cache re-bill.\n\nMEASURE: dsynth_build rounds per job that end in the patch phase;\nmake_patch calls; builds before the first one that reaches configure.\n","id":"poly-l18e.4","issue_type":"bug","owner":"github@dragonflybsd.org","priority":1,"status":"open","title":"make_patch cannot reset a half-patched WRKSRC and stops at the first reject, so the agent finds stale patches one dsynth build at a time","updated_at":"2026-09-17T08:34:34Z"}
{"_type":"issue","close_reason":"Cleanup stage 2 composes --delta-root /work/ports-main directly instead of via reapply (which hardcodes /work/DeltaPorts, the job's worktree symlink), and does it per-origin so one uncomposable origin no longer skips the rest. aca455c. Tests: test_cleanup_composes_from_baseline_not_the_worktree, test_cleanup_continues_past_a_sibling_that_cannot_compose, test_cleanup_baseline_compose_failure_does_not_flip_ok. NOT covered: worker.reset_port stage 2 has the same defect on the patch path, knowingly left by poly-15l but firing far more often.","closed_at":"2026-09-14T16:44:50Z","comment_count":0,"created_at":"2026-09-14T15:21:05Z","created_by":"Fred [bot]","dependencies":[{"created_at":"2026-09-14T17:21:05Z","created_by":"Fred [bot]","depends_on_id":"poly-lt5q.1","issue_id":"poly-mdf7","metadata":"{}","type":"discovered-from"}],"dependency_count":0,"dependent_count":0,"description":"verify's post-build cleanup can no longer restore the compose tree, so a\nfailed (or any) verify leaves /work/artifacts/compose/TARGET/ carrying the\nverify-applied state instead of HEAD.\n\ncli.py:394-410 documents the cleanup as three stages:\n\n  1. make clean against the per-origin compose root\n  2. substrate reset (load-bearing) - restore ports/ORIGIN/ to HEAD\n  3. reapply against the now-reset substrate so\n     /work/artifacts/compose/TARGET/ORIGIN/ reflects HEAD rather than the\n     verify-applied state\n\nStage 2 was removed (cli.py:446-456). The stated reason: \"Since B1 a verify\nrun gets its own worktree, destroyed with the job, so there is nothing to\nundo.\"\n\nThat holds for the substrate and not for the compose tree. PORTS_DIR is\n/work/DeltaPorts (layout.py:35), inside the per-job worktree and discarded\nwith it. DPORTS_COMPOSE_ROOT is /work/artifacts/compose/TARGET - env-level\nshared state, outside the worktree. Destroying the worktree reverts the first\nand leaves the second untouched.\n\nSo stage 3 now reapplies against a substrate that was never reset, and\nre-derives the verify state rather than HEAD. The header comment still\ndescribes the stage the code no longer has.\n\n## Observed\n\n2026-09-14, after the verify of lang_python314-20260912-021121Z failed:\n\n    delta root   ports/lang/python314/overlay.dops   115 bytes, bootstrap only\n    compose tree lang/python314/dragonfly/           6 patch files\n    compose tree lang/python314/pkg-plist            5 PYTHON_PLATFORM, 0 _freebsd_\n\nThe compose tree claims six DragonFly patches that the overlay does not\nproduce. A dsynth run against that tree builds something the source cannot\nexplain.\n\nNot additive residue: fsutils.py:132 copy_tree does rmtree + copytree and\nplan_types.py:96 calls it on every incremental compose, so the output dir is\nfully replaced each time. copytree preserves source mtimes, which is why the\ncomposed Makefile still carries its upstream date while pkg-plist and\ndragonfly/ carry the compose timestamp.\n\nIn this instance the cleanup also aborted early on the chained\n`reapply MASTER && reapply SLAVE` (poly-lt5q.1), so stage 3 never ran at all.\nBut a clean run reproduces the same end state, so fixing poly-lt5q.1 does not\nfix this.\n\n## Scope\n\nNot slave-specific. Any verify whose diff composes leaves the tree dirty; the\nnext build or operator inspection of the env starts from it. Same family as\npoly-e56 (symlink not restored) and poly-451 (bootstrap written to the shared\ncheckout, composed in a worktree that lacks it): shared env state versus\nper-job worktree.\n\n## Options\n\n- reset the substrate before stage 3, restoring what the removal dropped\n- have stage 3 compose explicitly from baseline HEAD rather than from\n  whatever the worktree holds\n- make the compose root per-job so discarding the job discards the tree, and\n  drop stage 3 entirely\n","id":"poly-mdf7","issue_type":"bug","labels":["agent","dev-env","verify"],"notes":"Revisited against the code and the live env.\n\nConfirmed in the env after the failed verify: /work/DeltaPorts -> ports-main,\nports-main on master, no worktrees left. Substrate clean, compose tree dirty -\nexactly the split the bead describes.\n\nTwo things the first pass missed.\n\nworker.reset_port already documents this defect on the agent side, in detail,\nand cites poly-15l (closed, \"The compose tree is regenerated on the wrong side\nof the worktree reset\"). poly-15l's resolution was compose-at-start in each\nconsumer, not a working cleanup. So build correctness does not rest on this:\nverify's own build-stage reapply composes the worktree before dtest, and the\nagent's dsynth_build refuses without a materialize_dports in the attempt. The\ndamage is a shared tree that contradicts the repository for anything reading\nit outside the tooling.\n\nReinstating the substrate reset was not an option: its absence is pinned by\ntest_diff_path_runs_post_build_cleanup (\"The source checkout does not: it goes\nwith the worktree\").\n\n## Implemented\n\ndev-env/dports_dev_env/cli.py, _post_build_cleanup stage 2 now runs `compose\n--delta-root /work/ports-main` directly instead of going through `reapply`,\nwhich hardcodes --delta-root /work/DeltaPorts. Nothing is reset; the worktree\nis left alone. Also per-origin rather than `reapply a && reapply b`, so one\norigin that cannot compose no longer skips the rest - which is what let\npoly-lt5q.1 take the whole cleanup down.\n\nTests: test_cleanup_composes_from_baseline_not_the_worktree,\ntest_cleanup_continues_past_a_sibling_that_cannot_compose,\ntest_cleanup_baseline_compose_failure_does_not_flip_ok. Suite 4016 passed.\n\n## Left open\n\nworker.reset_port stage 2 has the identical defect on the patch path and is\nuntouched here. poly-15l knowingly left it - its docstring says so - but it\nfires after every patch job, not just verify, so it is the more frequent\nsource of a contradictory tree.","owner":"github@dragonflybsd.org","priority":1,"status":"closed","title":"verify cleanup lost its substrate reset, so the compose tree keeps the verify-applied state","updated_at":"2026-09-14T16:44:50Z"}
{"_type":"issue","comment_count":0,"created_at":"2026-09-14T13:51:25Z","created_by":"Fred [bot]","dependencies":[{"created_at":"2026-09-14T15:51:25Z","created_by":"Fred [bot]","depends_on_id":"poly-lt5q","issue_id":"poly-lt5q.1","metadata":"{}","type":"parent-child"}],"dependency_count":0,"dependent_count":0,"description":"Sibling verification and compose origin discovery were never reconciled, so\nverify fails before it builds anything for every master port that has a slave.\n\npoly-lt5q made two changes that work against each other:\n\n- a slave's fix is authored on the master, so the slave keeps no overlay of\n  its own (agent/steps.py patch_origin)\n- the verdict must build the master and every slave, passed to the dev-env as\n  `also` (verify_fix.py:171 sibling_origins_for, call site :314)\n\ncompose disagrees about what an origin is. compose_discovery.py:148\ndiscover_overlay_contexts walks delta_root/ports/CATEGORY/PORT and only a\ndirectory counts. compose_stages.py:524 then errors on any selected origin it\ndid not discover. A slave that correctly has no overlay directory is therefore\nguaranteed to be rejected:\n\n    E_COMPOSE_SELECTED_ORIGIN_NOT_FOUND\n\ndev-env cli.py:333 chains the stage as `reapply MASTER && reapply SLAVE`\nand cli.py:524 returns the moment it fails, so dsynth never runs.\n\n## Observed\n\nverify request 174, bundle lang_python314-20260912-021121Z, 2026-09-14T12:53:40Z:\n\n    apply_exit=0  reapply_exit=2  dsynth_exit=null\n    compose 1: ports=1 ops=11 applied=11 errors=0     (lang/python314)\n    compose 2: ports=0 ops=0  applied=0  errors=1     (lang/python314t)\n\nThe fix under test builds green under the patch agent's own dsynth_build, but\ncannot pass the verify gate, so it cannot reach accept. lang/python313 ->\nlang/python313t will fail identically.\n\n## Two smaller faults in the same path\n\nThe fail-open guards the wrong thing. sibling_origins_for returns [] when the\nprobe raises (docstring: \"a probe failure must not turn a verifiable fix into\nan unverifiable one\"), but here the probe succeeds and yields an origin that\ncannot be composed. The call site passes it straight through with no guard.\n\nThe post-build cleanup reapply in the finally (cli.py:463-475) fails for the\nsame reason and leaves the tree inconsistent: after the failed verify the\ndelta root carried a 115-byte bootstrap overlay for lang/python314 while the\ncomposed tree still held all six dragonfly/ patch files.\n\n## Options\n\n- compose resolves a slave through its master (MASTERDIR), matching what\n  patch_origin already does. Fixes the cause.\n- move the fail-open from the probe to the use, so an uncomposable sibling\n  degrades to a visibly partial verdict (master verified, sibling recorded as\n  unverifiable with a reason) instead of failing the gate.\n- whatever authors a master overlay also emits the bootstrap header for each\n  sibling, so the directory always exists.\n\nThe bootstrap header is load-bearing and reads as scaffolding\n(\"Step 48 bootstrap: deterministic header; patch authors the body\"). Nothing\nin its name says it is what registers an origin with compose.\n","id":"poly-lt5q.1","issue_type":"bug","labels":["agent","deltaports","slave-ports"],"owner":"github@dragonflybsd.org","priority":1,"status":"open","title":"verify cannot compose a slave port, so no master with a slave can pass the gate","updated_at":"2026-09-14T13:51:45Z"}
{"_type":"issue","acceptance_criteria":"The rail scrolls inside itself with the page body still. The detail pane scrolls on its own. Selecting a row leaves the queue where it was, which is what poly-x3pg.6 was for. The test asserts the height constraint and not only the overflow property.","close_reason":"Closed","closed_at":"2026-09-13T18:13:47Z","comment_count":0,"created_at":"2026-09-13T18:09:36Z","created_by":"Fred [bot]","dependencies":[{"created_at":"2026-09-13T20:09:36Z","created_by":"Fred [bot]","depends_on_id":"poly-x3pg","issue_id":"poly-x3pg.9","metadata":"{}","type":"parent-child"}],"dependency_count":0,"dependent_count":0,"description":"Reported from a live instance 2026-09-13: clicking a queue row still throws you back to the top.\n\n.repairs-split sets 'min-height: 100%'. min-height does not bound anything -- the split grows to its content, both columns grow with it, and '.repair-rail { overflow-y: auto }' never engages because the rail has no height to overflow. The element that actually scrolls is '.app > main' (min-height: 0; overflow: auto), and a full navigation resets it to 0.\n\nSo the split has only ever had ONE scroller, the page, since M4 shipped it. The detail pane does not scroll independently either; the two panes scroll together as one tall document.\n\nThis is why poly-x3pg.6's fix is inert. repairs-rail.js saves and restores .repair-rail's scrollTop faithfully, and that value is always 0.\n\nWHY THE TEST DID NOT CATCH IT\n\ntest_each_half_scrolls_on_its_own asserts the split's CSS block contains 'overflow-y: auto' at least twice. It does. overflow-y with no height constraint is a no-op, and the test never looks for the constraint that gives it meaning.\n\nFIX\n\n.repairs-split needs 'height: 100%' and 'min-height: 0', and each column needs 'min-height: 0', so the grid is exactly as tall as the already-bounded main and each column can overflow inside it. The narrow breakpoint below 1100px already works, because there the rail gets an explicit 'max-height: 60vh' -- that path bounded it and the desktop path never did.","id":"poly-x3pg.9","issue_type":"bug","labels":["tracker-ui"],"notes":"Fixed 2026-09-13, and MEASURED rather than reasoned about this time.\n\n.repairs-split: min-height:100% -> height:100% + min-height:0, and min-height:0 on both columns (a grid item's default min-height is auto, which floors it at its content and re-breaks it one level down). The narrow breakpoint gets height:auto explicitly, because there the page should be the scroller and the rail is capped at 60vh instead.\n\nMeasured in a real browser at 1500x1000, through a probe served from /static so it was same-origin with /agentic and could read the iframe:\n\n  BEFORE (implied by the CSS): rail never overflowed, main was the scroller.\n  AFTER:  rail clientH=659 scrollH=4875 scrollable=true\n          detail scrollable=false (its content was short)\n          main   scrollable=false   <- the page no longer scrolls\n          rail.scrollTop 0->600 landed at 600\n\nThen the round trip poly-x3pg.6 was actually about: scrolled the rail to 900, clicked the 7th of 16 Ready-to-accept rows, and after the navigation rail.scrollTop=570 with the selected row present and fully visible. 570 rather than 900 because the selected row's group opens server-side, which shifts the rows above it, so ensureVisible re-centred it -- which is the designed behaviour.\n\ntest_each_half_scrolls_on_its_own now asserts the height constraint instead of counting 'overflow-y: auto'. The old assertion passed against a layout where both overflow rules were no-ops, which is how this survived M4 and a whole day of work on top of it.","owner":"github@dragonflybsd.org","priority":1,"status":"closed","title":"The Repairs split has never had two scrollers: .repairs-split is min-height, so nothing bounds the rail","updated_at":"2026-09-13T18:13:47Z"}
{"_type":"issue","acceptance_criteria":"Selecting an occurrence leaves the queue where it was. The group holding the selection renders open with no JS. The selected child row is marked. A deep link with no stored position scrolls the selection into view.","close_reason":"Closed","closed_at":"2026-09-13T13:10:40Z","comment_count":0,"created_at":"2026-09-13T11:10:54Z","created_by":"Fred [bot]","dependencies":[{"created_at":"2026-09-13T13:10:54Z","created_by":"Fred [bot]","depends_on_id":"poly-x3pg","issue_id":"poly-x3pg.6","metadata":"{}","type":"parent-child"}],"dependency_count":0,"dependent_count":0,"description":"The rail is its own scroll container (.repair-rail, overflow-y:auto) and every queue row is a plain link to ?occ=<bundle>. Selecting an occurrence is a full navigation, so the rail starts at scrollTop 0 again. At the 500-issue cap that is the whole queue of scrolling back, once per item -- which is exactly the case the split was built for.\n\nThree things compound it:\n\n- A multi-occurrence group's expansion lives only in the DOM (.wl-group.open, toggled by wlToggle). Selecting a child occurrence reloads, the group renders closed, and the row just chosen is display:none.\n- .wl-child.current is emitted by the template and defined nowhere in progress.css, so a selected occurrence inside a group is unmarked even once the group is open.\n- Every operator action in the right pane ends in location.reload(), so accepting a fix also throws the queue back to the top.","id":"poly-x3pg.6","issue_type":"bug","labels":["tracker-ui"],"notes":"Implemented 2026-09-13. Three parts, because the scroll position alone would have restored you to a view of a collapsed group.\n\n- dportsv3/tracker/static/repairs-rail.js (new). Restores .repair-rail's scrollTop from sessionStorage, but only when the page carries a selection -- arriving at /agentic from the nav is 'show me the queue' and starts at the top. Then pulls the .current row into view when the stored position does not already show it, which is also what makes a deep link land on its selection. Writes on pagehide rather than unload so it survives the back/forward cache and the location.reload() every operator action ends in.\n- agentic_index.html. The group holding the selection renders 'open' from the server, so it is true with no JS. Groups opened by hand are keyed by data-issue and carried across the navigation by the rail script; the element id is positional (grp-<band>-<n>) and re-sorts as bands change, so it could not be that key.\n- progress.css. .repair-rail .wl-child.current, which the template had been emitting all along with no rule behind it.\n\nBEYOND THE BEAD: the chevron never carried aria-expanded. That was survivable while the group only opened on a click; now that it can arrive open it is the row contradicting itself, so the button gets aria-expanded server-side, wlToggle updates it, and the rail script sets it on the groups it restores.\n\nTests: five new in tests/test_repairs_split.py under 'the queue keeps your place', plus test_the_chevron_says_whether_its_group_is_open. _seed grew an 'i-twice' issue with two occurrences, because the sample had no multi-occurrence issue and therefore no group at all.","owner":"github@dragonflybsd.org","priority":1,"status":"closed","title":"The Repairs queue loses your place on every selection","updated_at":"2026-09-13T13:10:40Z"}
{"_type":"issue","comment_count":0,"created_at":"2026-09-12T20:03:59Z","created_by":"Fred [bot]","dependency_count":0,"dependent_count":0,"description":"Abandoning a job marks its row dead but leaves the .job file in\npending/, so the filesystem claim path picks it up again and the runner\nworks a job the DB says is retired. The in-process loop has no\ncancellation check either, so once claimed it runs to its harness\nbudget regardless of state.\n\n    POST /api/jobs/<id>/abandon  ->  state=dead, retire_reason=abandoned\n    queue/pending/<id>.job       ->  still there, still claimable\n\nMEASURED, run 293 (@2026Q3):\n\n  16:16  operator abandons 3 queued java/openjdk25 patch jobs\n  16:43  runner claims 20260912-155114Z-...-java_openjdk25-...-patch.job\n         (\"processing job\"), whose row reads dead/abandoned\n  20:02  still running: A1.T181, 1,460,191 billable tokens, in a\n         4-flavor dsynth verification\n\n3h18m of runner time on a retired job, and 57 other jobs sat queued\nbehind it the whole time. Only a runner restart stopped it.\n\nTwo defects, one visible symptom:\n\n1. abandon does not remove the pending file, so the DB state and the\n   filesystem claim token disagree. Whichever path claims first wins,\n   and the file path does not consult the row.\n2. the agent loop never re-checks job state, so a job retired while\n   running keeps going to max_tool_turns / the harness attempt budget\n   (observed: \"attempt 2/8 ... 3265769/30000000\"). Note this is the\n   harness's own iterations/budget, not runner.max_patch_attempts.\n\nEither one alone is survivable. Together, abandon is not a control:\nit changes what the UI reports without changing what runs.\n\nNOTE ON SCOPE\n\nThis surfaced because the pending files had been bridged from the\ndev-env queue into the host queue to work around the chroot path\nmismatch (the poly-b2r condition). Bridging is what re-armed the file\npath. But the disagreement is latent in any deployment where the file\nexists: abandon simply does not clean up after itself.\n\nSHAPE OF A FIX\n\nRemove (or move to failed/) the pending file inside the abandon\nhandler, in the same transaction that writes the row. Then give the\nagent loop a cheap state re-check between turns - at minimum before\nstarting a dsynth_build, which is where the hours go - so a job\nretired mid-flight stops at the next turn boundary instead of running\nto budget.\n","id":"poly-q07p","issue_type":"bug","owner":"github@dragonflybsd.org","priority":1,"status":"open","title":"Abandoning a job leaves its .job file claimable, so the runner re-claims and runs it to budget","updated_at":"2026-09-12T20:03:59Z"}
{"_type":"issue","comment_count":0,"created_at":"2026-09-12T10:24:41Z","created_by":"Fred [bot]","dependency_count":0,"dependent_count":0,"description":"build_results is keyed by origin with no flavor column, so every flavor of\na port shares one row and the last write wins. Flavor results overwrite\neach other: failures vanish and a still-building flavor reads as finished.\n\n    dportsv3/db/schema.py, build_results:\n        PRIMARY KEY (build_run_id, origin)\n\n    dportsv3/tracker/db.py, record_result upsert:\n        ON CONFLICT(build_run_id, origin) DO UPDATE SET\n            version = excluded.version,\n            result  = excluded.result,\n            status  = excluded.status\n\nport_status carries the same shape: PRIMARY KEY (target, origin).\n\nMEASURED, build run 293 (@2026Q3, 100 operator-supplied origins):\n\nUNDER-REPORTED FAILURES. dsynth logged 78 failures, the tracker 72.\n\n    java/openjdk25        @jdk @jre @headless @jre_headless  4 failed -> 1 row\n    x11-servers/xorg-server  @xorg @xvfb @xephyr @xnest      4 failed -> 1 row\n\n6 results lost, and 78 - 6 = 72 exactly. The API agrees:\n\"failure_count\":72 on /api/builds/293.\n\nA PORT READS AS DONE WHILE IT IS STILL COMPILING. devel/llvm21 has two\nflavors. @lite finished, @default is 2.5h into a compile:\n\n    tracker:  - devel/llvm21 21.1.8_3 success\n    reality:  325938 dsynth: [02] WORKER build devel/llvm21@default\n\n@lite's success overwrote the 'building' status mark_port_building() had\nset for @default. Same for llvm19 and llvm22.\n\n\"building_count\":1 while dsynth had two workers busy, so the UI shows one\nport building and the other is invisible. This is not a stale UI - the row\nis wrong in the DB and refreshing cannot fix it.\n\nWHY IT MATTERS\n\nFailure counts drive what gets worked on. A port whose flavors disagree is\nreported by whichever flavor finished last, so a green flavor can mask a\nred sibling and the port is never queued for a fix. The inverse also\nhappens: a red flavor hides a green one and work is redone.\n\nDistinct from poly-8dt, which is about dsynth recompiling flavors that were\nalready green. This one is about the tracker being unable to represent them\nseparately at all.\n\nSHAPE OF A FIX\n\nAdd flavor to the key (build_run_id, origin, flavor) with '' for unflavored\nports, and to port_status (target, origin, flavor). Needs a migration plus a\npass over the readers that aggregate these tables - the *_count rollups in\ntracker/db.py and tracker/progress_adapter.py - to decide whether a\nflavored port rolls up as worst-of or is listed per flavor.\n","id":"poly-gzuv","issue_type":"bug","owner":"github@dragonflybsd.org","priority":1,"status":"open","title":"Tracker build_results is keyed by origin, so flavors overwrite each other and failures are lost","updated_at":"2026-09-12T10:24:41Z"}
{"_type":"issue","assignee":"Fred [bot]","comment_count":0,"created_at":"2026-09-10T23:34:48Z","created_by":"Fred [bot]","dependency_count":0,"dependent_count":0,"description":"The agent refuses every slave port outright, when the ports framework already\ntells it exactly where the fix belongs.\n\n`decision.py:294` short-circuits on `is_slave` to `escalate_manual` with reason\n`slave_port_unsupported`, before the classification table. That rule is why\n`security/krb5` and `databases/mysql84-client` were fixed by hand this cycle.\nIts stated justification -- \"inherits from master; no master-aware dops\nsupport\" -- overstates the gap. No new mechanism is needed. A master and its\nslaves compile the same source tree, so a source patch belongs on the master,\nand `DFLY_PATCHDIR` already points there.\n\n## What the framework already provides\n\nMk/bsd.port.mk:1215 sets `SLAVE_PORT` and `MASTER_PORT` automatically from\n`MASTERDIR`, and DeltaPorts' `DFLY_PATCHDIR?= ${PATCHDIR:H}/dragonfly`\n(special/Mk/diffs/bsd.port.mk.diff:130) follows `PATCHDIR`, which defaults to\n`${MASTERDIR}/files` (bsd.port.mk:1378).\n\nVerified against the live @2026Q3 compose root for databases/mysql84-client:\n\n    SLAVE_PORT     yes\n    MASTER_PORT    databases/mysql84-server\n    PATCHDIR       .../mysql84-client/../mysql84-server/files\n    DFLY_PATCHDIR  .../mysql84-client/../mysql84-server/dragonfly\n\nOne `make -V` answers the whole question. security/krb5-122 is the worked\nexample: one overlay on the master, three ports green.\n\nSome masters opt every slave into its own patch dir with the `${.CURDIR}`\nidiom (e.g. lang/php82/Makefile:184 `PATCHDIR= ${.CURDIR}/files`), because\n`.CURDIR` is evaluated wherever make runs. A static scan of freebsd-ports\nmain put roughly 29% of 1131 slaves in that group and the rest on the\nmaster's dir -- but treat those numbers as indicative only, see below.\n\nBoth regimes work today. The agent only needs to know which one it is in --\nwhich is the same `make -V`. Nothing in Mk or in the dops language needs to\nchange.\n\n## Why the resolution has to be a probe, not a scan\n\nThe first pass at this resolved PATCHDIR by reading Makefiles statically and\npredicted four broken overlays. Probing the live @2026Q3 compose root\ncontradicted two of them:\n\n    devel/smake                       -> devel/schilybase/dragonfly   (dead)\n    editors/pico-alpine               -> ./dragonfly                  (own)\n    emulators/mupen64plus-video-z64   -> its own dragonfly            (own)\n    multimedia/gstreamer1-plugins-bad -> its own dragonfly            (own)\n\nTwo reasons the scan was wrong, both structural:\n\n  - it read freebsd-ports *main*, while the pipeline composes a quarterly\n    branch. Ports differ between them -- multimedia/gstreamer1-plugins sets\n    PATCHDIR one way on main and another on @2026Q3.\n  - the composed tree is freebsd-ports *plus* DeltaPorts overlays, so no\n    reading of either input alone predicts it.\n\n`${PATCHDIR:H}` also degenerates in ways a lexical reader gets wrong:\neditors/pico-alpine sets `PATCHDIR=` empty on purpose, so DFLY_PATCHDIR comes\nback as the relative `./dragonfly`, which do-patch resolves against make's cwd\n-- the port's own directory.\n\nThe conclusion is the design: ask the framework, against the composed tree,\nper port. Only devel/smake is confirmed to ship a payload the build cannot\nread.\n\n## What is actually missing\n\n1. The agent is never allowed to try (decision.py:294).\n\n2. Blast radius is not covered on verify. A master overlay changes every\n   slave, but worker.py:3270 `dsynth_build(env, origin)` is single-origin, and\n   `dsynth test <master>` does not build the slaves -- separate origins,\n   separate packages. So `rebuild_ok` on a master today means \"the master\n   builds\", not \"the fix did not break the siblings\".\n\n3. One existing overlay is authored on the slave when the patch can only be\n   read from the master, so its payload never reaches the build:\n   devel/smake's `dragonfly/patch-lib_getexecpath.c`, whose DFLY_PATCHDIR\n   resolves to devel/schilybase/dragonfly. An overlay in the wrong place, not\n   a mechanism defect.\n\n   Moving it is a master change and therefore reaches every schily slave, so\n   it wants the sibling build in section 4 rather than a blind move.\n\n## Second hazard, unrelated to patches\n\nengine/executors/mk_ops.py:35 `_mk_var_set_insert_line` inserts a *new* `mk\nset` before the first Target or Include node; mk_ops.py:42\n`_insert_before_last_include_line` (used by `mk eval`) inserts before the last\none. For a slave the trailing include is `.include \"${MASTERDIR}/Makefile\"`\n(confirmed: databases/mysql84-client, www/yarn-node20; devel/smake uses\nMakefile.master), so either way the op lands *before* the master is read. A\n`mk set X` on a slave therefore survives only if the master uses `X?=` or\nnever sets X; a plain `X=` in the master silently clobbers it, and `mk add`\n(`+=`) is worse. This applies only to variables the slave does not already\ndeclare -- makefile_rewrite.py:44 `set_var` replaces an existing assignment in\nplace, and that case is safe.\n","design":"Works with the tree as it stands. No change to special/Mk/diffs/, no new dops\noperation, no EXTRA_PATCH_TREE.\n\n## 1. Resolve the relation instead of detecting slave-ness\n\nworker.py:2138 `is_slave_port` greps `^MASTERDIR=` and answers a boolean. The\nuseful answer is the whole relation. Replace with a probe returning\n`(slave_port, master_port, patchdir, dfly_patchdir)` from one\n\n    cd \"$DPORTS_COMPOSE_ROOT/<origin>\"; make PORTSDIR=\"$DPORTS_COMPOSE_ROOT\"\n      WRKDIRPREFIX=/work/obj PACKAGE_BUILDING=yes BATCH=yes\n      -V SLAVE_PORT -V MASTER_PORT -V PATCHDIR -V DFLY_PATCHDIR\n\ncached per origin, with the existing grep as the fail-open fallback.\nworker.py:2686 `_extracted_wrksrc` already establishes this exact incantation,\nso it is a small function on an existing pattern. Verified working against the\nlive @2026Q3 compose root.\n\nDerived fact the rest of this depends on: `patch_origin` -- the origin whose\ndragonfly/ the build will actually read. The port itself when DFLY_PATCHDIR\nresolves inside it, otherwise MASTER_PORT.\n\n## 2. Widen the per-origin invariants to an origin set\n\nPrecondition for everything after it. Three invariants are scoped to\n`ports/<origin>` and go quiet -- not red -- when work lands outside it:\n\n  - worker.py:1973 `assert_port_clean` -- `rel = f\"ports/{origin}\"`. A\n    master-origin change is invisible, so preflight reports clean while the\n    master carries uncommitted work.\n  - worker.py:1427 `_port_subtree_hash` -- the \"substrate changed since the\n    last successful materialize_dports\" detector is per-origin, so a master\n    edit does not invalidate the slave's baseline and dsynth_build can run\n    against a stale composed tree.\n  - worker.py:2551 `commit_port_changes` -- same `rel`; the triage-bootstrap\n    to patch handoff commit misses a master-origin overlay and leaves it\n    untracked into the patch job.\n\nThese are blind spots rather than gates, which is why they come first: a job\nthat writes at patch_origin would otherwise pass preflight and report green\nwith the change uncommitted or the compose stale, and nothing surfaces.\n\nNote they are already reachable without this work -- the pipeline expects\nfixes outside the bundle origin elsewhere (see \"already absorbed\" below), so\ntoday the exposure is latent. This bead makes it load-bearing.\n\nTake an origin set: the port, its patch_origin, and for verify the sibling\nset. `reset_port` / `reapply <origin>` need the same widening so the master is\nre-materialized.\n\n## 3. Let the agent run, and point it at patch_origin\n\nRemove the short-circuit at decision.py:294. A slave then classifies like any\nother port. The constraint is not \"which ports may be patched\" but \"where the\npatch is written\":\n\n  - source patches (`file materialize dragonfly/...`, `patch apply`) go to\n    `ports/<patch_origin>/overlay.dops`\n  - `mk` ops stay on the failing origin's own Makefile, assertion-checked (5)\n\nCoupling found on re-read: steps.py:440 gates `ensure_overlay_or_abort` on\n`not is_slave` too, so no slave has ever had a header overlay bootstrapped.\nLifting the gate makes runner.py:3616 `_ensure_overlay_or_abort` ->\n`probe_overlay_facts` -> `bootstrap_decision` run on slaves for the first\ntime, and it must bootstrap at `patch_origin`, not at the failing origin.\nCover that path before the gate comes off.\n\n## 4. Verify the sibling set, not just the origin\n\nA master overlay changes every slave. verify_fix.py:245 already replays the\nwhole changes.diff, so the master's change reaches the replay env -- but\nverify_fix.py:191 runs `apply-and-build <env> <origin>` and worker.py:3270\n`dsynth_build(env, origin)` builds one origin, and `dsynth test <master>` does\nnot reach the slaves (separate origins, separate packages).\n\n  - compute the sibling set: every port whose MASTERDIR resolves to\n    `patch_origin` (tree scan, cached per target -- 1131 slaves total, so the\n    index is small)\n  - build `patch_origin` plus its siblings before `rebuild_ok`\n  - a sibling that was green before and fails after is a rejection of the fix,\n    not a new failure to triage\n\nThe master/slave analogue of the flavour rebuild `_dsynth_run` already does\nfor meta-nodes.\n\n## 5. Post-compose assertion\n\nAfter applying an overlay, `make -V` the variables it set and confirm they\nsurvived. Catches the mk-clobber hazard, and catches a patch written where the\nbuild will not read it. engine/oracle.py already runs `bmake -V` per composed\nport root with `_CI_PROBE_VARIABLES = (\"PORTNAME\",\"CATEGORIES\",\"MAINTAINER\")`;\nextend that. Caveat: the oracle is best-effort and local -- it skips when\nbmake is absent, so it cannot be the only gate.\n\n## 6. Fix devel/smake\n\nProbing @2026Q3 reduced the suspected four to one: devel/smake's\n`dragonfly/patch-lib_getexecpath.c` needs to move to\n`ports/devel/schilybase/dragonfly/`. The other three resolve to their own\ndirectory and are fine as they stand.\n\nNOT done as part of the implementation. schilybase has a dozen-odd slaves\n(cdrtools, star, smake, sunpromake, schilyutils, tartest, ...), and moving a\npatch into the master means it is applied to every one of them. That is\nprecisely the change section 4 exists to gate, so it should go through the\nsibling build rather than be moved by hand on the strength of a probe.\n\n## Blast radius: what the per-origin pipeline already absorbs\n\nAudited. No gate blocks a cross-origin write today, and the two\narchitecturally hard pieces are done:\n\n  - runner.py:4193 captures the diff whole-tree (`rel = \".\"`), and the comment\n    names this exact case: \"fixes that correctly land outside the bundle\n    origin -- e.g. a slave port whose patch lives in the master's PATCHDIR --\n    are captured instead of vanishing\".\n  - worker.py:2101: the whole-tree substrate reset was removed because each\n    job's tree is a worktree destroyed with the job, so an escaped write\n    cannot poison the next job. Its docstring names the slave/master case too.\n  - worker.py:277 `_reject_orphan_dops_write` requires only the\n    /work/DeltaPorts/ports/ prefix, not the job origin, so writing the\n    master's overlay is already permitted.\n  - verify_fix.py:245 replays the whole changes.diff, so master-origin changes\n    reach the replay env.\n\nEverything that does not absorb it is in sections 2 and 4.\n\n## Acceptance\n\n- databases/mysql84-client and security/krb5 reach auto_patch instead of\n  escalate_manual, and their patches are authored at MASTER_PORT.\n- `assert_port_clean` and `_port_subtree_hash` report a change made at\n  patch_origin. Regression test: dirty the master, confirm the slave's\n  preflight goes red and its materialize baseline invalidates.\n- A fix on a master is rejected if it breaks a previously-green sibling.\n- An `mk set` whose value the master clobbers is reported at compose time, not\n  as a build failure.\n- devel/smake's patch reaches the build, and the move is proved by a\n  schilybase sibling build rather than asserted.\n- `_ensure_overlay_or_abort` has slave coverage before the decision.py gate is\n  removed.\n\n## Explicitly out of scope\n\nChanging DFLY_PATCHDIR, adding DFLY_OWN_PATCHDIR, or setting EXTRA_PATCH_TREE.\nThe 326 slaves whose master uses the ${.CURDIR} idiom already get a per-slave\npatch dir; the rest are served correctly by the master. A per-slave override\n(`mk set DFLY_PATCHDIR \"${.CURDIR}/dragonfly\"`) does work today -- verified on\nmysql84-client, the `?=` default yields to it -- but it shadows the master's\npatches wholesale, so it is a last resort for a port that genuinely needs to\ndiverge, not the design.\n","id":"poly-lt5q","issue_type":"feature","labels":["agent","deltaports","slave-ports"],"notes":"Implemented (polytropos + dev-env). 3918 tests pass.\n\nworker.py\n  probe_port_relation / _relation_from_vars / patch_origin_for /\n  forget_port_relation / invariant_origins / origin_set / slave_siblings.\n  is_slave_port kept as the fail-open fallback.\n  Widened to an origin set: assert_port_clean, _port_subtree_hash,\n  commit_port_changes, reset_port, materialize_dports, _dsynth_run,\n  dsynth_build.\n  install_patches now redirects to the resolved patch origin and says so.\ndecision.py\n  slave short-circuit removed; is_slave recorded on every decision instead.\nsteps.py\n  triage resolves the relation and bootstraps at patch_origin; patch\n  preflight checks / absorbs / resets across the origin set.\noracle.py + apply.py\n  run_bmake_oracle gained `expect`; apply derives it from the plan's\n  mk.var.set ops via literal_expectations (values without `$` only).\nverify_fix.py\n  sibling_origins_for + `--also` passthrough.\ndev-env/cli.py\n  apply-and-build gained repeatable --also; dirty check, reapply, dtest\n  and post-build cleanup all take the set. dtest already accepted\n  ORIGIN..., so it is one dsynth run.\n\nVerified live against @2026Q3:\n  databases/mysql84-client -> databases/mysql84-server\n  security/krb5            -> security/krb5-122\n  devel/gperf (non-slave)  -> itself\n  devel/smake              -> devel/schilybase\n  grep --include works on DragonFly grep, 2.6s over the full tree.\n\nCorrections found while implementing, both now in the bead:\n  - the static Makefile scan was wrong on 2 of 4 predicted-dead overlays.\n    It read freebsd-ports main while the pipeline composes a quarterly,\n    and the composed tree is freebsd-ports + DeltaPorts overlays, so\n    neither input predicts it. Only devel/smake is confirmed dead.\n  - DFLY_PATCHDIR can be relative (`./dragonfly`, editors/pico-alpine,\n    whose PATCHDIR is deliberately empty). do-patch resolves that\n    against the port's own dir, so it counts as own.\n\nNot done, deliberately: section 6's devel/smake move. schilybase has a\ndozen-odd slaves and moving the patch into the master applies it to all\nof them -- that is what section 4's sibling build exists to gate, so it\nshould go through that rather than a hand move. Left open in the bead.\nREVIEWED 2026-09-22. Sections 1,2,3,5 are done; section 4 is plumbed but\nnon-functional, so the third acceptance criterion is unreachable. NOT closed.\n\nVerified present:\n  S1 worker.py probe_port_relation:2371 _relation_from_vars:2284\n     patch_origin_for:2431 slave_siblings:2443 invariant_origins:2503\n     origin_set:2044; is_slave_port:2247 kept as fallback.\n  S2 assert_port_clean:2062 reset_port:2130 commit_port_changes:2902\n     _port_subtree_hash:1458 _dsynth_run:3569 dsynth_build:3667 all take a set.\n  S3 decision.py:299-303 records is_slave instead of short-circuiting;\n     steps.py:439-464 resolves patch_origin and emits patch_origin_resolved.\n  S5 oracle.run_bmake_oracle gained expect:72; apply.py:421 derives it via\n     literal_expectations.\n  tests/test_slave_port_patch_origin.py 42/42 pass, including\n  test_a_dirty_master_makes_the_slave_report_dirty:163 and\n  test_the_subtree_hash_changes_when_the_master_changes:180 (criterion 2).\n\nCriterion 3 -- 'a fix on a master is rejected if it breaks a\npreviously-green sibling' -- cannot fire. The sibling build never starts:\n\n  dev-env/cli.py:526  reapply \"$@\" with *build_origins (master + siblings)\n  env /usr/local/bin/reapply   for origin in \"$@\"; do compose --origin ... || exit $?\n  compose_stages.py:524-534    --origin sets selected_origins; any origin not\n                               in discover_overlay_contexts() ->\n                               E_COMPOSE_SELECTED_ORIGIN_NOT_FOUND\n  dev-env/cli.py:532-537       reapply_exit != 0 -> return, before dtest\n\nA slave patched at its master correctly keeps no overlay directory, so it is\nnever discovered, so reapply dies on it and dsynth is never reached. That is\npoly-lt5q.1 exactly; its cli.py:333 reference has drifted to :526 and the\nchaining is the loop inside reapply rather than 'a && b', but the mechanism\nis unchanged.\n\nOnly the post-build cleanup was made resilient (cli.py:456-487, pinned by\ntests/test_dev_env_apply_and_build.py:435\ntest_cleanup_continues_past_a_sibling_that_cannot_compose). The build path\nwas not.\n\nNet effect worth stating: the refusal was removed, so the agent now authors\nat a master, but the gate designed to make that safe does not run. Closing\nthis bead would record master patching as delivered with its blast-radius\ncheck silently absent.","owner":"github@dragonflybsd.org","priority":1,"started_at":"2026-09-10T23:48:33Z","status":"in_progress","title":"the agent refuses every slave port instead of authoring the fix at MASTER_PORT","updated_at":"2026-09-22T19:35:58Z"}
{"_type":"issue","close_reason":"Closed","closed_at":"2026-09-13T13:10:41Z","comment_count":0,"created_at":"2026-09-10T18:59:02Z","created_by":"Fred [bot]","dependency_count":0,"dependent_count":0,"description":"The Repairs worklist (/agentic) renders _cockpit.html in its right pane, but\nagentic_index.html overrides {% block scripts %} with only the inline wlToggle +\nenv-set handler and defines no head_extra. agentic_bundle.html -- the standalone\nhost of the same partial -- is where live.js, agentic-bundle.js and the cockpit's\n<style> block actually live.\n\nResult on /agentic?occ=<id>: tabs do not switch panels, operator action buttons\n(verify/accept/reject/retry/takeover/discard) post nothing, the delivery\nmark-merged/closed buttons are dead, chat does not send, and the artifact reader\ndoes not select. All the markup is there; nothing is wired.\n\nConfirmed by diffing the two renders of the same partial:\n  /agentic?occ=b-1        agentic-bundle.js False  live.js False  .chat-log css False\n  /agentic/bundles/b-1    agentic-bundle.js True   live.js True   .chat-log css True\n\n_cockpit.html's header comment already states the invariant this violates: 'One\ncopy, so they cannot drift.' The markup is one copy; its assets are not.\n\nFix: extract the cockpit's head styles and its two script tags into partials\n(_cockpit_head.html / _cockpit_scripts.html) included by both hosts, and pin the\nasset parity in tests/test_repairs_split.py alongside the existing\ntest_the_cockpit_is_one_template.","id":"poly-icif","issue_type":"bug","owner":"github@dragonflybsd.org","priority":1,"status":"closed","title":"Repairs split: cockpit JS/CSS never loaded, so the right pane is inert","updated_at":"2026-09-13T13:10:41Z"}
{"_type":"issue","comment_count":0,"created_at":"2026-09-10T15:49:37Z","created_by":"Fred [bot]","dependencies":[{"created_at":"2026-09-10T17:49:44Z","created_by":"Fred [bot]","depends_on_id":"poly-7eh","issue_id":"poly-cg6u.2","metadata":"{}","type":"related"},{"created_at":"2026-09-10T17:49:37Z","created_by":"Fred [bot]","depends_on_id":"poly-cg6u","issue_id":"poly-cg6u.2","metadata":"{}","type":"parent-child"}],"dependency_count":0,"dependent_count":0,"description":"A full context ends the JOB. It should end the ATTEMPT, and the loop\nshould carry on to the iterations the tier already granted.\n\nThe remedy is already in the harness and the failure never reaches it.\n\nWHAT HAPPENS NOW\n\ntool_loop puts the request on the wire, the endpoint answers 400\n\"exceeds the available context size\", and the exception climbs out of\nattempt_loop into steps.py. llm.is_transient sees an unrecognized 4xx --\n\n    if 400 <= status < 500: return False\n\n-- so it is terminal, _err retires the job to failed/, and\n_drop_bundle_branch_for_job throws the worktree away. ASSIST grants\nmax_iterations=8 and runner.max_tool_turns=400. One attempt is spent.\nSeven are discarded without being tried.\n\nMEASURED 2026-09-10, graphics/libavif, both on a local endpoint:\n\n  qwen/qwen3.6-27b   turn 6: request 31,908 vs context 31,744 -> job dead\n  openai/gpt-oss-20b turn 1: request 12,792 vs context  8,192 -> job dead\n\nTHE FIX IS ALREADY WRITTEN, JUST NOT REACHED\n\nattempt_loop.py:417 -- every attempt after the first starts from a fresh\nmessage list:\n\n    if attempt_idx == 1:\n        messages = list(base_messages)\n    else:\n        messages = list(base_messages) + [_failure_context_message(...)]\n\nThat is a hard context reset carrying a digest forward: the harness's own\ncompaction, at attempt granularity. An attempt that dies of a full context\nis precisely the case it cures, and it is the one case that never gets\nthere, because the exception leaves the loop instead of ending the attempt.\n\nFIX DIRECTION\n\nRecognize context exhaustion where the request is made and end the ATTEMPT,\nthe way tool_loop.EnvironmentBlocked ends one today -- but continue the for\nloop instead of returning. The next attempt rebuilds from base_messages and\nhas room again.\n\nDetection: the message text is not standardized across providers, but the\nshape is (\"context\", \"exceed\", \"too many tokens\", \"maximum context\"), and\nLM Studio also returns a typed field, exceed_context_size_error with\nn_prompt_tokens / n_ctx. Match on substring, and prefer the typed field\nwhen present.\n\nTHE GUARD THAT MATTERS\n\nIf the FIRST turn of an attempt blows the window before any tool call, the\nbase payload itself does not fit, and every later attempt will fail\nidentically. That is genuinely terminal and must be reported as its own\nthing -- \"the payload does not fit this endpoint's context (N vs M)\" --\nrather than burning eight identical attempts and reporting a generic\ngive-up. gpt-oss above is exactly this case; qwen is not.\n\nRELATION\n\npoly-7eh reduces how fast the transcript grows. This bounds what happens\nwhen it runs out anyway. They are independent: with 7eh fixed a full\ncontext is rarer, and with this fixed it costs one attempt instead of the\nwhole job. Both matter more on a small window, which is the case that makes\na local endpoint -- free inference -- usable at all.\n\nAlso note free_tier=true sets the token budget to 0/unbounded, so on a local\nendpoint max_iterations and max_tool_turns are the ONLY bounds left. Losing\nseven eighths of the iterations to a recoverable condition is most of the\nbudget.","id":"poly-cg6u.2","issue_type":"bug","owner":"github@dragonflybsd.org","priority":1,"status":"open","title":"a full context ends the job, not the attempt, so 7 of 8 granted iterations are discarded","updated_at":"2026-09-10T15:49:37Z"}
{"_type":"issue","comment_count":0,"created_at":"2026-09-10T15:09:01Z","created_by":"Fred [bot]","dependencies":[{"created_at":"2026-09-10T17:09:01Z","created_by":"Fred [bot]","depends_on_id":"poly-so8j","issue_id":"poly-so8j.1","metadata":"{}","type":"parent-child"}],"dependency_count":0,"dependent_count":0,"description":"_bare_model strips a leading \"openai/\" because \"openai\" is in\n_ROUTING_PREFIXES. But \"openai/\" is a real NAMESPACE on third-party hosts,\nnot a routing prefix: LM Studio, Ollama and OpenRouter all serve ids like\n\"openai/gpt-oss-20b\". The stripped id names a different thing.\n\nMEASURED 2026-09-10, LM Studio on the LAN, graphics/libavif:\n\n    _bare_model('openai/gpt-oss-20b', 'openai') -> 'gpt-oss-20b'\n\nThe endpoint had ONE instance loaded, \"openai/gpt-oss-20b\", at a 131,072\ncontext. The harness put \"gpt-oss-20b\" on the wire. LM Studio does not\nserve that id, so it JIT-loaded a SECOND instance under the bare name with\nits default 8,192 context, and every request landed there:\n\n    400 exceed_context_size_error: request (12,792 tokens) exceeds the\n        available context size (8192), n_ctx: 8192\n\nSide by side, same endpoint, same minute:\n  curl  \"model\":\"openai/gpt-oss-20b\"  12,074 prompt tokens -> 200 OK\n  agent  model=gpt-oss-20b (stripped) 12,792 prompt tokens -> 400\n\nUnloading the stray instance by hand does not help: the next request\nrecreates it. Two jobs died this way and the cause was invisible from the\nconfig, which correctly reads model = \"openai/gpt-oss-20b\".\n\nWHY THIS IS WORSE THAN THE CASE THE CODE ALREADY GUARDS\n\n_NAMESPACED_MODEL_IDS exists for exactly this, and says so:\n\n    \"meta\" is here for the opposite reason to \"nvidia\": ... \"meta/\" IS a\n    real namespace on somebody else's host (NIM serves \"meta/llama-...\"),\n    and letting it become a routing prefix would strip half of those ids\n    and 404 them.\n\n\"openai/\" is now that case. It does not 404 -- it silently resolves to a\ndifferent model instance with different settings. A 404 is a bad name; this\nis a wrong answer from a name that looks right.\n\nNO CONFIG CAN AVOID IT\n\n    return tail if head in _ROUTING_PREFIXES or head == provider else model\n\n_ROUTING_PREFIXES is tested FIRST, so \"openai/...\" is stripped whatever\nllm.<role>.provider says, including unset -- _provider_of infers \"openai\"\nfrom the slash anyway. There is no setting that expresses \"send the id\nverbatim\".\n\nFIX DIRECTION\n\nThe signal that distinguishes the two cases is already present: an explicit\napi_base. It means the operator has named the endpoint, so the model id\nbelongs to that endpoint's namespace and must not be rewritten. Stripping is\nonly ever right when we resolved the base ourselves from the provider name\n(_PROVIDER_API_BASE).\n\n  - Preferred: do not strip when api_base is set. _bare_model would need\n    the base passed in, or the decision moves to the call site that already\n    has both.\n  - Narrower: add \"openai\" to _NAMESPACED_MODEL_IDS. Fixes this case,\n    leaves \"qwen/\", \"zai-org/\", \"mistralai/\" on other hosts working by\n    accident rather than by rule -- those already survive only because they\n    are not in _ROUTING_PREFIXES.\n\nEither way _use_openai_sdk must keep routing these to the SDK: the api_base\n+ provider(\"openai\") path is what makes a local endpoint work at all, and\nthat part is correct today.\n\nTEST: assert the wire id for \"openai/gpt-oss-20b\" with an api_base set is\nunchanged. Cheap, and it pins the distinction the docstring already draws.","id":"poly-so8j.1","issue_type":"bug","owner":"github@dragonflybsd.org","priority":1,"status":"open","title":"_bare_model strips a real 'openai/' namespace, so a local endpoint silently serves a different model instance","updated_at":"2026-09-10T15:09:01Z"}
{"_type":"issue","comment_count":0,"created_at":"2026-09-10T11:16:40Z","created_by":"Fred [bot]","dependencies":[{"created_at":"2026-09-10T13:16:58Z","created_by":"Fred [bot]","depends_on_id":"poly-be6o","issue_id":"poly-cg6u.1","metadata":"{}","type":"related"},{"created_at":"2026-09-10T13:16:39Z","created_by":"Fred [bot]","depends_on_id":"poly-cg6u","issue_id":"poly-cg6u.1","metadata":"{}","type":"parent-child"}],"dependency_count":0,"dependent_count":0,"description":"When tool_loop.run raises, the attempt's token spend is discarded. The\nattempt is billed by the provider and recorded by nobody.\n\nattempt_loop.run adds usage to total_usage on the line AFTER the call:\n\n    response, attempt_usage, rebuild_ok_seen = tool_loop.run(...)\n    ...\n    total_usage.add(attempt_usage)\n\nA raise skips it, and the exception propagates past api_call_complete\n(the row that carries _usage_spend), so nothing reaches the audit.\n\nEnvironmentBlocked already solves this — it carries `usage` and the\nhandler does total_usage.add(blocked.usage), with the comment \"Without\nthis the caller loses the attempt's accounting and the audit\nunder-reports\". That is precisely what every other exception does.\n\nOBSERVED\n\nwww/chromium job 20260909-194249Z-2026Q3-www_chromium-309853-patch: an\nattempt ran 13 h 41 m, drove a build to 34,886/54,854 targets, and died\non a 400. Its spend appears in no row, no patch_audit.json, no token\ncard. Three attempts on that port billed 13,139,714 tokens; the number\nhad to be reconstructed by hand from provider-side data.\n\nThis matters most exactly where the numbers are largest: a context-length\n400 is the failure mode of an expensive attempt, so the attempts that\nvanish from the accounting are the ones that cost the most. Any per-port\nlifetime cost is therefore biased low by an unknown amount.\n\nFIX DIRECTION\n\ntool_loop already tracks `total` (it hands it to EnvironmentBlocked).\nMake it reachable when any exception leaves the loop — attach it to the\nraised exception the way EnvironmentBlocked does, or accumulate into a\ncaller-owned object (`messages` is already shared by reference, which is\nwhat makes the poly-be6o session dump work).\n\nThen in attempt_loop's `except Exception` (added by poly-be6o, which\ndumps the session and re-raises): add the usage before re-raising, and\nhave the patch step log the spend on the api_error path.\n\nRELATION\n\npoly-be6o added the hook this needs — the except is already there, it\njust has no usage to record.","id":"poly-cg6u.1","issue_type":"bug","owner":"github@dragonflybsd.org","priority":1,"status":"open","title":"an attempt that raises has its token spend discarded: the most expensive attempts are the ones missing from the accounting","updated_at":"2026-09-10T11:16:40Z"}
{"_type":"issue","comment_count":0,"created_at":"2026-09-10T10:55:21Z","created_by":"Fred [bot]","dependencies":[{"created_at":"2026-09-10T12:55:33Z","created_by":"Fred [bot]","depends_on_id":"poly-3x8o","issue_id":"poly-be6o","metadata":"{}","type":"related"},{"created_at":"2026-09-10T12:55:32Z","created_by":"Fred [bot]","depends_on_id":"poly-88k","issue_id":"poly-be6o","metadata":"{}","type":"parent-child"},{"created_at":"2026-09-10T12:55:33Z","created_by":"Fred [bot]","depends_on_id":"poly-9hjm","issue_id":"poly-be6o","metadata":"{}","type":"related"}],"dependency_count":0,"dependent_count":0,"description":"When harness_patch.run() raises, the patch step returns without capturing\nanything, and the caller drops the bundle branch anyway. Everything the\nagent did is destroyed.\n\nOBSERVED — www/chromium, job 20260909-194249Z-2026Q3-www_chromium-309853-patch\n\n  19:42:57  bundle_branch_checkout  bundle/www_chromium-20260909-193941Z (base=master)\n  20:33-01:09  put_file x4: dragonfly/patch-third__party_perfetto_src_base_periodic__task.cc\n               and dragonfly/patch-net_socket_udp__socket__posix.cc\n  01:09:39  dsynth_build -> ran 8 h, reached 34,886/54,854 targets (63.6%)\n  09:23:10  emit_diff relpath=overlay.dops diff_bytes=6923\n  09:23:39  api_error 400 invalid_request_error\n  09:23:42  moved job to failed/ ; bundle_branch_dropped (patch_failure)\n\n13 h 41 m. Recovered afterwards: nothing. The bundle holds manual_handoff.md,\ntriage.md, triage_result.json and the TRIAGE session only. No changes.diff,\nno patch session, no tool_trace. git reflog in /work/ports-main shows master\npulls only -- the agent never commits, so there was no commit to recover, and\nthe worktree that held the edits was destroyed.\n\nTHE TWO PLACES\n\n1. steps.py:1277 -- the except does four things and returns:\n\n     except Exception as exc:\n         services.activity_log(... \"api_error\" ...)\n         outcome = _err(str(exc), services, job_path, JobEvent.PATCH_GAVE_UP,\n                        transient=llm.is_transient(...))\n         if not outcome.detail.get(\"requeued\"):\n             _try_write_handoff(..., patch_result=None)\n         return outcome\n\n   Everything that persists work is below it, on the success path:\n   write_patch_audit, write_tool_trace, the orphan reconcile, and\n   write_changes_diff at :1349. None of it runs.\n\n2. runner.py:4533 -- the caller drops the branch unconditionally:\n\n     # Step 30 slice 4: patch is terminal for the bundle branch\n     # -- delivery.diff was captured (slice 2) on success, and on\n     # failure the branch's state is moot. Drop either way.\n\n   \"on failure the branch's state is moot\" is the flawed premise. It\n   conflates the agent trying and failing (state genuinely moot) with the\n   harness raising mid-flight (state holds everything the agent did).\n\nA THIRD CAPTURE POINT ALSO SITS DOWNSTREAM\n\nsession_dump is invoked from attempt_loop AFTER tool_loop.run() returns. The\n400 came from inside the tool loop, so no transcript was written either --\nwhich is why only the triage session is in the bundle. dump_attempt writes\nstraight to the artifact store (no local file), so there is no intermediate\ncopy to salvage.\n\nTHE TRANSIENT CASE IS WORSE\n\n_err with transient=True returns status=\"failed\" with requeued=True, so\nsuccess=False, so the branch is dropped just the same -- and the requeued job\nrestarts from master. A provider blip therefore discards hours of work\nsilently, and the operator sees a requeue rather than a loss.\n\nWHY THE FIX IS CHEAP\n\n_drop_bundle_branch_for_job runs at runner.py:4536, AFTER process_patch_job\nreturns. The except at steps.py:1277 is inside that call, so the worktree is\nstill intact at that moment.\n\nAnd _write_changes_diff already does the right thing there: it diffs\nwhole-tree (rel=\".\") against the base via worker._git_diff_against_base ->\n_git_in_ports_tree, which brackets the diff with `git add -N` and a trailing\n`git reset`. Freshly-created, never-committed files surface as additions --\nexactly the shape of the two dragonfly patches that were lost. emit_diff is\nNOT the tool for this: it is a pure read that returns to the model and\npersists nothing, which is why the 6,923 bytes evaporated.\n\nFIX DIRECTION\n\n- Call _write_changes_diff in the except at steps.py:1277, best-effort and\n  wrapped so a capture failure cannot mask the original error. This alone\n  would have saved the whole thing.\n- Move session_dump into a finally in attempt_loop so the transcript\n  survives a raise. Second line of defence: the put_file arguments alone\n  allow reconstructing the patches.\n- Consider writing tool_trace on the error path too; it is cheap and it is\n  the record of what the agent actually did.\n- Revisit \"on failure the branch's state is moot\" at runner.py:4533. At\n  minimum, do not drop when the failure was a harness raise rather than an\n  agent verdict.\n\nRELATION\n\npoly-3x8o and poly-9hjm are why the 400 happened: the payload floor was\n~950k because 480k of never-read patches were inlined, and dsynth_log has no\nbyte cap, so the request carrying a chromium log tail crossed the provider's\nceiling. This bead is about durability and is independent of cause -- with\nthe context fixed, any provider hiccup at hour thirteen still throws the work\naway today.\n","id":"poly-be6o","issue_type":"bug","notes":"IMPLEMENTED (not yet committed) — all four fix directions, plus one hole\nfound while re-reviewing.\n\n1. steps._rescue_work_on_raise, called from the api_error except BEFORE\n   _err (which may requeue). Files three artifacts off one capture:\n   analysis/tool_trace.jsonl (already in memory), analysis/changes.diff,\n   and analysis/rescued/<job_id>.diff. Every step swallowed — it runs\n   inside an except and must not replace the error the operator needs.\n\n2. attempt_loop: an `except Exception` after the EnvironmentBlocked\n   handler dumps the session and re-raises untouched, so a raise from\n   inside tool_loop no longer leaves the bundle with the triage\n   transcript only.\n\n3. tool_trace is written on the error path (part of 1).\n\n4. runner.process_job: the \"on failure the branch's state is moot\"\n   comment is replaced. The DROP ITSELF IS UNCHANGED, deliberately —\n   the worktree is keyed on bundle_id, so keeping a dirty one makes the\n   requeued job hard-refuse at assert_port_clean, turning a recoverable\n   provider blip into a permanent refusal. The bead's suggestion to\n   keep the branch on a harness raise would have made things worse. The\n   capture moving inside the step is what makes dropping safe, and the\n   comment now says so.\n\nTHE HOLE THE BEAD DID NOT NAME\n\nWriting only to changes.diff is not durable. A requeued attempt that\ngives up cleanly writes its own diff over it — and that diff is empty,\nbecause the retry starts from base and the rescued edits are not in its\ntree. So the rescue would have been undone by the retry it enabled.\nHence analysis/rescued/<job_id>.diff, which no later job overwrites.\nSame convention as the session dumps; shows in the UI under \"Other\".\n\nTWO SUPPORTING CHANGES TO _write_changes_diff\n\n- only_if_nonempty: skip the write when the capture is empty, and RAISE\n  on a capture failure rather than filing a tombstone. On the rescue\n  path \"nothing to save\" and \"could not look\" are different answers and\n  the caller reports them differently (patch_work_rescue_empty vs\n  patch_work_rescue_failed).\n- rc != 0 is now a failed capture, not an empty one. Previously an\n  unreachable chroot produced empty stdout, which every reader\n  downstream reads as \"the agent changed nothing\" — the one answer\n  indistinguishable from a real loss. Default path still files a\n  tombstone, now naming the git error.\n\nEMERGENT: _try_write_handoff runs after the rescue and reads\nchanges.diff, so manual_handoff.md now summarizes the rescued work\ninstead of being silent. Pinned by a test.\n\n20 tests in tests/test_patch_raise_rescue.py. Suite 3,868 passed.\n\nNOT FIXED HERE: the attempt's token spend is still discarded on the\nraise path — filed as poly-cg6u.1 under the token-economy epic.","owner":"github@dragonflybsd.org","priority":1,"status":"open","title":"a harness raise loses everything the patch agent did: no changes.diff, no session, and the branch is dropped anyway","updated_at":"2026-09-10T11:16:59Z"}
{"_type":"issue","comment_count":0,"created_at":"2026-09-10T08:45:11Z","created_by":"Fred [bot]","dependencies":[{"created_at":"2026-09-10T10:45:18Z","created_by":"Fred [bot]","depends_on_id":"poly-7ska","issue_id":"poly-dei7","metadata":"{}","type":"related"},{"created_at":"2026-09-10T10:45:17Z","created_by":"Fred [bot]","depends_on_id":"poly-l18e","issue_id":"poly-dei7","metadata":"{}","type":"parent-child"},{"created_at":"2026-09-16T21:28:36Z","created_by":"Fred [bot]","depends_on_id":"poly-l18e.3","issue_id":"poly-dei7","metadata":"{}","type":"relates-to"}],"dependency_count":0,"dependent_count":0,"description":"dsynth supports a compiler cache and the dev-env writes it off:\n\n  dev-env/dports_dev_env/dsynth.py:53\n      Directory_ccache= disabled\n\nEvery agent build round therefore recompiles the port from zero. On\nwww/firefox that is the ~45 min poly-7ska measures. On www/chromium it is\nabout thirteen hours.\n\nMEASURED — www/chromium, the attempt running 2026-09-10\n\n  dsynth_build started            01:09:39 UTC\n  at 08:11:44                     34,337 / 54,854 targets  (62.6%)\n  at 08:21:07                     34,886 / 54,854          (63.6%)\n  spot rate                       58.5 targets/min\n  projected finish                ~14:00-14:30 UTC, i.e. ~13 h\n\nRate curve from 28,764 object mtimes: 138/min early on cheap targets,\ndown to 17/min around 06:00, recovered to 45-59/min in Blink's V8\nbindings. The tail is link-heavy, so the last percent crawls.\n\nThe agent's loop is edit-one-patch -> rebuild. Those edits touch one or two\nleaf .cc files. With a warm cache the other ~54,000 objects hit and the\nround should fall to tens of minutes; without one, every round is the whole\nthirteen hours. That is the difference between a port that can be iterated\nand one that gets a single attempt a day.\n\nWHERE THE CHANGE GOES\n\nFour places, all in the dev-env distribution:\n\n1. dev-env/dports_dev_env/config.py — one Setting entry in the dev_env\n   table, next to dsynth_builders / dsynth_jobs. Per CLAUDE.md every\n   configurable value is one entry there and no environment variable.\n   Something like dev_env.dsynth_ccache (bool, default False) or a path.\n\n2. dev-env/dports_dev_env/config.py — matching typed field on DevEnvConfig\n   (dataclass at line ~31-38) and its construction at ~184-193.\n\n3. dev-env/dports_dev_env/dsynth.py — write_dsynth_config():\n   - add dsynth_root / \"ccache\" to the mkdir list (lines 30-37)\n   - emit Directory_ccache= /work/dsynth/ccache instead of \"disabled\"\n   The path must be the in-chroot one, like every other Directory_ entry.\n\n4. dev-env/dports_dev_env/config.py — dev_env.tool_pkgs_optional, which is\n   how the env provisions tooling. ccache is NOT installed on the builder\n   today: `pkg info -x ccache` -> \"No package(s) matching ccache\". dsynth\n   only wires the directory; the ports framework still needs the binary\n   present in the worker jail.\n\nEditing the live /usr/local/etc/... dsynth.ini instead would be reverted by\nthe next env refresh, since dsynth.py regenerates it.\n\nSPACE\n\n/     303G, 241G free\n/build 101G,  71G free\nexisting dsynth tree 43G\n\nChromium's cache is plausibly 15-30G. Not a constraint here.\n\nCAVEATS\n\n- dsynth reads its config at startup, so this cannot help a build already\n  running, and the env refresh should wait until one finishes.\n- The first build after enabling is a cold cache and saves nothing. The\n  payoff starts on the round after that.\n- chromium compiles with -fprofile-use=<...>.profdata; ccache has to hash\n  that input. Worth confirming the hit rate on the second build rather than\n  assuming it.\n- A patch to a widely-included header still invalidates a large slice. Leaf\n  .cc patches, which is what portability fixes usually are, should hit.\n\nRELATION TO poly-7ska\n\nSame goal, different mechanism, and they compose. poly-7ska keeps the\nobjdir and mounts alive (dsynth debug) so an incremental gmake is possible;\nit changes how the agent drives a build. ccache leaves dsynth's\nforce-rebuild exactly as it is and makes the recompile cheap instead,\nsurviving the jail teardown that poly-7ska works around. ccache is the\nsmaller change: configuration plus a package, no tool-loop work.\n","id":"poly-dei7","issue_type":"feature","owner":"github@dragonflybsd.org","priority":1,"status":"open","title":"dev-env writes Directory_ccache= disabled, so every agent build round recompiles from zero — 13 hours on chromium","updated_at":"2026-09-10T08:45:11Z"}
{"_type":"issue","close_reason":"Landed 6092166 (Sep 9), deployed — inline_bodies is present in the installed context.py. No notes were ever written on the bead; the commit body carries the record.","closed_at":"2026-09-14T18:16:21Z","comment_count":0,"created_at":"2026-09-09T19:53:34Z","created_by":"Fred [bot]","dependency_count":0,"dependent_count":0,"description":"ExistingPatchesSection inlines every port/files/patch-* in the bundle with no\ncap on how many. On www/chromium that is 1,604 patches and 93.5% of the\nprompt, and it is the wrong 1,604: the overlay's own dragonfly/ patches are\nnot in the payload at all.\n\nMEASURED — www_chromium-20260909-135208Z, patch attempt 1 session\n\n  payload (one user message)   2,056,421 chars   ~514,105 tok\n  \"### Existing Patches\"       1,921,730 chars   ~480,432 tok   93.5%\n    1,604 #### subsections, runs to end of payload\n  everything else                134,691 chars    ~33,672 tok    6.5%\n\nBundle artifact count for port/files/patch-* is 1,604 — one per subsection.\n\nCOST\n\nThree chromium patch attempts, from runner.log llm_turn accounting:\n\n  attempt   turns   billable     output   full cache misses   miss cost\n  1            92   2,242,243        3%    3                  1,959,653  87%\n  2            58   5,365,245        1%    8                  5,255,552  98%\n  3           154   5,532,226        2%    6                  4,645,529  84%\n                   ----------                                 ---------\n                   13,139,714                                 ~11.9M\n\nModel output is 1-3% of spend. Everything else is re-sent input, and a\nre-send costs 620-840k because the payload is that big. 17 misses did it.\n\nCache behaviour itself does not look controllable from here: misses follow\ngaps as short as 33s and hits as long as 173s, after every kind of tool, so\nit is not a TTL and not tied to the ~5 min dsynth_build. Payload size is the\npart we own.\n\nTHE PER-FILE CAP DOES NOT HELP\n\nrunner.context_file_cap = 32768. Across the 1,604 subsections: median 780\nchars, mean 1,198, max 26,376. Zero were truncated. The cap was added for\nthe opposite shape -- PortFilesSection's docstring cites python311's 533 KB\npkg-plist -- and a per-file cap cannot see 1,604 individually-reasonable\nfiles. There is no section-level or payload-level budget.\n\nTHE SELECTION IS ALSO WRONG\n\ncontext.py:472 filters on p.startswith(\"port/files/patch-\"). For chromium:\n\n  composed files/       1606 (1604 patch-*, plus chrome.in, chromium-browser.desktop.in)\n  composed dragonfly/    144\n  in both                 96   ours overriding a FreeBSD patch of the same name\n  dragonfly-only          48\n  bundle port/dragonfly/   0\n\nSo the agent is shipped 1,604 FreeBSD patches and none of the overlay's own.\nSpot-checked four dragonfly-only patches (patch-base_process_launch__posix.cc\nand three others): none appear in the payload. For the 96 shared names it\nsees the FreeBSD content, not the DragonFly override that the port actually\ncarries. A patch agent working on a DragonFly overlay is being shown\neverything except the DragonFly patches.\n\nWHAT THE AGENT ACTUALLY USED\n\nSame session, its own retrieval across 92 turns:\n\n  get_file               14 calls    ~14,772 tok\n  grep                   15 calls    ~19,504 tok\n  get_effective_overlay   1 call      ~4,109 tok\n  dops_reference          1 call      ~3,088 tok\n  list_dir                2 calls       ~402 tok\n  TOTAL                  33 calls    ~41,876 tok\n\nIt has the tools and uses them. Fetching on demand costs ~42k against\n~480k for inlining everything up front.\n\nFIX DIRECTION\n\n- Give the section a total budget, not just a per-file one, and prefer a\n  manifest over bodies past that budget: 1,604 filenames is ~50k chars\n  (~12k tok) against ~480k for the contents.\n- Include the overlay's dragonfly/ patches, and where a name exists in both,\n  show the one that governs.\n- Consider ordering by relevance to the failing build rather than sorted()\n  when a budget forces a choice.\n\nPayload would drop ~514k -> ~35k, making every cache miss ~15x cheaper. The\nsame three attempts fit inside the old 5M tier budget with room to spare.\nNote this also bounds the value of raising policy.tiers.ASSIST.max_tokens to\n30M (done 2026-09-09): at 800k a miss that mostly buys more expensive misses.\n\nCORRECTION TO AN EARLIER READING\n\nSplitting www/chromium's monolithic dragonfly/patch-all into 142 per-file\npatches (2026-09-09) did NOT contribute. Those live in dragonfly/, which the\npayload does not include. The 1,604 are FreeBSD's own and predate it.\n","design":"REVISED after looking at what other harnesses do. The first draft proposed\n\"budget + rank + inline until the budget is spent\". That is still the wrong\nshape. No comparable harness pushes file bodies into the prompt at all.\n\nWHY runner.context_file_cap IS THE WRONG PRIMITIVE\n\nIt is used in exactly one place -- context.py -- i.e. only on content pushed\ninto the prompt unasked. The pull side already does the right thing:\nget_file takes limit_lines plus start_line/end_line, which is ordinary\npagination.\n\nA per-file cap on pushed content fails twice over:\n  - it cannot bound the total. 1,604 files x median 780 chars = 480k tokens,\n    every one of them under the 32,768 cap, zero truncated.\n  - when it does fire it hands the model a head+tail splice of a file it\n    never asked for, with no way to know what was elided.\n\nPRIOR ART\n\nAider (aider.chat/2023/10/22/repomap.html) does not inline files. It builds a\nrepo MAP: a graph of symbol definitions and references, personalized PageRank\nto rank by relevance, then renders top-ranked DEFINITIONS -- signatures, not\nbodies -- binary-searched to fit a token budget that defaults to 1k tokens.\nWe spend 480k on the same job.\n\nSWE-agent (arxiv.org/abs/2405.15793) rejects inlining explicitly: \"Flooding a\nlanguage agent's context window with too much file content can be\ninefficient, since the majority of file content is usually irrelevant to the\nissue,\" and notes such inefficiency \"can exhaust an agent's cost budget or\ncontext window, leading to even worse performance.\" It gives the agent\nwindowed viewing (100-line windows) with scroll/goto instead.\n\nClaude Code, Pi and OpenClaw cap file READS -- 256KB stat check, 2,000 lines\nor 50KB -- and pair the cap with offset/limit pagination, persisting\noversized results outside the context. The cap belongs on what a tool\nreturns when asked, which is where ours already is for get_file.\n\nOn truncation as such: \"Truncation that's too aggressive isn't just lossy --\nit's misleadingly lossy, because the agent has no way to know it's missing\nthe relevant span.\"\n\nREVISED FIX\n\n1. ExistingPatchesSection emits a MANIFEST, not bodies. Names only, grouped,\n   with the dragonfly/ override marked where a basename exists in both. For\n   chromium that is ~1,604 names (~50k chars) or, better, per-directory\n   counts plus the paths -- against 1,921,730 chars today.\n\n2. No inlined bodies by default. The measured case for inlining is empty:\n   in attempt 1 the agent made 14 get_file calls and read NONE of the 1,604\n   patches. It read overlay.dops, the port Makefile, Mk/Features/lto.mk,\n   bootstrap.py, gen.py, libcxx22/newport/Makefile -- several with\n   start_line/end_line. It fetches precisely when it wants something.\n\n3. Name the location. The manifest should give the composed port path\n   (/work/artifacts/compose/@2026Q3/<origin>/files and .../dragonfly) so\n   get_file and list_dir reach the real files. The agent already reads live\n   chroot paths, so it needs no bundle artifact to see a patch.\n\n4. Leave runner.context_file_cap alone for PULLED results, and stop using it\n   as if it were a section budget. If a pushed section ever needs a bound,\n   it needs a section-level budget, not a per-file one -- but per 2, the\n   right bound here is zero bodies.\n\n5. Optional, later: if orientation beyond names proves useful, the Aider\n   shape is the model -- rank by relevance to the failing build (patches\n   whose target path appears in logs/errors.txt), and render something\n   smaller than a body, e.g. the ---/+++ header and hunk count.\n\nWHAT DOES NOT CHANGE\n\nLayer 1 stands: hook_pkg_failure has no dragonfly/ loop, so no bundle has\nport/dragonfly/ artifacts and the overlay's own patches are invisible. That\nis a correctness bug independent of the token question, and a manifest that\nlists only port/files/ would still be wrong.\n\nEXPECTED EFFECT\n\nPayload 514k -> ~35k plus a names list. Cache misses ~15x cheaper. The three\nattempts that cost 13.1M fit inside the old 5M tier budget. And per\nSWE-agent's finding, less irrelevant content should improve the agent's\nbehaviour, not merely its cost.\n","id":"poly-3x8o","issue_type":"bug","owner":"github@dragonflybsd.org","priority":1,"started_at":"2026-09-09T20:26:54Z","status":"closed","title":"the patch payload inlines every port/files patch — 93% of chromium's prompt, and it omits the overlay's own dragonfly patches","updated_at":"2026-09-14T18:16:21Z"}
{"_type":"issue","comment_count":0,"created_at":"2026-09-09T15:27:43Z","created_by":"Fred [bot]","dependency_count":0,"dependent_count":0,"description":"Six defects in the dops/mk engine itself -- not in the agent that drives\nit. Each one is an operation that silently does nothing and reports success,\nso the agent, the gate and the operator all read a fix that never reached\nthe build.\n\n  poly-8fxz  assess_overlay counts newport/ as unmigrated, so no type-dport\n             port can reach 'converted' and every successful dport patch is\n             escalated. 88 ports affected; one measured case discarded a\n             correct fix after 963,612 billable tokens.\n  poly-ov2b  a dragonfly/ file with no 'file materialize' op is inert\n  poly-cpd   mk eval OPTIONS_DEFAULT lands after options are computed\n  poly-85p   mk disable-if leaves an unguarded body and an if-less endif\n  poly-npw9  a diffs/REMOVE is ignored once a port has an overlay.dops\n  poly-bm9   the 2026Q3 locale fix misses WRK_ENV, so the build is unchanged","id":"poly-bz7n","issue_type":"epic","owner":"github@dragonflybsd.org","priority":1,"status":"open","title":"Compose engine: an op that does nothing must not report success","updated_at":"2026-09-09T15:27:43Z"}
{"_type":"issue","comment_count":0,"created_at":"2026-09-09T15:27:41Z","created_by":"Fred [bot]","dependency_count":0,"dependent_count":0,"description":"One round of the patch loop costs a full dsynth test -- ~45 min and ~380k\ntokens measured on www/firefox -- and most of that is avoidable. Five beads,\nno parent.\n\n  poly-7ska  every round force-rebuilds; dsynth debug could be incremental\n  poly-9sw   dsynth_build and dsynth_test both run, so a successful attempt\n             compiles the port twice for ~3% of extra signal\n  poly-8dt   dsynth_build rebuilds every flavor: 91 minutes recompiling a\n             flavor that was already green\n  poly-bxo   the agent's build reaches any dependency its own patch\n             introduces, unbounded and unfiltered\n  poly-aoi   dsynth_log returns lines=0 for a log that exists\n\nOverlaps poly-zze (EPIC) at poly-9sw: which build is the oracle is a Block A\nquestion, how much it costs is this one.","id":"poly-l18e","issue_type":"epic","owner":"github@dragonflybsd.org","priority":1,"status":"open","title":"Build economics: make one agent build round cheap","updated_at":"2026-09-09T15:27:41Z"}
{"_type":"issue","comment_count":0,"created_at":"2026-09-09T15:27:40Z","created_by":"Fred [bot]","dependency_count":0,"dependent_count":0,"description":"poly-abr shipped the three rc.d scripts and closed all twelve children.\nNine measured defects since then are its consequences, and they have no\nparent. Successor to poly-abr, not a reopening of it.\n\nInstalling:  the extras cannot install on DragonFly at all (poly-170), and a\n  packaged install has no 'dportsv3 dev-env' though the docs say to use it\n  (poly-12c, mapped to poly-15g).\nUpgrading:   the pidfile rename orphans a running stack (poly-af7), and the\n  documented upgrade leaves every env's dsynth hooks stale (poly-440).\nRunning:     nothing warns when the evidence root is tmpfs and it had already\n  emptied (poly-cl3); sudo runs leave root-owned files in the venv (poly-6qs).\nScripting:   dev-env create (poly-a9b) and dbuild (poly-irk) both exit 1 on\n  success, so neither can be driven from a script.\nConfiguring: dev_env.delta_root is an env var naming a fixed checkout, not a\n  setting (poly-wnn); make_extract has no override channel for base-owned\n  config (poly-0oe).","id":"poly-2540","issue_type":"epic","owner":"github@dragonflybsd.org","priority":1,"status":"open","title":"Operable deployment: the aftermath of the rc.d epic","updated_at":"2026-09-09T15:27:40Z"}
{"_type":"issue","comment_count":0,"created_at":"2026-09-09T15:27:40Z","created_by":"Fred [bot]","dependency_count":0,"dependent_count":0,"description":"The LLM path was written against one endpoint and hardcodes its dialect.\nEight beads, no parent.\n\nPortability:  the openai-SDK path mangles the model id and speaks one\n  thinking dialect (poly-ajd); a second real endpoint confirmed it (poly-p0h).\nCost control: reasoning cannot be turned off because litellm 1.65.0 drops\n  extra_body (poly-r1g) -- 94% of output on the measured attempt -- and the\n  chat role never disables it either (poly-91w).\nFailure honesty: a transient error kills the job with a 1.5s retry window\n  (poly-4av), and a provider outage is recorded as agent_gave_up, so the\n  verdict is untrustworthy (poly-jg9; poly-e75 is the same defect filed two\n  days earlier and should be closed into it).\nPrevention:   config check never preflights the model (poly-7no).","id":"poly-so8j","issue_type":"epic","owner":"github@dragonflybsd.org","priority":1,"status":"open","title":"Provider layer: one model interface that survives a second provider","updated_at":"2026-09-09T15:27:40Z"}
{"_type":"issue","comment_count":0,"created_at":"2026-09-09T15:27:39Z","created_by":"Fred [bot]","dependency_count":0,"dependent_count":0,"description":"Two halves that keep being filed separately and are the same subject.\n\nWHAT IT COSTS. poly-0g0 established that two quantities are both called\n'tokens' -- billable (what the budget enforces) and the re-billed total,\nwhich runs 7-21x higher -- and that nothing on screen says which is which.\nFive follow-ons remain: the cards select spend by stage name (poly-5bl),\nseven message strings still print a bare 'tokens' (poly-cwi), the collapsed\ncard headlines the 21x figure (poly-9t9), 'Billable (real cost)' is not the\nreal cost (poly-tgc), and the handoff asks about a budget it never states\n(poly-6e4). activity_log's global cap silently shrinks the answer (poly-lvyr).\n\nWHAT ENDS IT. The budget is not what stops an attempt: a hardcoded turn cap\nof 30 is (poly-lvw). Attempts start under a budget they cannot finish in\n(poly-5e1), min_attempt_budget_fraction makes max_iterations above 4\nunreachable (poly-n7ab), tool results accumulate untruncated so cost grows\nquadratically (poly-7eh), tool_loop never prunes (poly-hnk3), one dsynth_log\ncall cost 690k tokens for want of a byte cap (poly-9hjm), and a free endpoint\nstill gets a cost bound it does not need (poly-w3s).","id":"poly-cg6u","issue_type":"epic","owner":"github@dragonflybsd.org","priority":1,"status":"open","title":"Token economy: what an attempt costs, and what actually ends it","updated_at":"2026-09-09T15:27:39Z"}
{"_type":"issue","comment_count":0,"created_at":"2026-09-09T15:27:38Z","created_by":"Fred [bot]","dependency_count":0,"dependent_count":0,"description":"Fourteen measured defects in the patch and triage agents, six of them already\nin progress and none of them owned by an epic. They are one body of work:\nevery one changes what the agent is told, what it can write, or what it can\nobserve about its own attempt.\n\nThree sub-threads:\n  * AUTHORITY — triage writes the fix and patch transcribes it (poly-6cj),\n    the payload offers a prior diff as an exemplar (poly-9u2), and closing\n    the diff channel did not break it because the recipe is in prose (poly-0j4).\n  * WRITE PRIMITIVES — put_file truncates source files (poly-7jw), the\n    genpatch -> install_patches flow dead-ends (poly-dq5), so the agent\n    hand-edits patch files (poly-n78) and once proposed deleting one (poly-28j).\n  * WHAT IT KNOWS AND KEEPS — toolchain playbooks never fire (poly-j83), it\n    never reads a sibling version's overlay (poly-hek), it re-reads its own\n    context (poly-47d), greps its own evidence tree (poly-9oo), and a harness\n    exception throws away the artifacts of an attempt that did real work\n    (poly-4cil).","id":"poly-783c","issue_type":"epic","owner":"github@dragonflybsd.org","priority":1,"started_at":"2026-09-16T17:22:55Z","status":"in_progress","title":"Patch/triage loop correctness: the agent must fix the port, not transcribe a recipe","updated_at":"2026-09-16T17:22:55Z"}
{"_type":"issue","comment_count":0,"created_at":"2026-09-09T15:21:49Z","created_by":"Fred [bot]","dependencies":[{"created_at":"2026-09-09T17:22:00Z","created_by":"Fred [bot]","depends_on_id":"poly-qkp","issue_id":"poly-hzd0","metadata":"{}","type":"related"},{"created_at":"2026-09-09T17:32:37Z","created_by":"Fred [bot]","depends_on_id":"poly-zze","issue_id":"poly-hzd0","metadata":"{}","type":"parent-child"}],"dependency_count":0,"dependent_count":0,"description":"The proof-block orphan rescue in attempt_loop cannot tell \"no proof block\nemitted\" from \"proof block emitted saying false\", so it overrides an explicit\ngave-up report and resolves the bundle agent_fixed.\n\nOBSERVED\n\nwww_chromium-20260909-135208Z, patch attempt 1. The agent's own analysis/patch.md:\n\n  ## Rebuild Status\n  gave-up\n\n  ## Rebuild Proof (JSON)\n  { \"rebuild_ok\": false }\n\nand in its summary: \"the link then fails on undefined reference to\n'__cxa_thread_atexit' ... which I could not resolve within this attempt\".\n\nThe agent was correct and honest. What got written was\nanalysis/rebuild_proof.json:\n\n  { \"rebuild_ok\": true, \"source\": \"tool_result\", ... }\n\nand the bundle resolved resolution=agent_fixed on a port whose last\ndsynth_build returned rebuild_ok=False.\n\nMECHANISM\n\nattempt_loop.py:592-612\n\n    proof = _parse(prev_text)\n    rebuild_ok = bool(_ok(proof))\n    ...\n    if not rebuild_ok and rebuild_ok_seen and is_success is None:\n        proof = {\"rebuild_ok\": True, \"source\": \"tool_result\"}\n        rebuild_ok = True\n\nTwo problems.\n\n1. `not rebuild_ok` is satisfied by BOTH an absent proof block and a proof\n   block that explicitly says false. The comment above the branch only\n   contemplates the first (\"the LLM may have run out of budget before it\n   could emit ## Rebuild Proof (JSON)\"). Nothing distinguishes them, so an\n   explicit negative is silently upgraded.\n\n2. `is_success is None` reads like a status guard but is not one. It is the\n   optional custom success-predicate parameter, None for every patch run\n   (convert passes its own). It never blocks anything here.\n\n`rebuild_ok_seen` is also sticky for the whole attempt. Here it was set at\n14:22:40 by a dsynth_build that returned rebuild_ok=True after 4 seconds --\nagainst ~5 minutes for the real builds in the same attempt -- then survived\nfour subsequent FAIL builds over the next 30 minutes to arm the rescue at\n14:54:43. Even a rescue that only fired on a genuinely absent proof would be\nlifting a stale True.\n\nRELATION TO poly-qkp\n\nSame rescue, same synthesized {\"rebuild_ok\": true, \"source\": \"tool_result\"},\nsame bogus agent_fixed. poly-qkp is the attempt hitting the turn cap so no\nfinal report exists -- the case the branch was written for. This is the\nbranch firing against a report that exists and says gave-up. A fix that only\naddresses the turn-cap path leaves this one open, and vice versa; most\nlikely both want the same repair.\n\nFIX DIRECTION\n\n- Distinguish absent proof from negative proof. Only rescue when _parse\n  returned nothing at all; never when it returned rebuild_ok=false.\n- Do not lift a stale rebuild_ok_seen. If the rescue is kept, gate it on the\n  MOST RECENT dsynth_build result, not on any-time-during-the-attempt.\n- Rename or remove the `is_success is None` clause; it does not do what it\n  reads as.\n\nSECONDARY, NOT VERIFIED\n\nWhy the 14:22:40 build exited 0 in 4 seconds is unconfirmed. rebuild_ok is\np.returncode == 0 (worker.py:3264, identical in the installed copy on the\nbuilder) with no guard for dsynth ignoring or skipping a port rather than\ncompiling it, and tools.py:169 asserts to the model that \"rebuild_ok=true\nmeans rc==0, i.e. it built AND passed the gate\". The ignore/skip hypothesis\nhas NOT been tested; the per-port log was overwritten by later attempts and\nagent builds run hooks-disabled, so nothing was recorded. Confirm before\nacting.\n","id":"poly-hzd0","issue_type":"bug","owner":"github@dragonflybsd.org","priority":1,"status":"open","title":"the proof-block orphan rescue overrides an explicit rebuild_ok:false, so a gave-up attempt still reports agent_fixed","updated_at":"2026-09-09T15:33:11Z"}
{"_type":"issue","comment_count":0,"created_at":"2026-09-09T00:02:52Z","created_by":"Fred [bot]","dependencies":[{"created_at":"2026-09-09T17:28:19Z","created_by":"Fred [bot]","depends_on_id":"poly-bz7n","issue_id":"poly-8fxz","metadata":"{}","type":"parent-child"}],"dependency_count":0,"dependent_count":0,"description":"A `type dport` port can never be classified `converted`, so the patch flow\nescalates every successful fix on one. Affects 88 ports.\n\nWHAT HAPPENED (devel/libcxx22, 2026-09-08)\n\n  bundle devel_libcxx22-20260908-224530Z\n  patch job 20260908-225449Z-2026Q3-devel_libcxx22-554173-patch.job\n\ncheck-plist failed (577 `Error: Orphaned:` + 1 `Error: Missing:`). The agent\nedited `ports/devel/libcxx22/newport/pkg-plist` -- the correct and only\nsurface for a dport -- adding exactly the 577 orphans and removing the 1\nmissing entry. Rebuild passed: rebuild_ok=True, patch status=success.\n\nIt was then escalated:\n\n  Reason: patch_non_dops_substrate\n  Detail: build passed but port dops_state='needs_judgment'\n          (expected 'converted'); fix likely written as compat artifacts\n          instead of overlay.dops\n\nThat detail is false. There are no compat artifacts; the agent edited the\nonly file that exists. Cost: 963,612 billable over 213 turns, discarded.\n\nMECHANISM\n\nThe gate is steps.py:1415:\n\n    post_state = ctx.state.get(\"post_patch_dops_state\")\n    if post_state != \"converted\":\n        ... ESCALATE_MANUAL, reason=\"patch_non_dops_substrate\"\n\nCorrect for a `type port`: a build that went green by editing a leftover\nMakefile.DragonFly is not a dops fix and should not be stamped agent_fixed.\n\n`post_patch_dops_state` comes from classify_dops -> assess_overlay\n(overlay_state.py). There, `newport/` is counted as an unmigrated legacy\nartifact:\n\n    overlay_state.py:245   unmigrated = (... + ((\"newport/\",) if facts.newport else ()))\n    overlay_state.py:294   \"legacy_newport_present\", severity=\"conversion_blocker\"\n    overlay_state.py:298   invariants.append(\"dops_with_unmigrated_newport\")\n    overlay_state.py:299   if not unmigrated: return state=\"converted\"\n\nFor a `type port`, a stray `newport/` genuinely is an unfinished migration.\nFor a `type dport`, `newport/` IS the port, permanently and by design -- the\noverlay.dops is a 4-line header with zero ops. So a fully and correctly\nmigrated dport is indistinguishable from a half-migrated normal port, and\nline 299 is unreachable for any of them.\n\nROOT CAUSE: MISSING INPUT, NOT A WRONG COMPARISON\n\nOverlayFacts (overlay_state.py:35) records only presence:\n\n    overlay_dops: bool = False     # not the declared type\n    newport: bool = False\n\nThe declared `type` never reaches the assessor, so it cannot distinguish the\ntwo cases even in principle. The planner already extracts it\n(engine/planner.py:444-452, TypeDirective -> plan_type).\n\nThe same file knows the right answer 200 lines earlier. The bootstrap\ndecision at overlay_state.py:89-94:\n\n    # A pure dport: newport/ is the whole port -- declare type=dport, drop\n    # the now-redundant STATUS (the header carries the type).\n    if facts.newport:\n        return BootstrapDecision(\"bootstrap\", overlay_type=\"dport\", ...)\n\nSo the code can author a dport correctly and then cannot recognise the thing\nit authored.\n\nBLAST RADIUS\n\n88 ports in DeltaPorts declare `type dport`, and all 88 have `newport/`\n(grep -l '^type dport' ports/*/*/overlay.dops). None can reach `converted`,\nso the agent can never auto-fix any of them however correct the fix -- it\nwill always build green and then escalate with a misleading accusation.\n\nSUGGESTED FIX\n\n1. Carry the declared overlay type into OverlayFacts (parse the `type`\n   directive; planner.py already does this).\n2. In assess_overlay, exclude `newport/` from `unmigrated` and from the\n   `conversion_blocker` rule when the declared type is `dport`.\n3. Keep both when the declared type is `port` -- that case is a real\n   unfinished migration and the existing guard is right.\n4. Consider flagging the inverse (`type dport` with NO newport/) since that\n   overlay declares a source tree that does not exist.\n\nThe steps.py:1415 gate itself needs no change once the classification is\nright.\n\nNOTE ON THE ESCALATION TEXT\n\nEven after the classification is fixed, `patch_non_dops_substrate`'s detail\nstring asserts a cause (\"fix likely written as compat artifacts\") it has not\nchecked. Worth making it name the artifacts it actually found, so a wrong\ndiagnosis is visible rather than plausible.","id":"poly-8fxz","issue_type":"bug","owner":"github@dragonflybsd.org","priority":1,"status":"open","title":"assess_overlay counts newport/ as unmigrated, so no type-dport port can reach 'converted' and every successful dport patch escalates","updated_at":"2026-09-09T00:02:52Z"}
{"_type":"issue","comment_count":0,"created_at":"2026-09-08T16:02:27Z","created_by":"Fred [bot]","dependencies":[{"created_at":"2026-09-09T17:28:20Z","created_by":"Fred [bot]","depends_on_id":"poly-bz7n","issue_id":"poly-ov2b","metadata":"{}","type":"parent-child"}],"dependency_count":0,"dependent_count":0,"description":"In dops-mode compose the compat auto-copy is suppressed\n(compose_stages.py:832, `if ctx.mode != \"compat\": continue`, surfaced as\nI_COMPOSE_MODE_DOPS_SUPPRESSES_COMPAT). A file under dragonfly/ therefore\nreaches the build ONLY via an explicit `file materialize` op.\n\nNothing tells the agent when that op is missing. The patch phase cannot\nfail on it -- a patch that is not in the composed tree is not applied, and\nnot applying is not an error -- so the build proceeds and fails for the\noriginal reason. From the agent's side the fix \"did not work\".\n\nTHREE INSTANCES THIS WEEK\n\n1. multimedia/v4l_compat (2026-09-06). Agent wrote the patch, omitted the\n   materialize op, and the bundle read as an incomplete fix.\n\n2. www/firefox run 2 (2026-09-07). patch-tools_profiler_lul_LulElfInt.h\n   created, not materialized. Caught in review before commit; the composed\n   tree had 7 patches where dragonfly/ had 8.\n\n3. www/firefox run 4 (2026-09-08). THE EXPENSIVE ONE.\n   patch-config_gcc-stl-wrapper.template.h created, not materialized. The\n   agent then spent its last three rounds -- roughly two hours of wall\n   clock -- trying variants that landed in the *generated* wrapper headers\n   instead, because its correct approach (patch the template that\n   generates them) appeared not to work. It never reached a build.\n\nWHAT EXISTS TODAY, AND WHY IT DOES NOT COVER THIS\n\ndportsv3/agent/reconcile.py handles the OPPOSITE direction: when an edit\nremoves a materialize op and strands its file, reconcile_orphaned_artifacts\ndeletes the file. Its docstring is explicit that an unreferenced artifact\nis \"inert ... it does no harm to the build, but it lingers as dead\nsubstrate\".\n\nThat is true for a stranded file. It is not true for a NEW file: there the\ninertness is the whole problem, because the agent believes it applied a fix.\n\nIt also runs too late to help. steps.py:1323:\n\n    if (result.status or \"\").lower() == \"success\":\n        ...\n        removed = reconcile_orphaned_artifacts(port_dir)\n\nSuccess-only, after the attempt, before the diff is captured. A failing\nattempt never reaches it, which is exactly the case where the missing op\ncaused the failure.","design":"Wanted: a signal DURING the attempt, at the moment the agent would\notherwise start a 45-minute build against a tree that does not contain its\nchange.\n\nNatural home is validate_dops or materialize_dports -- the agent already\ncalls both, and materialize_dports is the last step before dsynth_build in\npractically every round. Either could report:\n\n    dragonfly/ contains 35 files; overlay.dops has 34 `file materialize`\n    ops. Unreferenced: patch-config_gcc-stl-wrapper.template.h\n    (this file will NOT be applied).\n\nTwo design points:\n\n- Warn, do not block. An unreferenced file is legitimate when the agent is\n  deliberately retiring a patch -- www/firefox run 5 did exactly that,\n  removing the materialize op for a patch it had proved could not work.\n  Blocking would have prevented the correct action.\n\n- Distinguish the two directions if cheap. \"New file, no op\" is nearly\n  always a mistake; \"op removed, file stranded\" is the case reconcile.py\n  already cleans up. The wording should not tell the agent to re-add an op\n  it deliberately deleted.\n\nThe invariant is simply: count(dragonfly/*) == count(`^file materialize`),\nand naming the difference is enough. Every instance above would have been\ncaught by that one line.","id":"poly-ov2b","issue_type":"bug","owner":"github@dragonflybsd.org","priority":1,"status":"open","title":"A dragonfly/ file with no 'file materialize' op is silently inert during the attempt; the agent tests a fix that never reached the build","updated_at":"2026-09-08T16:02:27Z"}
{"_type":"issue","close_reason":"Observation masking and the note tool, a012685 (on top of 1294712). Notes do not reach later jobs yet; see the REVIEWED note.","closed_at":"2026-09-16T18:43:11Z","comment_count":0,"created_at":"2026-09-08T16:01:11Z","created_by":"Fred [bot]","dependencies":[{"created_at":"2026-09-17T15:23:40Z","created_by":"Fred [bot]","depends_on_id":"poly-783c.3","issue_id":"poly-hnk3","metadata":"{}","type":"relates-to"},{"created_at":"2026-09-09T17:27:58Z","created_by":"Fred [bot]","depends_on_id":"poly-cg6u","issue_id":"poly-hnk3","metadata":"{}","type":"parent-child"}],"dependency_count":0,"dependent_count":0,"description":"tool_loop.run appends and never removes. The only mutations of the message\nlist are:\n\n  tool_loop.py:233   messages.append(_assistant_message_from(response))\n  tool_loop.py:293   messages.append(...)\n\nNo prune, no cap, no elision anywhere in the file. Context therefore grows\nmonotonically for the life of an attempt, and since every dsynth_build\noutlives the prompt cache, the cost of each build round is whatever the\ncontext has reached by then.\n\nMEASURED (www/firefox, 2026-09-07/08)\n\n  run 4, attempt 1     78k at T1  ->  185k at T106  ->  348k at T124\n                       per-eviction cost rose with it, ~140k -> ~348k\n\n  run 3, attempt 1     166k, then one oversized dsynth_log took it to\n                       857k, and every later eviction cost ~860k\n                       (see poly-9hjm for that call)\n\nAttempts currently get their context reset only by ending. That works --\nrun 3 attempt 2 restarted at 80,697 after attempt 1 reached 314,527 -- but\nit is a reset, not a compaction: everything is re-derived from the bundle,\nwhich is why the first turns of an attempt re-read files the previous\nattempt had already read.\n\nWHY IT MATTERS MORE NOW\n\nrunner.max_tool_turns is 300 in the live config. A 300-turn attempt on a\nbig port does ~15 build rounds; with unbounded growth the last rounds cost\nseveral times the first. The turn cap is only usable if the per-turn cost\ndoes not climb with turn number.","design":"THE POLICY IS ALREADY WRITTEN\n\nfix_chat.render_session_transcript (fix_chat.py:101) does exactly the\nright thing, and its docstring states the reasoning:\n\n    \"When over cap, keeps the head and tail and elides the middle: the\n     head holds the failure + triage + first-move reasoning, the tail\n     holds the winning approach + rebuild proof -- both are what 'why'\n     questions hinge on. The repeated middle build cycles are the most\n     expendable. Whole-block, so no turn is cut mid-sentence (a single\n     oversized block is head+tail clipped first).\"\n\nDEFAULT_CONTEXT_CAP = 96 * 1024 (fix_chat.py:31).\n\nIt is only reached from the tracker's fix-review chat --\nagentic_api.py:373 via build_chat_messages -- which is a tools-off Q&A\nover frozen artifacts. The loop that actually accumulates context never\nuses it.\n\n\"The repeated middle build cycles are the most expendable\" describes a\n300-turn porting attempt more accurately than it describes the review\nchat.\n\nWHY THIS IS CHEAP\n\nfix_chat's elision is pure text/block manipulation. It costs nothing to\nrun. Claude-Code-style compaction by summarisation would cost one full\ncontext read to apply -- at 860k that is a 17% tax just to shrink.\n\nCARE NEEDED\n\n- fix_chat renders tool_calls as text because it runs tools-off. A live\n  loop must keep the structured tool_call/tool_result pairing intact, so\n  elision has to drop whole call+result pairs, not rewrite them.\n- Dropping a tool result the model still refers to will confuse it. The\n  head/tail shape helps (recent turns survive) but the boundary needs\n  thought.\n- Interacts with worker's \"you already have this\" dedup\n  (reset_attempt_caches, worker.py:1352): if an elided file read is later\n  deduped as already-seen, the model is refused content it no longer has.\n  That failure mode is already documented in attempt_loop.py:399.","id":"poly-hnk3","issue_type":"feature","notes":"REVISED 2026-09-16: mask old tool output, don't drop exchanges.\n\n1294712 (unpushed) did the first design: over a byte cap, leave the oldest\nwhole exchanges out of the request. The literature says that is the variant\nnobody uses: it deletes the agent's actions and its failed turns.\n\nWHAT THE EVIDENCE SAYS\n\n- JetBrains, \"The Complexity Trap\" (arXiv 2508.21433, NeurIPS'25 DL4Code).\n  SWE-agent and OpenHands on SWE-bench Verified, five model setups.\n  Replacing tool observations older than the last 10 turns with a\n  placeholder, while keeping every reasoning step and action, cut cost\n  by more than half and matched or beat LLM summarization in 4 of 5\n  settings (Qwen3-Coder: +2.6 points solved, 52% cheaper). Summarization\n  made agents run 15% longer, cost up to 7% on its own, and got almost no\n  cache reuse. The window is agent-specific and must be tuned.\n- Anthropic clear_tool_uses_20250919: keep the calls, replace old results\n  with a placeholder, keep=3 by default, exclude_tools, and clear_at_least\n  so each cache invalidation is worth paying for. Clearing +29%, clearing\n  plus the memory tool +39% on their agentic-search eval.\n- Manus: prompt-cache hit rate is the top cost metric; append-only\n  context; restorable compression (drop content, keep the path); keep\n  failed turns visible.\n- Server-side context editing and compaction are out of reach: the\n  harness calls chat.completions / litellm on OpenAI-compatible endpoints.\n\nGAP SPECIFIC TO THIS HARNESS\n\nMasking relies on the model's reasoning surviving it. In misc/py-onnx\n(2026-09-16) 0 of 138 assistant turns stored reasoning_content and 19 had\nany text, so masked outputs would take the why with them. The literature's\nanswer is note-taking (Anthropic's memory tool, Manus's todo.md).\n\nCONSIDERED AND REJECTED\n\nMasking when a slow tool call has probably let the cache expire, to make\nthe mask free. Measured on py-onnx: 60-120s calls preceded a miss 2 times\nin 10, and 5 misses followed no long call at all. Not predictable enough.\n\nDESIGN\n\n1. tool_loop masks tool results older than llm.patch.context_keep_turns\n   (default 10) with a fixed placeholder that keeps ok/rebuild_ok and says\n   to repeat the call. Calls are never masked. messages, and so the\n   session dump, keep everything; only the request is masked.\n2. Batched: mask once llm.patch.context_mask_batch bytes (32KB) of old\n   output are maskable, so the cached prefix holds between masks.\n3. dops_reference and note are never masked. A mask resets get_file's\n   scroll-back dedup.\n4. note(text): the agent records why a build failed. Calls are never\n   masked, and the retry hand-over carries the notes to the next attempt.\n5. No LLM summarization until a measured need.\n\nMEASURE: observations_masked (count, bytes, turn), repeat_after_mask (a\nread repeated after its output was masked), turns per attempt, cost,\nrebuild_ok rate.\nREVIEWED 2026-09-16 (still uncommitted, on top of 1294712)\n\n• Placeholders said \"Repeat the call\" for every tool. Repeating dsynth_build rebuilds; repeating edit_file applies the edit twice. Only reads say it now; dsynth_build points at dsynth_log, edit_file/put_file at get_file.\n• Results under 512 bytes are not masked. On py-onnx they were 46 of 160 results and 4% of the bytes: validate_dops diagnostics, refused writes, edit confirmations. Masking them saved 2%.\n• An empty note is refused. The hand-over carries at most 20 notes, 500 chars each. runner.log shows the note text.\n\nREPLAY (py-onnx attempt 1, stored dump, 3.42 bytes/token)\n  unmanaged                 peak 153k  sent 13.9M  on 9 real misses 971k\n  mask 10 turns/32KB/512B   peak  82k  sent  8.5M  on 9 real misses 597k  + 107k re-billed by 7 masks\n  (without the 512B floor:  peak  81k  sent  8.3M)\n\nNOT DONE: notes stop at the job. A later job's Prior Attempts section (context.py) shows the last 12 trace events and renders a note with no text.","owner":"github@dragonflybsd.org","priority":1,"status":"closed","title":"tool_loop grows its message list without bound; the head/tail elision policy already written in fix_chat is wired to the read-only chat instead","updated_at":"2026-09-16T18:43:11Z"}
{"_type":"issue","close_reason":"dsynth_log is capped by max_bytes (default 32KB), f2f127f.","closed_at":"2026-09-16T18:50:05Z","comment_count":0,"created_at":"2026-09-08T16:00:31Z","created_by":"Fred [bot]","dependencies":[{"created_at":"2026-09-09T17:27:58Z","created_by":"Fred [bot]","depends_on_id":"poly-cg6u","issue_id":"poly-9hjm","metadata":"{}","type":"parent-child"}],"dependency_count":0,"dependent_count":0,"description":"worker.dsynth_log (worker.py:3310) is\n\n    def dsynth_log(env, origin, tail_lines: int = 200, flavor: str = \"\")\n\nand truncates purely by line count:\n\n    if tail_lines > 0 and len(lines) > tail_lines:\n        truncated = True\n        kept = lines[-tail_lines:]\n\nThere is no byte cap, and tail_lines is caller-supplied, so the returned\nsize is unbounded for a log whose lines are long.\n\nWHAT HAPPENED (www/firefox, 2026-09-07, bundle\nwww_firefox-20260907-104857Z)\n\nThe agent called dsynth_log and got 4301 lines back. firefox's failing\nbuild log is mostly cargo verbose output, whose `Running `CARGO=...`` lines\nare multiple KB each. Measured from runner.log:\n\n  12:15:49  [tool:dsynth_log] origin=www/firefox lines=4301 ok\n  A1.T82    in=849,927 (cached=166,385)  billable=684,329\n\nOne tool call cost 684k billable -- 13.7% of that run's entire 5M budget.\n\nThe aftershock was worse than the call. The conversation went from ~166k\nto ~857k and stayed there, so every subsequent cache eviction cost ~860k\ninstead of ~140k. Every dsynth_build evicts (the call outlives the TTL),\nso this was once per build round:\n\n  A1.T98   billable=859,775\n  A1.T105  billable=863,000 (approx)\n\nAccounting for that attempt: 92 turns, 3,112,527 billable, of which 6\ncache-miss turns took 2,298,935 (73.9%). Model output across the whole\nattempt was 41,116 tokens -- 1.55% of spend.\n\nCONTRAST\n\ngrep (worker.py:1213) caps at max_bytes=8192 and says why:\n\n    \"The token cost difference between 'matches + a few context lines'\n     and 'the whole 200KB Makefile.in' is the difference between\n     affordable and not.\"\n\ndsynth_log has the same exposure and no such cap.\n\nCOUNTERFACTUAL\n\nThe next run (2026-09-08) asked for 39-99 line tails. Context stayed at\n~140k, evictions cost ~140k, and the same 5M covered roughly 7x more build\nrounds.","design":"The helper already exists. worker.py:125:\n\n    def _tail(s: str, max_bytes: int = _MAX_STREAM_BYTES) -> tuple[str, bool]\n\nwith _MAX_STREAM_BYTES = 32_768 (worker.py:122). It is used for diff\noutput at worker.py:1197 but not by dsynth_log.\n\nSmallest fix: pass dsynth_log's assembled tail through _tail() before\nreturning, and report byte truncation alongside the existing line\ntruncation so the agent knows it was cut.\n\nWorth considering as well:\n\n- Clamp tail_lines to a sane ceiling rather than trusting the caller.\n- The tool's own docstring could state the cost, since the model chooses\n  the argument. A one-line \"keep this at the default unless you have a\n  reason\" is cheap and would have prevented this instance.\n\nSizing: a 32KB cap is ~8k tokens, comparable to grep's 8192-byte cap at\n2k. Either is far below the 690k this cost.","id":"poly-9hjm","issue_type":"bug","notes":"CONSTRAINT ON THE FIX: the cap must be a raisable DEFAULT, not a hard ceiling.\n\nMeasured the opposite failure on devel/libcxx22 (2026-09-08, bundle\ndevel_libcxx22-20260908-224530Z, patch job ...-554173-patch.job).\n\ncheck-plist failed with 578 enumerated error lines -- 577 `Error: Orphaned:`\nplus 1 `Error: Missing:`. For a plist-error the log IS the fix: it names\nevery discrepancy exactly. The agent called dsynth_log at turn 10 and got\n199 lines back (the 200 default), tail-anchored, so it saw the end of the\norphan list and none of the __cxx03/__algorithm/* bulk at the start.\n\nIt then spent ~40 turns and ~250k billable reconstructing that list from the\nextracted source tree (grep 'cxx03|assertion_handler|\\.imp|cxxabi|tr1|\n__config_site|clang-format', reading libcxx/utils/libcxx/header_info and the\nlibcxxabi dir) -- deriving what the log had already printed in full. Reading\nall 578 lines would have cost roughly 25k.\n\nNote this is NOT a second defect in the tool: dsynth_log already returns\n`truncated: true` and `total_lines`, so the agent was told it had a partial\nview and could have re-asked. That half is agent guidance (the\nerror-plist-mismatch playbook never says to read the whole check-plist\nblock), filed separately.\n\nWhat it does constrain here: if the byte cap this bead asks for is a hard\nceiling, the firefox fix makes the libcxx22 case permanently unreadable --\na large structured error block is exactly the content worth spending tokens\non, and it is not distinguishable by size from 4301 lines of cargo noise.\nThe two cases differ by INTENT, not volume. So:\n\n  - byte cap = default, overridable by an explicit caller argument\n  - keep returning truncated/total_lines so the caller can size a re-read\n  - consider making the truncation head+tail or marker-anchored rather than\n    tail-only, so a truncated read of an error block still shows its start","owner":"github@dragonflybsd.org","priority":1,"status":"closed","title":"dsynth_log caps by caller-supplied line count with no byte cap; one call cost 690k tokens and inflated every later build in that run to ~870k","updated_at":"2026-09-16T18:50:05Z"}
{"_type":"issue","comment_count":0,"created_at":"2026-09-08T15:59:52Z","created_by":"Fred [bot]","dependencies":[{"created_at":"2026-09-09T17:28:12Z","created_by":"Fred [bot]","depends_on_id":"poly-l18e","issue_id":"poly-7ska","metadata":"{}","type":"parent-child"}],"dependency_count":0,"dependent_count":0,"description":"worker.dsynth_build (worker.py:3270) runs `dsynth -S -y test`, which\nforce-rebuilds by design. On a large port that is the whole compile every\ntime, however small the edit.\n\nMEASURED on www/firefox this week (2026-09-06..08), 5 runs:\n\n  build round wall clock    ~45 min   (39:33 - 45:38 across ~20 rounds)\n  token cost per round      ~380k     (the tool call outlives the prompt\n                                       cache, so the turn that reads the\n                                       result re-sends the whole context)\n\nRun 5's winning attempt spent 2 rounds on a hypothesis that a 30-second\nincremental compile would have refuted. Run 4 spent 3 consecutive rounds\n(2h15m) re-testing one inert value.\n\nThe wall-clock cost and the token cost have the same root: the tool call\ntakes minutes, so every round evicts the prompt cache. A build that\nfinished in seconds would stay inside the TTL and cost ~2k instead of\n~380k.\n\ndsynth already supports the substrate:\n\n  test   [ports] - 'build' w/DEVELOPER=yes and pre-deletes pkgs\n  debug  [ports] - like 'test' but leaves mounts intact\n\n`debug` leaves /construction/<origin> and the builder mounts in place, so\nan incremental `gmake` in the existing objdir is possible without dsynth\nre-entering. This was used by hand during the node24 SIGSEGV work to keep\na core file and its tree alive.","design":"SHAPE\n\n  1. dsynth debug once per attempt, leaving the worker mounted.\n  2. A new tool -- build_incremental(origin) -- runs gmake in the existing\n     /construction/<origin>/.build and returns the compiler output.\n  3. dsynth_build (test) stays exactly as it is, and remains the ONLY\n     source of rebuild_ok.\n\n_dsynth_run (worker.py:3182) is already parameterised by subcommand, so\nthe dsynth side is small.\n\nWHY THIS IS NOT A poly-9sw REGRESSION\n\npoly-9sw removed a second tool because dsynth_test and dsynth_build both\ndid a full build -- test measured at 103% of build on a port that\ncompiles, i.e. the same work twice for ~3% more signal. An incremental\ngmake is not a second full build; it is a partial compile of what changed.\n\nWHY THIS MUST NOT BECOME A poly-8ni REGRESSION\n\npoly-8ni: the loop's success oracle (dsynth build) diverged from the\nacceptance gate (dsynth test with DEVELOPER=1), so DEVELOPER/QA defects\npassed the loop and failed verify. An incremental build is an even weaker\noracle than dsynth build was.\n\nThe constraint that keeps this safe: build_incremental must be a\nDIAGNOSTIC, never an oracle. It answers \"does this TU compile now\"; it\nmust not set rebuild_ok, must not resolve a bundle, and the attempt must\nstill end on a dsynth_build (test) that passes. Stale objects can and will\nlie -- that is acceptable for iteration and unacceptable for a verdict.\n\nOPEN QUESTIONS\n\n- Exclusivity: a held debug worker blocks the builder. Needs a lock and a\n  release path, and interacts with the feeder.\n- The stale-compose guard in _dsynth_run (baseline from\n  _MATERIALIZE_STATE) needs an equivalent, or incremental builds will run\n  against un-composed edits.\n- Cleanup if an attempt dies with the worker still mounted.","id":"poly-7ska","issue_type":"feature","owner":"github@dragonflybsd.org","priority":1,"status":"open","title":"Every agent build round costs a full dsynth test (~45 min, ~380k tokens); dsynth debug could make iteration incremental","updated_at":"2026-09-08T15:59:52Z"}
{"_type":"issue","close_reason":"One capability (_common.can_operate, from tracker.public_readonly) now decides composition and permission for both audiences. It reaches both policy levels (bundle_actions/issue_actions and action_allowed/issue_action_allowed) and every mutating handler refuses anonymously at entry with 403 rather than a state or validation error. Read path and machine ingest untouched. 33 tests; UI-1 applies the three tiers per view as it rebuilds them.","closed_at":"2026-09-05T23:50:47Z","comment_count":0,"created_at":"2026-09-05T16:58:14Z","created_by":"Fred [bot]","dependencies":[{"created_at":"2026-09-05T18:58:13Z","created_by":"Fred [bot]","depends_on_id":"poly-0e02","issue_id":"poly-0e02.12","metadata":"{}","type":"parent-child"},{"created_at":"2026-09-05T18:58:21Z","created_by":"Fred [bot]","depends_on_id":"poly-fij.5","issue_id":"poly-0e02.12","metadata":"{}","type":"relates-to"}],"dependency_count":0,"dependent_count":4,"description":"Clarified 2026-09-05: \"read-only\" means ANONYMOUS — unauthenticated\npeople who want to see how the build is going. Not a user account with a\nviewer role. Roughly 10 operators, several dozen anonymous readers.\n\nThis is a DESIGN constraint on every view, to be honoured now, not a\nfeature that waits for authentication to exist. Auth is the mechanism\n(poly-fij.5) and can land later; the page composition it will switch\nbetween has to be designed up front, because retrofitting it means\nwriting every template twice.\n\nONE CAPABILITY, NOT A ROLE SYSTEM\n\nTemplates take a single boolean — can_operate — resolved once per\nrequest. No user table, no role model, no permissions matrix is needed\nto design or build this. Today it is forced True for everyone, so\nbehaviour is unchanged. The day poly-fij.5 lands it is derived from the\nsession and nothing else moves.\n\nFAIL CLOSED\n\nThe default is anonymous. Authentication GRANTS capability; it never\nrestricts it. A missing or unrecognised session must render the public\npage, not the operator one. Written this way, the tracker becoming\npublicly reachable before auth lands is a configuration decision rather\nthan an accident.\n\nTHREE TIERS, NOT TWO\n\nNaming them now because page composition depends on it:\n\n1. Public status — anonymous. Active builds, history, per-run results,\n   port detail, compare, target diff, the run view, pipeline inventory\n   counts, and which issues exist and what state they are in. This is\n   the \"how is the build going\" surface and it is the default landing.\n2. Operator actions — every mutating control: accept, reject, discard,\n   retry, take-over, release, reopen, deliver, mute, resolve,\n   cancel-build, verify, env selection, manual context submission.\n3. Agent internals — the session viewer, fix-chat, tool trace and token\n   accounting. Not mutating, but agent working material rather than\n   build status. Decide per surface whether anonymous sees it; the\n   default should be no.\n\nWHAT THIS MEANS FOR THE TEMPLATES\n\nThe action surface must be a SEPARABLE REGION in every view — one\nomittable block — not controls interleaved through status panels. If it\nis interleaved it cannot be omitted later without a rewrite. This is the\nwhole reason the bead lands before UI-1.\n\nConcrete consequences already visible in the mock:\n\n- UI-6's four tabs are audience-dependent. Fix and Evidence are public;\n  Chat and History (sessions, tool trace, token cost) are tier 3. An\n  anonymous cockpit has two tabs, not four.\n- UI-5's queue shows bands and state to anyone; the per-row action and\n  take-over are tier 2.\n- The shell's secondary nav differs: Manual and Runner are operator\n  destinations.\n- UI-2 has no mutating actions at all, so Builds is identical for both\n  audiences. That is a reason / serves Builds, not just a kit\n  preference.\n\nBOTH LEVELS, STILL\n\nThe policy is two-level by design: ACTION_ALLOWED is the authoritative\nendpoint gate, bundle_actions() / issue_actions() are the deliberately\nnarrower UI surface. can_operate has to reach both. A hidden button in\nfront of a live POST endpoint is a hidden button, not read-only.\n\nNothing in fix_state.py or issue_state.py has any notion of an actor\ntoday; ACTION_ALLOWED keys purely on resolution and verification_status.\nThere is no Depends, Authorization or session anywhere in the tracker.\n","id":"poly-0e02.12","issue_type":"task","labels":["tracker-ui"],"notes":"IMPLEMENTED 2026-09-06.\n\nBEYOND THE BEAD: the capability is resolved from a real setting,\ntracker.public_readonly (bool, default False), not merely forced True.\nThe bead said 'forced True today', which would have made the anonymous\ncomposition unreachable outside tests. Given the tracker has no auth and\nseveral dozen readers are expected, an instance-wide public mode is the\nthing that makes this usable now rather than after poly-fij.5.\n\ncan_operate() takes NO request argument yet, deliberately: there is\nnothing to read one from. That is the documented seam -- when a session\nexists it grows a parameter and its callers pass one. Fails closed by\nconstruction: auth will GRANT, never restrict.\n\nREFINEMENT: threading can_operate into action_allowed / issue_action_\nallowed is necessary but NOT sufficient. It refuses correctly but reports\nthe wrong reason -- an anonymous accept came back 409 'cannot accept in\nstate agent_fixed', and eight endpoints that validate their body first\ncame back 400 'body context is required', leaking the schema. So\nforbid_anonymous() also runs as the FIRST statement of every mutating\nhandler. 15 of 16 now answer 403; the policy functions keep the check so\na future caller that forgets the guard is still safe.\n\nKNOWN GAP: POST /api/manual-requests/{run}/{origin}/context answers 422,\nnot 403, because FastAPI validates its Pydantic model before the handler\nbody runs. The action is refused either way. Fixing it needs Depends\nthreaded through the optional-import shim, which is not proportionate for\none route with no anonymous-reachable link.\n\nSCOPE BOUNDARY THE BEAD DID NOT STATE: machine ingest is deliberately NOT\ngated. /v1/* and /api/builds* belong to the farm's hooks, not to an\noperator, and gating them would mean turning on public_readonly stops the\nbuild recording. Runner/builder authentication is poly-fij.4's, not this.\n\nTEMPLATES: guarded the existing shell where the audience already changes\ncomposition -- the bundle page's operator panel (it already keyed on\noperator_actions.show, so passing the capability was enough), the\nfix-chat panel, the issue controls, and the Runner/Manual subnav entries.\nThe full audit across all 37 templates belongs to UI-1, which rebuilds\nthem; this bead establishes the mechanism and the three tiers it applies.\n\ntests/test_tracker_audience.py: 33 tests over the policy layer, every\nmutating endpoint, the read path, machine ingest, tier 3, and page\ncomposition.","owner":"github@dragonflybsd.org","priority":1,"status":"closed","title":"Design every view for two audiences: anonymous and operator","updated_at":"2026-09-05T23:50:47Z"}
{"_type":"issue","assignee":"Fred [bot]","close_reason":"Rendered artifacts now come from a byte-bounded LRU keyed on (sha256, relpath, kind) -- 57 ms cold, 0.0 ms warm on a 3 MB log -- with only the derived half cached so one deduplicated blob serves every bundle pointing at it. The worklist's inline reconcile loop became one throttled sweep (tracker.delivery_sweep_seconds, default 60), so the page stops opening a SQLite connection per open delivery on every render. 9 tests.","closed_at":"2026-09-06T00:19:52Z","comment_count":0,"created_at":"2026-09-05T16:19:01Z","created_by":"Fred [bot]","dependencies":[{"created_at":"2026-09-05T18:19:01Z","created_by":"Fred [bot]","depends_on_id":"poly-0e02","issue_id":"poly-0e02.11","metadata":"{}","type":"parent-child"},{"created_at":"2026-09-05T18:19:18Z","created_by":"Fred [bot]","depends_on_id":"poly-fij.9","issue_id":"poly-0e02.11","metadata":"{}","type":"relates-to"}],"dependency_count":0,"dependent_count":2,"description":"Two per-render costs the redesign multiplies. Both are cheap to fix and\nneither needs an architecture change (see the process-model bead under\npoly-fij for that).\n\nARTIFACT RENDERS ARE UNCACHED\n\nrender/artifacts.py decompresses up to MAX_DECOMPRESSED_BYTES (4 MiB)\nand UTF-8 decodes it on every request. No cache anywhere. The cockpit\ngives evidence its own tab and switches between artifacts in place, so\nthe same log gets decompressed repeatedly in one sitting.\n\nBlobs are content-addressed: artifact_refs carries sha256 and put_blob\nalready computes it. That is a free, perfect cache key — the bytes for\na sha can never change. A bounded LRU keyed on (sha256, render mode)\nremoves the repeat work with no invalidation problem.\n\nTHE QUEUE RENDER DOES DELIVERY RECONCILIATION\n\n/agentic loops open_delivery_bundle_ids() and calls\ndelivery_sync.reconcile_bundle_delivery() per bundle, inside the\nrequest, before reading issues. Each call opens its own connection\n(\"self-contained connection management so both page routes can call\nit\").\n\nIt is throttled at DEFAULT_MIN_INTERVAL_S = 300, so it does not hammer\nGitHub. The cost is per-render connection setup and latency on the one\nendpoint UI-5 wants to poll live. A queue that refreshes every few\nseconds should not carry a reconciliation loop.\n\nMove it: a background sweep, or a separate endpoint the page calls at a\nslower cadence than the queue itself.\n\nALREADY RIGHT, KEEP IT\n\nagentic_bundle_artifact_fragment renders only the detail pane from the\nsame _artifact_detail.html partial the full page uses — one renderer,\nno drift, no full-page re-render on artifact switch. The redesign\nshould extend that pattern, not replace it.\n","id":"poly-0e02.11","issue_type":"task","labels":["tracker-ui"],"notes":"IMPLEMENTED 2026-09-06.\n\nARTIFACT CACHE. Byte-bounded LRU in render/artifacts.py keyed on\n(sha256, relpath, kind), guarded by a lock because the tracker serves on\na threadpool. Measured on a 3 MB gzipped log: 57 ms cold, 0.0 ms warm.\n\nThree details the bead did not settle:\n• The key is (sha256, relpath, kind), not (sha256, render mode). The same\n  bytes render differently under a different name -- a diff is not a log\n  -- so the name is part of the identity, and 'render mode' is derived\n  from it rather than independent of it.\n• Only the DERIVED half is cached. bundle_id, relpath, ref and size come\n  from the request: blobs are deduplicated, so one log hangs off several\n  bundles and the cached render has to serve all of them. Pinned by a test.\n• Bounded by BYTES, not entries. A 2 KiB diff and a 4 MiB log are both one\n  entry, so a count-based bound means nothing. tracker.artifact_cache_bytes\n  (default 64 MiB, 0 disables).\n\nA ref with no sha256 -- the fs backend -- is deliberately not cached: the\npath's contents can change underneath, so no key stays true. Renders that\nerrored are not cached either; the cause is usually the file rather than\nthe bytes, and a transient read failure must not stick to the sha.\n\nDELIVERY SWEEP. Chose the process-wide throttle over the bead's other\noption (a separate endpoint the page calls). An endpoint would have meant\nreconciliation stops happening until the new UI is written to call it --\na functional regression in the interim, for a view that does not exist\nyet. delivery_sync.reconcile_open_deliveries() now does the whole sweep\nin one call, throttled by tracker.delivery_sweep_seconds (default 60),\nand the worklist calls it once instead of looping. Per-bundle rate\nlimiting is unchanged, so GitHub is asked no more or less often; what\nchanged is that the render stops opening one SQLite connection per open\ndelivery every time anyone loads the page.\n\nAlso folded the per-candidate target lookup into the sweep's own query --\nthe old loop did a SELECT per bundle to find it.\n\nCONSEQUENCE WORTH KNOWING: a merge that lands just after a sweep is\ninvisible for up to the sweep interval. That is the trade, and it is why\nthe interval is a setting. reset_sweep_throttle() exists because the\nthrottle is process-global state that tests asserting reconcile-on-render\nhave to control -- test_tracker_agentic_views needed it.\n\n9 tests in tests/test_render_cache_and_sweep.py.","owner":"github@dragonflybsd.org","priority":1,"started_at":"2026-09-06T00:12:40Z","status":"closed","title":"Cache artifact renders by sha256, and take delivery reconciliation off the queue render","updated_at":"2026-09-06T00:19:52Z"}
{"_type":"issue","assignee":"Fred [bot]","close_reason":"Three-state token system (bare :root light, prefers-color-scheme guarded, [data-theme] override) ported from the mock, with the old token names kept as aliases so all 354 existing rules follow the theme without a rewrite. 152 stylesheet literals and 14 template ones tokenised; 4+5 undefined names defined; body moved from 12px monospace to 15px sans with monospace kept for data; the 12px floor applied everywhere; four accents shifted a shade so everything clears AA on both grounds. Shell is Builds | Pipeline | Repairs with a theme toggle. 15 guard tests.","closed_at":"2026-09-06T00:49:32Z","comment_count":0,"created_at":"2026-09-05T15:18:12Z","created_by":"Fred [bot]","dependencies":[{"created_at":"2026-09-05T17:18:11Z","created_by":"Fred [bot]","depends_on_id":"poly-0e02","issue_id":"poly-svnn","metadata":"{}","type":"parent-child"},{"created_at":"2026-09-05T19:01:31Z","created_by":"Fred [bot]","depends_on_id":"poly-0e02.12","issue_id":"poly-svnn","metadata":"{}","type":"blocks"}],"dependency_count":1,"dependent_count":6,"description":"The shared shell everything else sits on. Blocks the other view work.\n\n- _base.html shell, Builds|Pipeline|Repairs primary nav, per-view secondary\n  nav, breadcrumbs.\n- Design tokens as a three-state set (light default, prefers-color-scheme\n  dark, [data-theme] override) plus the toggle control.\n- Reusable primitives: status pill, data table, panel/content-head, action\n  buttons, empty state, live indicator.\n- Replace progress.css. Verify afterwards that no colour is defined only\n  inside a media or [data-theme] block and that every token used is defined.\n\nTypography floor from the proposal: 15px body, 14px operational tables,\n13px metadata, nothing essential under 12px. Monospace for origins, IDs,\nmetrics and evidence only — not for prose.\n","id":"poly-svnn","issue_type":"task","labels":["tracker-ui"],"notes":"IMPLEMENTED 2026-09-06.\n\nCORRECTS THE EPIC'S DIAGNOSIS. The epic said progress.css had '63 uses of\n6 CSS variables that are never defined'. Measured: the stylesheet had FOUR\nundefined names in 16 uses (--panel-bg, --mono, --text-faint, --c-link).\nThe rest were in TEMPLATE inline styles -- --c-fail, --c-info-bg,\n--c-success-bg, --c-fail-bg, --c-ok, 18 uses, each with a hardcoded\nfallback from the old palette, which is exactly why they ignored the\ntheme. All defined now.\n\nAPPROACH: aliases, not a rewrite. The old token names (--bg-card, --border,\n--c-built ...) are redefined in terms of the new ones rather than being\nremoved, so all 354 existing rules follow the theme today without\nrewriting each. Each view bead can retire its own aliases as it restyles.\n\nTokenised 152 colour literals in the stylesheet and 14 in template inline\nstyles, by hue, lightness and the property they sit on. Two iterations\nwere needed before applying: HLS saturation inflates for pale colours, so\nwarm and cool greys first came out as amber and cyan -- chroma\n(max-min) is the right measure, with a lower floor above 0.90 lightness\nbecause a genuine tint carries little chroma either. A dark background\nalso needed its own rule or it inverted to a light one.\n\nCONTRAST, MEASURED NOT ASSUMED. Three light accents and one dark sat just\nunder AA against the grounds they actually sit on: --cyan 4.48, --amber\n4.49, --faint 3.84, dark --faint 4.48. All four shifted a shade, same\nhues. Everything clears 4.5 on both the page ground and the panel surface\nnow.\n\nTYPOGRAPHY. body was 12px monospace for the whole document. Now 15px sans\nwith monospace kept for the data surfaces. 34 sub-12px rules in the\nstylesheet, 33 in inline styles and 22 in template <style> blocks raised\nto the floor; measured 0 elements below 12px across 20 page/theme renders,\ndown from 36 on the run view alone.\n\nNAV RENAME. Builds | Pipeline | Repairs. Pipeline has no overview yet\n(UI-4 owns /pipeline), so it lands on Jobs -- one of its own secondary\ndestinations -- rather than a dead entry.\ntest_view_nav_includes_agentic asserted '>Agentic<' and is replaced by\ntest_view_nav_offers_the_three_primary_views; the epic flagged this as a\ndeliberate change.\n\nVERIFIED: 20 page/theme combinations rendered headless. Every page paints\nits own ground (light rgb(233,237,242) / dark rgb(9,13,18)), zero\nhorizontal overflow, zero text below 12px, worst contrast 4.48 -> all\nclear after the accent shift. tests/test_design_tokens.py pins all of it,\nincluding the two failure modes that had actually happened.","owner":"github@dragonflybsd.org","priority":1,"started_at":"2026-09-06T00:24:28Z","status":"closed","title":"UI-1 Shared shell, design tokens and primitives","updated_at":"2026-09-06T00:49:32Z"}
{"_type":"issue","comment_count":0,"created_at":"2026-09-04T20:28:16Z","created_by":"Fred [bot]","dependencies":[{"created_at":"2026-09-04T22:28:21Z","created_by":"Fred [bot]","depends_on_id":"poly-5e1","issue_id":"poly-lvw","metadata":"{}","type":"related"},{"created_at":"2026-09-04T22:28:22Z","created_by":"Fred [bot]","depends_on_id":"poly-7eh","issue_id":"poly-lvw","metadata":"{}","type":"related"},{"created_at":"2026-09-09T17:27:56Z","created_by":"Fred [bot]","depends_on_id":"poly-cg6u","issue_id":"poly-lvw","metadata":"{}","type":"parent-child"}],"dependency_count":0,"dependent_count":0,"description":"The patch loop's turn cap is 30, a hardcoded default on\npatch.py:19 (`max_tool_turns: int = 30`). It is not in settings.py, so\nit cannot be changed without editing code, and `dportsv3 config show`\ndoes not mention it. steps.py:842 records it as deliberate -- when\n_lift_budget_if_free drops the token budget, \"max_tool_turns (30, from\npatch.py) and tier.max_iterations still bound the loop\". So it is a\nreal backstop, not an oversight. What follows is an argument that 30 is\nthe wrong number, not that the bound should go.\n\nMEASURED, comms/hamlib 2026-09-04, ASSIST at 600k:\n\n  A  turns  billable  cache%   stop\n  1     30    80,797   95.5%   turn cap\n  2     30    67,348   97.2%   turn cap\n  3     30   215,730   90.9%   turn cap\n  4     27    87,863   95.3%   text_only -> rebuild_ok\n\nFour attempts, 451,738 billable of a 600,000 budget. Every attempt that\ndid not finish stopped on the TURN cap; none stopped on tokens, and\n~150k of budget went unspent. The port was fixed, so this is not a hard\nport hitting a sensible wall -- it is a solvable port being chopped\nmid-step four times.\n\nThe closing line of each attempt shows where the cut landed:\n\n  A1 \"now deciding between disabling USB support or patching the test\n      utility\"\n  A2 \"drops out cleanly when USB is disabled -- but I need to check\n      what that costs the core library\"\n  A3 \"a one-line guard suffices -- staging the dragonfly patch\"\n  A4 \"The patch applies cleanly -- prior attempt likely just ran out of\n      turns before rebuilding. Time to build.\"   -> rebuild_ok\n\nOne continuous line of work, severed three times at 30 turns. A4 needed\n27 turns only because A1-A3 had already done the investigation.\n\nTHE SECOND, INDEPENDENT REASON. Each attempt is an unavoidable cold\nstart: poly-2w6 shipped prompt_cache_key and it hits roughly 1 retry in\n3, so an attempt boundary costs ~33k billable two times out of three.\nThis run paid 98,597 in cold starts plus an 83,111 mid-attempt eviction\n-- 40% of its billable -- largely because it needed four attempts. Every\nattempt boundary removed is a ~33k dice roll removed.\n\nSo the turn cap and the cache interact: a low turn cap forces more\nattempts, and more attempts cost more cold starts. Raising the cap\nattacks both.\n\nNOT AN ARGUMENT FOR CUTTING max_iterations. That was proposed and\nretracted on poly-5e1 -- hamlib needed all four attempts and the fix\nlanded on the fourth. Fewer attempts is good only if the work finishes\ninside them, which is what raising the turn cap is for.\n\nWHAT TO DO\n  1. Make it a setting (runner.max_tool_turns or similar) rather than a\n     patch.py default, so it can be tuned without a deploy and shows up\n     in `config show`. Every other bound in this loop is already a\n     setting; this one is invisible.\n  2. Raise the default. 30 was not chosen against this evidence.\n     Candidate 50-60: A1-A3 each had budget left and were mid-step.\n  3. Re-measure hamlib and one other multi-attempt port. The signal to\n     watch is attempts-to-fix and total billable, not turns.\n\nCAUTION. The budget gate is checked BETWEEN turns and a single turn can\novershoot it (observed +48% on a 120k tier), so a higher turn cap\nraises the worst-case overshoot too. poly-7eh (tool results accumulate\nuntruncated) is the thing that makes late turns expensive, so these\nshould be looked at together -- raising the cap without bounding\nper-turn growth moves the failure from \"chopped early\" to \"one huge\nturn\".\n","design":"DESIGN, settled after reading the code 2026-09-04.\n\nFLAT SETTING, NOT PER-TIER. The obvious-looking home is policy.tiers,\nbeside max_iterations and max_tokens -- it is the same kind of bound.\nDo not. policy.tiers is read as one whole table (_from_tables uses\nspec.get(key, 0)), and the live config on the builder overrides it\nwholesale:\n\n  [policy.tiers.AUTO]    max_iterations=2  max_tokens=30000\n  [policy.tiers.ASSIST]  max_iterations=4  max_tokens=600000\n  [policy.tiers.MANUAL]\n  [policy.tiers.CONVERT] max_iterations=2  max_tokens=150000\n\nNone of those carry a turn field, so a per-tier max_turns would resolve\nto 0 on the deployed box the moment it shipped -- an attempt with no\nturns at all. That is exactly the trap CLAUDE.md warns about (\"read as\none whole table -- a partial override silently discards the rest\").\nA flat runner.max_tool_turns has no such failure mode. Per-tier is a\nlater change with a migration, not this one.\n\nTHE DEFAULT: 50. Not a round guess -- match it to the budget the tier\nalready grants. A1 spent 80,797 billable over 30 turns, ~2.7k/turn.\nASSIST's 600k over max_iterations=4 is ~150k per attempt, which at that\nrate is ~55 turns. 30 turns can only ever spend ~80k of a 150k\nallowance, which is why 150k went unspent on a run that hit the turn\ncap three times. 50 lets an attempt actually reach the budget it was\ngiven; the budget then bounds the spend, as it was always meant to.\n\nWHY THIS DOES NOT UNBOUND ANYTHING. The token budget still binds and\nstill fires first when it is the real constraint -- tool_loop stops on\ntoken_budget independently of max_turns. Raising the turn cap does not\nraise the spend ceiling; it stops the loop from ending BELOW that\nceiling. The three bounds remain: max_tokens, max_iterations, and this.\n\nTHREADING. patch.py:19 is the only production value; attempt_loop.run\n(12) and tool_loop.run (12) are library defaults for direct callers and\nshould stay. Resolve in patch.py via a None sentinel so an explicit\nargument still wins -- the manual harnesses and tests pass their own.\n\nTWO STALE COMMENTS TO FIX with the change, both of which name the\nliteral 30 and would start lying:\n  - settings.py, llm.patch.free_tier: \"the patch harness's 30 tool turns\"\n  - steps.py:842, _lift_budget_if_free: \"max_tool_turns (30, from patch.py)\"","id":"poly-lvw","issue_type":"bug","notes":"SHARPER EVIDENCE THAN THE DESCRIPTION USES.\n\nTurns per attempt, comms/hamlib, the same port run either side of an\nunrelated change (poly-5e1's retry handoff) that demonstrably altered\nthe agent's behaviour:\n\n  OLD:  A1 30   A2 30   A3 30   A4 30    -> patch_gave_up\n  NEW:  A1 30   A2 30   A3 30   A4 27    -> agent_fixed\n\nSeven of eight attempts consumed exactly 30. The eighth used 27 because\nit FINISHED -- stop reason text_only with rebuild_ok -- not because it\nran out of work.\n\nThat is the argument in one table. Change what the agent knows, change\nwhat it spends its turns on, change the outcome from gave-up to fixed,\nand the turn count does not move. A model with turns remaining uses\nthem, so 30 is not a safety margin the agent rarely reaches; it is the\nthing that decides when nearly every attempt ends.\n\nCONSEQUENCE: the turn cap is a hard ceiling on progress per attempt,\nindependent of the token budget (~150k unspent here) and independent of\nany improvement to the prompt. No change to what the agent is told can\nraise it, because the saved turns are simply reallocated rather than\nbanked -- measured on poly-5e1, where a handoff that removed a large\namount of re-derivation produced no reduction in turns at all.\n\nThis is a cleaner argument than the \"chopped mid-step\" framing in the\ndescription, which it supersedes. The mid-step evidence is still true\nand worth reading -- it shows WHERE the cut lands -- but it invites the\nreply \"then the attempts should be smarter\", and this table forecloses\nthat: they were smarter, and it changed nothing about when they stopped.\nIMPLEMENTED 2026-09-04, not yet deployed or measured.\n\n  settings.py   runner.max_tool_turns, int, default 50\n  patch.py      max_tool_turns: int | None = None, resolved from the\n                setting when None so an explicit argument still wins,\n                and read per call rather than as a default argument\n                (settings are cached per process; a default would\n                freeze whatever loaded first)\n  steps.py      _lift_budget_if_free's docstring no longer names 30\n  settings.py   llm.patch.free_tier's note no longer names 30, and its\n                wall-clock arithmetic now scales with the setting\n  sample        regenerated\n\nLeft alone on purpose: attempt_loop.run (12) and tool_loop.run (12) are\nlibrary defaults for direct callers, not production values -- patch.py\nis the only production path and always passes.\n\ntests/test_turn_cap_setting.py, 9 tests. tests/test_free_tier_budget.py's\nturn test rewrote from `signature(patch.run).default == 30` to the\nbehaviour it meant: patch.run resolves the setting, passes it down, and\nit exceeds attempt_loop's own 12. Pinning the literal is what made it\nfail on a value change that was the point of the change.\n\nMutation-checked: never reading the setting (4 fail), reverting the\ndefault to 30 (1 fail), ignoring an explicit argument (2 fail). Full\nsuite 3126 passed.\n\nWHAT THIS DOES NOT ESTABLISH. The evidence proves 30 was BINDING -- it\nended seven of eight attempts -- not that 50 produces better outcomes.\n50 is derived from the budget arithmetic, not measured. Step 3 of WHAT\nTO DO still stands and is now the whole remaining task: re-run hamlib\nand one other multi-attempt port, and watch attempts-to-fix and total\nbillable. Two outcomes would argue for reverting rather than tuning:\nattempts that now run 50 turns without finishing any sooner, or a\nworse budget overshoot per turn (see the CAUTION and poly-7eh).\n\nOPERATIONAL NOTE FOR WHOEVER DEPLOYS. Jobs get longer in wall-clock in\nproportion: at the measured 10-30s per turn, an attempt moves from\n5-15 minutes to 8-25, and four attempts from ~1h to ~1h40 worst case.\nThe dsynth gate pauses the runner during builds, so this reduces\nthroughput per port even where it improves the outcome. Worth watching\nalongside the token numbers.","owner":"github@dragonflybsd.org","priority":1,"status":"open","title":"the patch turn cap is a hardcoded 30 and it, not the budget, is what ends attempts","updated_at":"2026-09-04T21:21:33Z"}
{"_type":"issue","comment_count":0,"created_at":"2026-09-04T11:41:17Z","created_by":"Fred [bot]","dependencies":[{"created_at":"2026-09-04T13:41:16Z","created_by":"Fred [bot]","depends_on_id":"poly-lt1","issue_id":"poly-86t","metadata":"{}","type":"related"},{"created_at":"2026-09-09T17:28:15Z","created_by":"Fred [bot]","depends_on_id":"poly-udzu","issue_id":"poly-86t","metadata":"{}","type":"parent-child"}],"dependency_count":0,"dependent_count":0,"description":"Accept makes a decision and performs a side effect in one action, commits\nthe decision first, and offers no way to re-run the side effect when it\nfails. The bundle is left accurate but unactionable.\n\nMEASURED 2026-09-04: 17 bundle_review_requests rows sit at create_failed,\n14 of them from the poly-lt1 clone race. Every one names a bundle whose\nresolution is already 'accepted' and which has no PR. Recovering each one\nmeans reopening a decision that was never wrong.\n\nORDER OF OPERATIONS\n\napi_bundle_accept:\n\n  1. UPDATE ... resolution = 'accepted', accepted_at = ?    (committed)\n  2. _accept_delivery_step(...)  -> opens the PR\n\nStep 2 failing does not roll back step 1 -- deliberately, since the\ncreate_failed row is the record of what happened. But then\nfix_state.py:95 refuses a second attempt:\n\n    \"accept\": lambda r, v: r not in TERMINAL_RESOLUTIONS and v == VERIFIED\n\nand RESOLUTION_ACCEPTED is in TERMINAL_RESOLUTIONS. The comment above\nthat set states the consequence plainly: \"Terminal decisions -- nothing\nacts on these except reopen.\"\n\nWHY REOPEN IS THE WRONG INSTRUMENT\n\nThe operator's decision was correct and is correctly recorded. Only the\nside effect failed. Reopen retracts the decision to re-trigger the side\neffect, which:\n\n  - loses accepted_at and the bundle_accepted event's meaning\n  - re-runs the full accept path, including the same clone acquisition\n    that failed, so batching reopens reproduces poly-lt1 immediately\n  - reads, in lineage views, as though the fix was un-accepted and\n    re-accepted, when nothing about the judgement changed\n\nTHE MODEL ALREADY SEPARATES THESE\n\n  - _accept_delivery_step is already a standalone function, not inlined\n    in the endpoint.\n  - bundle_review_requests carries its own status lifecycle\n    (create_failed / merged) independent of bundles.resolution.\n\nSo the schema already treats the decision and the delivery as two\nthings. Only the action layer conflates them.\n\nPROPOSED\n\nA 'deliver' action (endpoint + button) that:\n\n  - is allowed when resolution == 'accepted' AND the newest\n    bundle_review_requests row for the bundle is create_failed\n  - re-runs _accept_delivery_step and writes a fresh review-request row\n  - changes no resolution and emits no bundle_accepted event\n\nIt cannot corrupt state the way reopen->accept can, because it never\ntouches the decision. It also turns the current 14-bundle backlog into\none action each rather than a reopen/accept round trip that re-races the\nclone every time.\n\nRELATED, NOT THE SAME\n\npoly-lt1 is why delivery fails (a shared clone with no mutex at the\nrequest layer). This is why an operator cannot recover once it has.\nFixing poly-lt1 lowers the frequency; it does not provide the exit.\nThree of the 17 create_failed rows are NOT the race -- GitApplyConflict,\nGitCommitError \"nothing to commit\", GitWrongBranch -- and those need a\nregenerated fix rather than a retry, so the action should surface the\nerror rather than retry blindly.\n","id":"poly-86t","issue_type":"bug","labels":["delivery","tracker","ux"],"notes":"REVISITED against the code before implementing. The diagnosis holds; the\nshape of the fix is constrained by three things worth recording.\n\nVERIFIED 2026-09-04. All 17 create_failed rows are stranded by the strict\ndefinition — the newest review-request row for the bundle is create_failed\nAND bundles.resolution = 'accepted':\n\n    DeliveryError: another delivery is using the clone   14\n    GitApplyConflict: git apply --3way produced ...       1\n    GitCommitError: nothing to commit ...                 1\n    GitWrongBranch: clone is on branch 'agentic/...'      1\n\n1. THE GATE HAS TO SPLIT ACROSS TWO PLACES.\n\nACTION_ALLOWED entries are `(resolution, verification_status) -> bool`\n(fix_state.py:93-104). Delivery status is not in that signature and must\nnot be forced into it — the module's own contract says the table\n\"captures ONLY the state gate each endpoint checks today; the endpoint\nkeeps its metadata/concurrency guards and HTTP shaping.\"\n\nSo: `\"deliver\": lambda r, v: r == RESOLUTION_ACCEPTED` in the table, and\nthe newest-row-is-create_failed check in the endpoint, returning 409.\n\n2. THE UI FLAG DOES NOT BELONG IN bundle_actions().\n\nbundle_actions() is documented pure over resolution / verification_status\n/ target / origin with no DB access; live reads are merged by the caller\n(the verify env-picker precedent, pages.py:325-340). But nothing new is\nneeded: pages.py:299 already loads `delivery_request` via\nlatest_review_request_for_bundle for the Delivery card, and\n_bundle_delivery.html already branches on `dr.status`. The button is one\nmore branch in a template that already has the data.\n\n3. THE ERROR IS ALREADY SURFACED.\n\n_bundle_delivery.html renders `dr.error` when present, so \"surface rather\nthan retry blindly\" needs no new plumbing — the operator can already see\nthat a row failed on GitApplyConflict rather than the clone race. Do NOT\nstring-match the error to block a retry: three of the seventeen are not\nthe race, but a retry of those is cheap, writes a fresh row with the same\nerror, and is more honest than a gate guessing which errors are\nretryable.\n\n4. THE RETRY MUST NOT PRETEND TO BE AN ACCEPT.\n\nAccept emits `bundle_accepted` + a bundle_accepted activity row and calls\nmark_issue_resolving. The retry path must do none of that — the decision\nalready happened and the issue is already resolving. It should reuse only\n_accept_delivery_step, then write the same `delivery_complete` activity\nrow accept writes (bundle_actions.py:421-434) so the ribbon reads as one\ncontinuous story, plus its own event so the retry is distinguishable from\nthe original attempt.\n\n5. `skipped` IS A REAL OUTCOME OF THE RETRY.\n\n_accept_delivery_step returns status='skipped' with a skip_reason for\nno_config / no_changes_diff / changes_diff_empty / changes_diff_unreadable\n/ operator_optout. A retry can legitimately land there (e.g. delivery was\nswitched off since the accept). Report it as-is rather than treating it\nas success or failure.","owner":"github@dragonflybsd.org","priority":1,"status":"open","title":"a failed delivery strands an accepted bundle: no PR, and accept refuses to run again","updated_at":"2026-09-04T14:23:08Z"}
{"_type":"issue","comment_count":0,"created_at":"2026-09-04T07:49:36Z","created_by":"Fred [bot]","dependencies":[{"created_at":"2026-09-09T17:28:12Z","created_by":"Fred [bot]","depends_on_id":"poly-l18e","issue_id":"poly-bxo","metadata":"{}","type":"parent-child"}],"dependency_count":0,"dependent_count":0,"description":"The agent's own build reaches any port in the tree, including every one\nexcluded from the loop, and nothing bounds how long it takes.\n\nMEASURED, devel/hyprutils, 2026-09-04.\n\nThe patch agent investigated the compiler setup -- grep\n'USE_GCC|PORT_COMPILER|df.gcc', then get_file on bsd.df.gcc.mk.diff and\nbsd.default-versions.mk.diff -- wrote a patch, and called dsynth_build at\n06:39:26. Ninety minutes later the job had logged nothing further and the\nhost was running:\n\n    dsynth -S -y -p 2026Q3 test devel/hyprutils\n    dsynth: [00] WORKER package  lang/gcc13\n\nlang/gcc13 is on the feeder's denylist, holds an open origin_skip_flag,\nand had already been refused a direct build. It is being built anyway,\nas a dependency the agent's own patch introduced.\n\nMECHANISM\n\nworker.py:3252, dsynth_build ->\n\n    dsynth -S -y -p \"$DPORTS_DSYNTH_PROFILE\" test \"$1\"\n\ndsynth re-resolves the dependency tree from the Makefile as the agent\njust edited it. A patch that adds USE_GCC makes a compiler a build\ndependency, and dsynth builds it. The tool takes (env, origin) and\nconsults nothing else.\n\nWHY THE EXISTING EXCLUSIONS DO NOT COVER IT\n\n  - The feeder previews with `dsynth status <origin>` and refuses a big\n    dependency set. That preview reads the port as found on disk, BEFORE\n    the agent edits it. The set that actually gets built is the post-edit\n    one, which does not exist at preview time. The guard cannot see this\n    by construction.\n  - origin_skip_flags gate the runner picking up a JOB for an origin\n    (confirmed working: devel/llvm19 retired dead, origin_locked). They\n    say nothing about dsynth building that origin as somebody else's\n    dependency.\n  - The denylist regex filters only the feeder's own dtest calls.\n\nNO UPPER BOUND\n\n_exec declares `timeout: int | None = None` (worker.py:1302) and\ndsynth_build calls it without one, so subprocess.run gets timeout=None.\nThere is no ceiling other than how long the largest dependency takes.\nllm.patch.timeout (600) bounds the LLM call, not the build.\n\nBLAST RADIUS\n\nThe runner pauses while any dsynth is on the host. So one agent's build\nstalls every other job on the box -- other patch jobs, and verify jobs an\noperator queues meanwhile. The batch feeder blocks behind its drain. A\nsingle agent decision can idle the whole pipeline for hours.\n\nTHE FIX IS NOT FLAGS\n\ndsynth -x / -xx (\"do not rebuild packages with dependencies which require\nrebuilding\") would bound it, but poly-8ni established that the loop must\nbe proved by the gate it is judged by: dsynth test, the same command\nverify-fix runs. Weakening the agent's build below the acceptance gate\nreintroduces exactly what poly-8ni fixed.\n\nCandidates that keep the gate intact:\n\n  1. Preview before building. Run `dsynth status <origin>` on the EDITED\n     port inside dsynth_build, and refuse with a blocked_by reason when\n     the plan exceeds a package cap or names an excluded origin. The\n     agent already handles blocked_by (stale_compose does this today),\n     so a refusal is a turn it can act on rather than a crash.\n  2. Give dsynth_build a timeout, so an unbounded build cannot idle the\n     host indefinitely. Needs to be generous -- a legitimate large port\n     is slow -- but a ceiling is better than none.\n  3. Consider whether a patch that introduces a NEW build dependency is\n     a fix the loop should accept at all. Every observed case here came\n     from the agent reaching for a different compiler rather than fixing\n     the code.\n\nDiscovered while feeding ports through the loop on hardware.\n","id":"poly-bxo","issue_type":"bug","labels":["agent","build","dev-env"],"owner":"github@dragonflybsd.org","priority":1,"status":"open","title":"the agent's dsynth_build builds any dependency its own patch introduces, unbounded and unfiltered","updated_at":"2026-09-04T07:49:36Z"}
{"_type":"issue","comment_count":0,"created_at":"2026-09-03T12:57:23Z","created_by":"Fred [bot]","dependencies":[{"created_at":"2026-09-09T17:27:51Z","created_by":"Fred [bot]","depends_on_id":"poly-cg6u","issue_id":"poly-0g0","metadata":"{}","type":"parent-child"}],"dependency_count":0,"dependent_count":1,"description":"Two quantities are both called \"tokens\" and each display picks whichever\nits event happened to carry. Nothing labels which one is on screen.\n\n  billable = uncached prompt + completion   <- what the budget enforces\n  total    = prompt + completion, where prompt re-counts the whole\n             cached prefix EVERY turn       <- what the provider reports\n\nOn one real patch job (audio/cdparanoia, 2026-09-03) they differ by\n21x: 86,141 billable against 1,817,637 total. Neither is wrong. They\nanswer different questions and are presented identically.\n\nTHE CLEAREST CASE -- two adjacent lines of the bundle timeline, same\nword:\n\n  attempt 1/4 for audio/cdparanoia (tokens used 0/120000)\n  attempt 1 for audio/cdparanoia: rebuild_ok=False tokens=1396534\n  attempt 2/4 for audio/cdparanoia (tokens used 72041/120000)\n  attempt 2 for audio/cdparanoia: rebuild_ok=False tokens=421103\n\nAttempt 1 used 1,396,534 by one line and 72,041 by the next.\nattempt_loop.py:243 sends billable as tokens_used_so_far;\nattempt_loop.py:386 sends total as tokens; agentic_bundle.html:129,131\nrenders both as \"tokens\".\n\nWHERE EACH SITE STANDS\n\n  _job_token_usage.html      billable headline + total marked\n                             \"incl. re-billed cache\"   <- the only\n                             one that is right\n  agentic_bundle.html:145    port lifetime card: Prompt / Completion /\n                             Total only. cdparanoia shows 5,313,167\n                             where the port cost 395,837 billable\n  render/activity.py:63      activity group total = sum of total_tokens\n  agentic_session.html:169   per-turn cumulative_total_tokens, while\n                             tool_loop.py:191 emits\n                             cumulative_billable_tokens right beside it,\n                             unused\n\nSo the same job reads as 86k, 1.4M or 5.3M depending on the page.\n\nTWO MORE, found while tracing\n\n1. TRIAGE MEASURES NO CACHING AT ALL. triage.py:132-140 emits an\n   llm_turn without cached_tokens; tool_loop.py:186 includes it. The\n   Usage object HAS the number, the event drops it. The card then\n   computes billable = prompt + completion, so every triage job claims\n   a 0% cache hit -- and \"no cache\" is indistinguishable from \"not\n   measured\".\n2. LATENT: token_usage_for_job matches stage LIKE '%llm_turn',\n   token_usage_for_port matches stage = 'llm_turn' exactly\n   (agentic_queries/jobs.py). Dormant -- no convert:llm_turn rows exist\n   today -- but the two cards would disagree silently the moment one\n   appears.\n\nThe pie is internally consistent (it is a breakdown of total) but\nrenders as ~95% blue \"cached\" directly above a Billable row of 86,141,\nso the picture is dominated by a number that is not the cost.","design":"ONE UNIT FOR THE HEADLINE, EVERYWHERE: billable. Total stays -- it is\nthe honest provider number and the ratio between the two is itself the\nsignal a prefix cache is working -- but it never appears without the\n\"incl. re-billed cache\" qualifier that _job_token_usage.html already\nuses. That template is the model; the rest is what it has not reached.\n\nFIX THE TWO WRITERS FIRST. Everything else is a consequence, and\nfixing the readers one by one would be chasing symptoms:\n\n  1. runner.py:4087  patch_audit.json's tokens_used gains cached and\n                     billable. result.usage already has them. This one\n                     change is what lets the PR, the handoff, the\n                     proposed fix and the agent's context all report\n                     cost instead of re-billed traffic.\n  2. attempt_loop.py:386  attempt_end carries billable alongside total,\n                     so the bundle timeline can show the same unit as\n                     the line above it.\n  3. triage.py:132   emit cached_tokens and billable_tokens like\n                     tool_loop.py:186 does. Until then triage claims a\n                     0% cache hit and _activity_row.html:28 shows a\n                     total under a header that says billable.\n\nTHEN THE READERS\n\n  4. orchestrator.py:545  the PR says which number it is, or says\n                     billable. This one is published upstream, so it\n                     matters most and should not wait.\n  5. token_usage_for_port returns cached/billable -- it already sums\n     the rows it needs -- and the port card leads with billable.\n  6. _job_activity.html:108, render/activity.py:63, the session view\n     and agentic-bundle.js:427 use the billable figures the events\n     already carry.\n  7. the two stage matchers agree (LIKE '%llm_turn' vs = 'llm_turn').\n\nWHAT NOT TO DO: do not drop total, and do not \"reconcile\" the two by\npicking whichever is smaller. They measure different things and both\nare wanted; the defect is that they are unlabelled and interchanged.\n\nA CARD ONLY SEES llm_turn ROWS. Any spend that is charged to\nresult.usage without emitting one is invisible to the job card while\nstill appearing in the audit -- a real 34,682-token divergence was\nobserved this way. Whatever the fix, keep one source of truth for a\njob's cost rather than two sums that can drift.\n\nVERIFY on real jobs, not fixtures: take one patch job and one triage\njob and check the card, the activity group header, the audit, the PR\nbody and the handoff all report the same billable figure.","id":"poly-0g0","issue_type":"bug","labels":["cost","llm","ui"],"notes":"RE-REVIEW. This is not a display bug. It starts at two writers and\neverything downstream inherits it, including surfaces outside the\ntracker.\n\nTHE TWO WRITERS\n\n  runner.py:4087-4094  patch_audit.json's tokens_used carries\n                       prompt/completion/total and DROPS cached, even\n                       though result.usage has it. Every audit consumer\n                       is therefore stuck with total.\n  attempt_loop.py:386  attempt_end sends attempt_usage.total_tokens as\n                       \"tokens\", while attempt_loop.py:243 sends\n                       total_usage.billable_tokens as\n                       \"tokens_used_so_far\". Adjacent events, same word,\n                       different units.\n\nWHERE THAT REACHES, VERIFIED ON REAL DATA\n\n  * THE PULL REQUEST. bundle_actions.py:181 reads tokens_used.total and\n    passes it to deliver(); orchestrator.py:545 writes\n    \"- Agent: model=... attempts=2 tokens=<total>\" into the PR body.\n    That number is published upstream. The real audit blob for\n    audio/cdparanoia says total 1,852,319 where the job cost 86,141\n    billable.\n  * THE AGENT'S OWN CONTEXT. context.py:733 feeds the model \"- tokens:\n    prompt=... completion=... total=...\" from the audit, and\n    context.py:779-790 renders attempt_start (billable) and attempt_end\n    (total) as adjacent lines. On a retry the model reads its own spend\n    at ~14x what the budget actually counted.\n  * MANUAL HANDOFF. manual_handoff.py:304 uses usage.total_tokens, :316\n    falls back to audit total; printed as \"Tokens used: N\".\n  * PROPOSED FIX. proposed_fix.py:306-310 reads the same three fields.\n\nSHARPEST SINGLE SCREEN, and probably what prompted this:\n_job_activity.html:108 prints an attempt group header as\n\"N events - 1,396,534 tokens - 12 tool calls\" directly above a table\nwhose own column header is \"Cum (billable)\" (:77), whose last row for\nthat same attempt reads 72,041. One screen, one attempt, 19x apart.\n\nCORRECTIONS TO THE ORIGINAL DESCRIPTION\n\n  * _activity_row.html is RIGHT and I was wrong to imply otherwise. It\n    prefers cumulative_billable_tokens and the column is labelled\n    \"Cum (billable)\". Its real defect is narrower: the fallback at :28\n    silently shows cumulative_total under that billable header whenever\n    the field is missing -- which the comment frames as \"pre-H4 rows\"\n    but is in fact EVERY triage row today, because triage.py:132-140\n    still does not emit it.\n  * agentic-bundle.js:427 is another unlabelled total (\"last turn: N\n    tokens\"), in the fix-chat.\n  * A card can only see spend that arrives as an llm_turn row. The\n    cdparanoia audit above reads 1,852,319 against the card's\n    1,817,637; the 34,682 difference was exactly the poly-b05 re-prime,\n    which was charged to result.usage but was not an llm_turn. The\n    re-prime is reverted so no such gap exists today, but the structure\n    that allowed it does.\n\nSCALE, 14 patch jobs on 2026-09-03: every one runs 7x to 21x, mean\n~14x.\n\n  cdparanoia   1,817,637 / 86,141  = 21.1     nspr      1,796,840 / 94,235 = 19.1\n  redis        1,483,186 / 96,884  = 15.3     net-snmp  1,649,959 / 95,898 = 17.2\n  p5-Sub-...   1,140,728 / 62,571  = 18.2     timidity    391,788 / 41,090 =  9.5\n  deb2targz      154,099 / 21,788  =  7.1     libfyaml    520,652 / 42,797 = 12.2\n\nThe ratio is not noise and it is not constant, so no reader can correct\nfor it mentally.\nIMPLEMENTED (uncommitted, not yet on hardware).\n\nWRITERS, which is where it started\n\n  runner.py         patch_audit.json tokens_used gains cached +\n                    billable; each attempt entry gains billable_tokens.\n                    Both phase results gain tokens_cached /\n                    tokens_billable.\n  attempt_loop.py   AttemptInfo gains billable_tokens; attempt_end\n                    carries billable_tokens + cached_tokens beside the\n                    total, so it reports the same unit as the\n                    attempt_start above it.\n  triage.py         emits cached_tokens, billable_tokens and\n                    cumulative_billable_tokens, like tool_loop already\n                    did. Triage no longer claims a 0% cache hit.\n  phase_result.py   tokens_cached / tokens_billable added with\n                    defaults and NO schema bump -- a bump would make\n                    every artifact already in the store raise\n                    PhaseResultVersionMismatch, and load_phase_result\n                    already fills missing keys from defaults and\n                    filters unknown ones.\n\nREADERS\n\n  orchestrator.py   the PR line says which count it is: \"tokens=86141\n                    billable\" or \"tokens=1852319 total, incl. re-billed\n                    cache\". No kind -> bare number, no claim.\n  bundle_actions.py prefers billable, falls back to total AND says so.\n  context.py        the agent is told billable first, and the total is\n                    always qualified. attempt_end in the trace summary\n                    now reports billable to match attempt_start.\n  manual_handoff.py billable, falling back to total for a usage object\n                    without the field -- reporting 0 would be worse\n                    than reporting the inflated number.\n  proposed_fix.py   the Cost section leads with \"Billable (real cost,\n                    triage + patch)\"; totals stay, qualified.\n  jobs.py           token_usage_for_port returns cached + billable and\n                    matches stage LIKE '%llm_turn', same as\n                    token_usage_for_job. The two cards are now\n                    comparable.\n  activity.py       group headers sum billable, falling back to a row's\n                    total. The header no longer contradicts the \"Cum\n                    (billable)\" column directly beneath it.\n  sessions.py       the per-turn running total is billable.\n  agentic_api.py    chat usage returns cached_tokens + billable_tokens.\n  templates/js      port card leads with billable; the bundle timeline\n                    shows billable on attempt_end; the group header\n                    says \"billable tokens\"; the session badges are\n                    billable; the chat says \"billable tokens\", or\n                    \"tokens, incl. re-billed cache\" when it only has\n                    the total. _activity_row marks a fallback value\n                    with * and a title, because a total under a header\n                    saying billable is the bug in miniature.\n\nTESTS: tests/test_token_accounting_units.py (25), organised by\nboundary rather than by module -- the audit, the PR, the agent's own\ncontext, the two tracker cards, triage, the cost document, the\nhandoff. Behavioural where it matters: the port-card tests drive real\nsqlite through the real schema, and the attempt_end tests drive\nattempt_loop.\n\nMUTATION-VERIFIED, 24 mutations, every one now fails at least one\ntest. Two SURVIVED the first pass and both were my tests being too\nweak -- source greps that matched a string still present elsewhere in\nthe file. Replaced with behavioural tests. Worth remembering: an\ninspect.getsource assertion passes for the wrong reason far too\neasily.\n\nAlso fixed two test doubles that had drifted from the types they\nmirror: _usage() in test_phase_result_producers now builds a real\nUsage (billable_tokens is derived, so a namespace double has to\nreimplement it), and _StubAttempt gained the field.\n\nSuite 3028 passed. Templates parse; agentic-bundle.js passes\nnode --check.\n\nNOT DONE: no hardware verification yet. The acceptance check in the\ndesign is to take one patch job and one triage job and confirm the\ncard, the group header, the audit, the PR body and the handoff all\nreport the same billable figure.","owner":"github@dragonflybsd.org","priority":1,"status":"open","title":"two quantities are both called \"tokens\": the re-billed total reaches the PR, the handoff and the agent's own context as if it were the cost","updated_at":"2026-09-03T13:22:28Z"}
{"_type":"issue","closed_at":"2026-09-03T12:47:06Z","comment_count":0,"created_at":"2026-09-03T12:00:10Z","created_by":"Fred [bot]","dependencies":[{"created_at":"2026-09-03T14:00:09Z","created_by":"Fred [bot]","depends_on_id":"poly-2w6","issue_id":"poly-b05","metadata":"{}","type":"discovered-from"}],"dependency_count":0,"dependent_count":0,"description":"Follow-up to poly-2w6, which sent prompt_cache_key and did NOT fix the\nretry cold start. This is the piece that does.\n\nWHY THE KEY ALONE IS NOT ENOUGH. The provider matches only the\nMOST-RECENTLY-USED prefix for a key. Attempt 1 grows its conversation to\n~110k tokens, so that long chain becomes the match target. Attempt 2's\nfirst request shares only the early [system, payload] prefix -- still\ncached, an exact repeat of it hits -- but it is not what the lookup\ncompares against, so attempt 2 pays its whole cold start again.\n\nTHE FIX. Before the first request of attempt N>1, send one throwaway\nrequest containing EXACTLY attempt 1's opening messages. Exact matches\nhit regardless of recency, so that call is nearly free, and it makes the\nearly prefix most-recent again. The attempt's real first request is then\na plain extension of it and hits.\n\nMEASURED BY REPLAY of the real cdparanoia transcripts, two arms on\nseparate keys, chain grown 30 turns so the failure actually reproduces:\n\n  ARM A  attempt2 turn1, no re-prime\n         prompt=31269 cached=    0 out=170  BILLABLE=31439\n  ARM B  re-prime (max_tokens=1)\n         prompt=31172 cached=31153 out=  1  BILLABLE=   20\n  ARM B  attempt2 turn1\n         prompt=31269 cached=31217 out=271  BILLABLE=  323\n\n  NET SAVING: 31,096 billable tokens per retry, cost 20.\n\nThe throwaway completion is NOT a hidden cost: with max_tokens=1 it\nemitted one output token: reasoning did not blow through the cap. That\nwas the open question when the approach was first described.\n\nFor scale: cdparanoia's tier budget is 120k and its cold start ~31k, so\nattempt 2 currently spends a quarter of the remaining budget re-reading\ncontext it already had. nspr's cold start is 42k, so its saving is\nlarger. This is the difference between max_iterations: 4 being fiction\nand being real.","design":"Send attempt 1's exact opening messages once, before attempt N>1 starts,\nwith the same prompt_cache_key and max_tokens=1.\n\nWhere: attempt_loop, at the top of an attempt when attempt_idx > 1. It\nneeds the first request's messages from attempt 1, which the loop\nalready has -- the payload is rebuilt per attempt and the system prompt\nis constant, so \"attempt 1's opening\" is reconstructible without storing\na transcript.\n\nBEST-EFFORT, NON-NEGOTIABLE. Wrap so that any failure -- timeout, 4xx,\n5xx, provider change -- is a no-op that does not touch the attempt, does\nnot consume the budget ladder, and does not log an error louder than\nDEBUG. It is an optimisation; nothing may depend on it.\n\nOnly for providers whose dialect declares cache controls. Reuse\n_CACHE_DIALECTS from poly-2w6: no dialect, no re-prime. DeepSeek has no\nkey and no most-recent-prefix behaviour to exploit.\n\nMEASURE, do not assume: log the re-prime's own usage and the following\nturn's cached_tokens, so the gain is visible per run rather than\ninferred from this bead. If a future provider change kills it, that\nnumber goes to zero and says so.\n\nRISKS, both real:\n\n1. UNDOCUMENTED BEHAVIOUR. Most-recently-used matching appears nowhere\n   in Meta's docs; it is inferred from measurement. It can change\n   silently. The best-effort wrapper is what makes that survivable.\n\n2. THERE IS A REGIME WHERE NONE OF THIS HELPS. The decay probe went cold\n   at 30 minutes and never recovered across an hour of continued keyed\n   traffic (poly-2w6 notes, behaviour 3, unexplained, n=1). So the tier\n   budget must still be sized as though the cache is absent -- this\n   reduces cost when it works, it does not make max_iterations safe on\n   its own.\n\nNOT WORTH IT WHEN attempts are short: with 20 turns of growth the miss\ndoes not occur and the re-prime is a no-op costing 20 tokens. That is\ncheap insurance, not waste, and it is why this should be unconditional\nfor attempt N>1 rather than gated on a size heuristic nobody can tune.","id":"poly-b05","issue_type":"feature","labels":["cost","llm","provider"],"notes":"IMPLEMENTED (uncommitted, NOT yet verified on hardware).\n\nllm.supports_prompt_cache -- true only when the provider has a\n                    _CACHE_DIALECTS entry AND the request takes the SDK\n                    path. Both halves matter: on the litellm path the\n                    key never reaches the endpoint, so a re-prime there\n                    would be a full-price request buying nothing.\nllm.prime_cache     -- one throwaway request, max_tokens=1, returns its\n                    own Usage or None. Never raises: the support check\n                    is inside the guard too, because it reads settings.\n                    Goes straight to _complete_openai rather than\n                    through complete() -- the staged retry ladder exists\n                    to protect turns already spent, and minutes of it on\n                    a request whose whole value is being cheap defeats\n                    the purpose. Timeout capped at 60s for the same\n                    reason: a hang must cost seconds, not the attempt's\n                    ten minutes.\n_complete_openai    -- gained max_tokens. Only prime_cache sets it.\nattempt_loop        -- re-primes with base_messages + the same tool\n                    schemas tool_loop sends, at attempt_idx > 1, after\n                    both budget gates so a bailing attempt spends\n                    nothing.\n\nTWO DEFECTS FOUND IN RE-REVIEW, both fixed:\n\n1. supports_prompt_cache sat OUTSIDE the try. It reads settings via\n   _backend(), so a config failure would have raised out of a function\n   documented as never raising, straight into the attempt loop.\n2. Charging the re-prime shrinks `remaining`, and tool_loop reads\n   max_tokens=0 as \"NO CAP\". A re-prime that MISSED costs a whole cold\n   start, so it could land remaining on exactly zero and uncap the\n   retry instead of stopping it. Now gated: remaining <= 0 after the\n   re-prime returns budget-exhausted.\n\nThe cost IS charged to the budget deliberately. ~20 billable when it\nworks, a full cold start when it does not -- and that second case is\nprecisely the leak the budget has to be able to see. Not counting it\nwould hide a provider change behind a silent overspend.\n\nVisible per run, not inferred from this bead: a cache_reprime event\ncarries prompt/cached/billable and PatchEventDispatcher writes it to\nthe activity log. If the trick stops working, cached goes to 0 in that\nrow and says so.\n\nTests: tests/test_cache_reprime.py (27). Mutation-verified -- 12\nmutations, each fails at least one test: dropping max_tokens=1, the\ntimeout cap, the swallow, the inside-the-try move, the SDK-path\nrequirement, the attempt_idx > 1 condition, base_messages vs attempt\nN's messages, the tool schemas, the budget charge, the exhausted guard,\nthe activity row, the event. Suite 3029 passed.\n\nSTILL TO DO, and it is the whole bead: deploy and re-run devel/nspr or\naudio/cdparanoia, then read A2T1. Today that row says cached=0/34372.\nACCEPTANCE RUN 2026-09-03, audio/cdparanoia. THE MECHANISM WORKS AND\nTHE SAVING DOES NOT. Net effect is a wash; the bead's 31,096 does not\nreproduce in production.\n\n  run              turns  billable  A2T1\n  12:34 re-prime      41   120,823  cached=34673/34744  billable=   194\n  11:04 key only      36   123,720  cached=    0/34372  billable=34,531\n  10:21 key only      42   121,189  cached=    0/33931  billable=34,080\n\n(12:34's total is 86,141 in llm_turn rows PLUS the 34,682 re-prime,\nwhich is a cache_reprime row and not counted in that sum.)\n\nWHAT WORKED: A2T1's cold start is gone -- 34,531 -> 194 billable, a\n99.8% hit. The re-prime does make the prefix current, exactly as\ndesigned.\n\nWHAT FAILED: the re-prime paid the cold start itself.\ncached=0, billable=34,682. So the spend moved from attempt 2 turn 1\ninto the re-prime rather than disappearing. Same total, same\nbudget-exhausted verdict, 41 turns against 36 and 42 for the two runs\nbefore it.\n\nTHE PREMISE IS REFUTED. This bead rests on \"an exact repeat hits\nregardless of recency\". The re-prime was byte-identical to attempt 1\nturn 1 -- both report prompt=34,681, the same request to the token --\nand it got cached=0. Whatever made attempt 2 turn 1 miss makes an\nexact repeat miss too.\n\nThe replay measured that same re-prime hitting 31,153. It cannot be\nreconciled with this run; the replay's conditions differ from\nproduction in ways that turn out to matter.\n\nTWO MODELS FIT THE DATA AND I CANNOT SEPARATE THEM AT n=1:\n\n  (a) the growing chain displaces the early prefix, so nothing but the\n      most recent entry is reachable -- the original story\n  (b) the cache only serves a request that closely FOLLOWS another on\n      the same key. Every \"first request after a gap\" in this run\n      missed (A1T1 cold, re-prime after ~5min of tool work) and every\n      request seconds behind another hit (A1T2-A1T30, A2T1 right after\n      the re-prime)\n\n(b) also fits the decay probe never recovering after 30 minutes, which\n(a) never explained. Neither is worth asserting without a designed\nexperiment.\n\nWHAT DID NOT REGRESS: the safety design held. The re-prime's true cost\nwas visible in its own activity row rather than hidden, the budget was\ncharged 34,682 for it (attempt_start showed 72,041/120,000 going into\nattempt 2), and nothing depended on the hit. Charging it is what made\nthe wash measurable instead of looking like a 34k saving in the turn\nrows.\n\nRECOMMENDATION: revert. It buys nothing measurable, costs one extra\nrequest per retry and ~350 tokens, and the assumption underneath it is\nnow known to be false. Reverting leaves poly-2w6's key in place, which\nis still correct and still cheap. The alternative worth more than\nanother cache trick is the one already noted here: have attempt N\ncontinue attempt N-1's conversation instead of rebuilding it, which\nneeds no provider assumption at all.\nREVERTED in ad6857d. Not a design that needs another pass -- the\nassumption it rests on is measurably false, so there is nothing here to\ntune. poly-2w6's cache key is unaffected and stays.\n\nWhat survives for whoever picks this up:\n\n  * within-attempt caching works and is worth having (99% hits between\n    consecutive turns) -- that is poly-2w6, already shipped\n  * the cross-attempt cold start is real and unfixed. cdparanoia pays\n    ~34k of a 120k budget to re-read context it already had\n  * the fix that needs no provider assumption is to have attempt N\n    continue attempt N-1's conversation instead of rebuilding it. That\n    conflicts with the deliberate workspace reset between attempts, so\n    it is a real design question, not a trick\n  * raising the tier budget remains the blunt instrument that would\n    actually move these ports\n\nDo not re-file a cache-key variant without first explaining why an\nexact repeat of a 34,681-token prefix returned cached=0.","owner":"github@dragonflybsd.org","priority":1,"status":"closed","title":"re-prime the cache before a retry: measured 31,096 billable tokens saved per attempt for a cost of 20","updated_at":"2026-09-03T12:47:06Z"}
{"_type":"issue","close_reason":"Implemented (5d4be30), deployed and verified on hardware: the bead's own test -- attempt 2 turn 1 reporting a large cached_tokens -- passed at cached=31985, billable 33019 -> 989. Benefit is partial (~1 hit in 3 retries) and the residual miss is provider-side routing the key cannot control, same finding as the closed poly-b05. No further code fix on our side.","closed_at":"2026-09-04T20:27:31Z","comment_count":0,"created_at":"2026-09-03T10:34:43Z","created_by":"Fred [bot]","dependency_count":0,"dependent_count":0,"description":"MEASURED, not inferred. Meta's endpoint caches prefixes, but only routes\nyou back to the backend holding one if the request carries\nprompt_cache_key. We send none, so every request after a divergence is\nload-balanced to an arbitrary backend whose auto-cache has never seen\nour prefix.\n\nSame ~7.5k-token prefix, five requests, diverging tails:\n\n    1. cold, WITH key                 cached=0\n    2. diverging, WITH key            cached=7537   <- hit\n    3. diverging, NO key              cached=0      <- key needed to READ\n    4. diverging, WITH key again      cached=7537   <- still there\n    5. diverging, DIFFERENT key       cached=0\n\nStep 3 is the finding: the prefix demonstrably existed (step 4 hit it\nstraight after) and a keyless request still could not find it. So\n\"populate once with a key, then rely on auto-cache\" does not work -- the\nkey must be on EVERY request that wants the cache.\n\nDocs agree with the mechanism: \"Requests that share a key route\ntogether, so a request is more likely to land on a backend that already\nholds its prefix.\" Auto-caching is real -- it is why we see 99% hits\nbetween consecutive turns of one attempt -- but across a gap that\naffinity is luck, and we measured it losing 6 times out of 6.\n\nWHY IT MATTERS MORE THAN THE PRICE. Our tier budget counts\nbillable = raw - cached, so a cached prefix is nearly free against the\nBUDGET, not just the bill. Cold-start size predicts budget exhaustion\nalmost perfectly across 11 ports run 2026-09-03:\n\n    42291  nspr             budget_exhausted\n    34702  net-snmp         budget_exhausted\n    33823  cdparanoia       budget_exhausted\n    31904  timidity         fixed\n    25770  libfyaml         fixed\n    24577  redis            budget_exhausted   <- the one exception\n    24172  google-perftools fixed\n    22922  p5-Sub-HandlesVia-XS fixed\n    19800  mxml             fixed\n    18140  deb2targz        fixed\n    17806  cdb              fixed\n\nThe three largest all exhausted; six of the seven smallest were all\nfixed. At 120k, a 33k cold start leaves ~87k and a second attempt costs\nanother 33k -- one attempt and a stub. nspr has now hit exactly 30 turns\non four separate runs under two different models.\n\nA working cross-attempt cache turns attempt 2's cold start from\n24k-42k billable into a few hundred, which is the difference between\n1.4 attempts and the 4 that max_iterations already promises. No prompt\nchanges, no budget increase.","design":"Send a stable prompt_cache_key on every request, per provider that has\nthe concept.\n\nSCOPE: Meta only. DeepSeek's caching is automatic with no equivalent\nknob (prefix-only from token 0, 64-token chunks, reported as\nprompt_cache_hit_tokens/prompt_cache_miss_tokens). So this belongs\nbeside _REASONING_DIALECTS as a second per-provider capability, not as a\nblanket parameter -- sending an unknown field to deepseek is exactly the\n400 that _reasoning_meta exists to avoid.\n\nWHAT THE KEY SHOULD BE. One stable key per group of requests that share\na prefix. A per-bundle key fixes the retry case; a key per (role,\nplaybook selection) would also let different ports share the static\nprefix. Since a bundle's attempts share their playbook selection too,\nthe coarser key subsumes the finer one -- and at one-job-at-a-time\nvolume, pinning all traffic to one backend is what we want, not a\nscaling risk. Simplest workable version: one key per role,\ne.g. \"polytropos-patch\". Nothing is stored or expires; the key is\nderived, never persisted, because it is a routing hint and not a handle.\n\nAlso set prompt_cache_retention: \"24h\". It is documented as a hint, not\na guarantee (\"the server may evict entries early under load\"), so treat\nit as opportunistic. A decay probe measuring real survival time is\nrunning; if the prefix does not survive the gap between attempts, this\nbead only helps within an attempt and the tier budget stays the real\nconstraint.\n\nTWO IMPLEMENTATION TRAPS, both verified against the current code:\n\n1. extra_body COLLISION. _complete_openai does\n       kwargs.update(_reasoning_kwargs(reasoning, provider))\n   and _reasoning_kwargs returns {\"extra_body\": {...}} for deepseek-off\n   and for nvidia. A cache-key addition that also sets extra_body will\n   clobber the reasoning payload via .update(). The two must MERGE into\n   one extra_body dict, and a test should pin that -- silently losing\n   `thinking: disabled` would restore the ~50% reasoning spend poly-r1g\n   exists to cut.\n\n2. THE SDK IS TOO OLD FOR THE NAMED KWARG. openai 1.70.0 on the box\n   raises \"Completions.create() got an unexpected keyword argument\n   'prompt_cache_key'\". Both parameters have to go through extra_body,\n   which is also why trap 1 is unavoidable rather than incidental.\n\nPLUMBING. llm.complete() takes no context identifier today, and neither\ndoes tool_loop.run() (messages, model, env, api_base, api_key,\ncustom_llm_provider, timeout, max_turns, max_tokens, on_event,\nattempt_idx, tool_whitelist, reasoning). A role-level constant needs no\nplumbing at all, which is a further argument for the coarse key over a\nper-bundle one.\n\nVERIFY ON HARDWARE, not in tests: re-run devel/nspr and check that\nattempt 2 turn 1 reports a large cached_tokens instead of 0. That single\nnumber is the whole bead.","id":"poly-2w6","issue_type":"feature","labels":["cost","llm","provider"],"notes":"IMPLEMENTED (unpushed, not yet verified on hardware).\n\n  _CACHE_DIALECTS  -- second per-provider capability table, beside\n                      _REASONING_DIALECTS. Meta only; absent means\n                      \"nothing to send\".\n  _cache_meta      -- prompt_cache_key = \"polytropos-<model>\", plus\n                      prompt_cache_retention \"24h\", through extra_body\n                      because openai 1.70.0 rejects both as kwargs.\n  _merge_call_kwargs -- update(), except extra_body MERGES.\n\nThe merge is the load-bearing part, not tidiness. _complete_openai did\nkwargs.update(_reasoning_kwargs(...)), and both tables return an\nextra_body: deepseek-off sends `thinking`, nvidia sends\n`chat_template_kwargs`, Meta's cache controls go there too. A plain\nupdate() drops whichever ran first -- and losing `thinking: disabled`\nsilently restores the ~50% of billable spend poly-r1g exists to cut.\n\nKey is scoped by MODEL, not by bundle or role. Everything talking to one\nmodel routes together, which is what we want at one-job-at-a-time\nvolume: a bundle's attempts share a prefix and so do different ports up\nto their first divergence. It needs no plumbing at all -- llm.complete()\nand tool_loop.run() carry no context id, so a per-bundle key would have\nmeant threading an argument through both for no measured gain.\n\nSDK path only. litellm 1.65.0 forwards extra_body as a literal\ntop-level JSON key instead of unpacking it, so cache controls sent down\nthat path would be a junk field on the wire rather than a cache hit.\n\nDECAY MEASURED while implementing: the keyed prefix held 5169/5226\n(98.9%) flat at 1, 2, 5, 10 and 20 minutes, with the unkeyed control\nmissing at every single step. No decay curve -- it is binary. Attempts\nare 1-5 minutes apart, so the gap that matters is covered severalfold.\nLonger points (30/45/60/90m) were still running; they only bear on\ncross-batch reuse, not on this decision.\n\nTests: tests/test_llm_prompt_cache.py (13). Mutation-verified both ways\n-- replacing the merge with update() fails 3, dropping the cache kwargs\nfrom the SDK path fails 1. Suite 3002 passed.\n\nSTILL TO DO: the acceptance test is one number and it needs a deploy --\nre-run devel/nspr and confirm attempt 2 turn 1 reports a large\ncached_tokens instead of 0. nspr has hit exactly 30 turns on four runs\nunder two models, so it is the cleanest possible control.\nDECAY, CORRECTED. The probe reached 30 minutes and the prefix was GONE:\n\n    [1m]  keyed 5169/5226   unkeyed 0\n    [2m]  keyed 5169/5226   unkeyed 0\n    [5m]  keyed 5169/5226   unkeyed 0\n    [10m] keyed 5169/5226   unkeyed 0\n    [20m] keyed 5169/5226   unkeyed 0\n    [30m] keyed    0/5226   unkeyed 0     <- evicted\n\nNot a TTL. The 10->20m gap was ten minutes and hit; the 20->30m gap was\nalso ten minutes and missed. That is eviction under pressure, exactly as\ndocumented, and the \"24h\" retention hint plainly did not deliver 24h.\n\nSo: treat a hit as opportunistic, never build anything that needs one.\nThe case this bead exists for is unaffected -- attempts within a bundle\nare 1-5 minutes apart, comfortably inside where the cache held. Reuse\nacross a longer gap (a later batch, the next session) should be assumed\nabsent, which also caps how much the cross-port sharing idea could ever\nbe worth.\n\nThe code comment has been corrected; it previously said \"at least 20\nminutes\", which was true but read as monotonic survival.\nRE-REVIEW.\n\nFixed: _CACHE_DIALECTS was declared empty and then mutated 67 lines\nlater (_CACHE_DIALECTS[\"meta\"] = _cache_meta), while _REASONING_DIALECTS\ndirectly below it is a literal. Reading the declaration told you nothing\nabout the table's contents. Reordered to the established shape --\ndialect function, then literal table, then the lookup.\n\nChecked and NOT defects, but worth knowing:\n\n- The key follows the configured model spelling. _bare_model keeps the\n  prefix on \"meta/muse-spark-1.3-contributor\" because meta is in\n  _NAMESPACED_MODEL_IDS (there to protect NIM's meta/llama-*), so that\n  config yields a different key than the bare id. Harmless: such a\n  config is already broken, since the API would receive\n  \"meta/muse-spark-...\" and reject it before caching mattered. The\n  documented expectation for this provider is the bare id.\n\n- The key carries no host, path or user identifiers.\n\n- An api_base-only config (no provider name) gets no cache controls,\n  exactly as it gets no reasoning dialect. Same trap, already recorded\n  on poly-p0h -- naming the provider is what activates both.\n\nKNOWN LIMITATION, accepted deliberately: triage and patch run the same\nmodel today, so they share one key and one routing group despite having\ndifferent prefixes. Under eviction pressure they compete. A per-role key\nwould separate them but needs an argument threaded through\nllm.complete() and tool_loop.run(), neither of which carries a context\nid. Not worth it without evidence of thrash -- eviction was observed at\n30 minutes with a SINGLE prefix, so a second one is unlikely to be the\ndeciding factor.\n\nSuite 3002 passed; mutations still fail the right tests after the\nreorder.\nACCEPTANCE TEST FAILED ON HARDWARE 2026-09-03. The change does NOT\ndeliver the cross-attempt benefit it was filed for.\n\nRe-ran the four budget-exhausted ports with the key deployed:\n\n  devel/nspr         budget_exhausted  30 turns  1 attempt\n  net-mgmt/net-snmp  budget_exhausted  30 turns  1 attempt\n  audio/cdparanoia   budget_exhausted  36 turns  2 attempts  A2T1 cached=0/34372\n  databases/redis    budget_exhausted  30 turns  1 attempt\n\nIdentical to the run before the change. nspr hit exactly 30 turns for\nthe fifth time.\n\nWHAT DOES WORK, so this is not a regression:\n  - the key IS on the wire (verified against the deployed build:\n    extra_body carries prompt_cache_key + retention, merged correctly\n    alongside reasoning_effort)\n  - within-attempt caching is unchanged: cdparanoia attempt 1 turn 2\n    cached 34225/34521, turn 30 cached 57329/57684\n\nWHAT FAILS: attempt 2 turn 1, cached=0, on a request that is\nBYTE-IDENTICAL to attempt 1 turn 1 (system 19,076 chars identical, user\n89,540 chars identical), sent 9 seconds after attempt 1's last turn and\n4.9 minutes after attempt 1's first.\n\nFOUR HYPOTHESES TESTED AND DISPROVEN, each with a measurement:\n  1. Time-based eviction -- no: 4.9 minutes, well inside the 20 the\n     decay probe held.\n  2. Extension supersedes the short form -- no: primed a short prefix,\n     extended it 8 times, re-sent the short form, still hit 4977/5021.\n  3. Prompt size -- no: an 85k-token prompt repeated hit 85169/85211.\n  4. Tools, or cross-port contention on the shared key -- no: tools do\n     not change prompt_tokens or break the hit, and the activity log\n     shows cdparanoia ran alone from 11:04 to 11:09 with no interleaving.\n\nUNEXPLAINED: attempt 2 turn 1 reports 34,372 prompt tokens where\nattempt 1 turn 1 reported 34,275 -- a 97-token difference on messages\nthat are byte-identical. Tools are not in the session transcript, and\nthey do not affect prompt_tokens in a direct test, so the delta's source\nis not yet identified. It is the most promising thread: something\noutside the recorded messages differs between attempts, and whatever it\nis sits early enough to end the prefix match.\n\nTHE COMMIT MESSAGE OVERCLAIMED. It says \"This is budget, not just\nprice\". On this evidence the budget claim is unsupported: the mechanism\nis real and measurable in isolation, and does not reach the case it was\nbuilt for. The change is harmless and correct in itself -- keep or\nrevert on taste -- but the tier budget remains the actual constraint on\nthese four ports, and raising max_tokens is back on the table as the\nthing that would actually move them.\nROOT CAUSE FOUND, and it is deterministic -- the earlier \"probabilistic,\nn=1\" reading was wrong.\n\nTHE PROVIDER MATCHES ONLY THE MOST-RECENTLY-USED PREFIX FOR A KEY, not\nevery cached prefix.\n\nReproduced end to end with the real transcripts:\n\n    state: large chain (30 grown turns)   prompt= 84175  cached=81521\n    branch WITHOUT re-prime               prompt= 31269  cached=0\n    re-prime: exact [sys,user]            prompt= 31172  cached=31153\n    branch AFTER re-prime                 prompt= 31269  cached=31217\n\nDuring attempt 1 the conversation grows to ~110k tokens, so the\nmost-recent entry for the key is that long chain. Attempt 2's first\nrequest shares only the early [system,user] prefix. That prefix is still\ncached -- an exact repeat of it hits 31,153 -- but it is not what the\nlookup compares against, so the attempt-2 branch gets nothing. Touch it\nfirst and it becomes most-recent again, and the branch then hits 99.8%.\n\nThis explains every observation at once:\n  - within-attempt hits: each turn extends the most-recent chain\n  - attempt-2 miss: shares only the early prefix\n  - synthetic 30-turn test hit: growth was ~1k, so the early prefix was\n    still effectively the most recent\n  - exact-repeat probes hit: exact match works regardless of recency\n\nWHAT WOULD ACTUALLY FIX IT: before the first request of attempt N>1,\nissue a throwaway \"cache touch\" with the exact prefix attempt 1 opened\nwith. That call is itself ~99.9% cached -- 31,172 prompt, 31,153 cached,\nso ~19 billable tokens plus a minimal completion -- and it converts the\nattempt's 24k-42k cold start into near zero.\n\nCAVEAT worth weighing before building it: most-recent-prefix matching is\nNOT documented anywhere. It is inferred from measurement and could\nchange without notice. A re-prime call is cheap and harmless if the\nbehaviour changes (it just stops helping), but it is one extra request\nper attempt and it encodes an assumption about provider internals. The\nalternative -- having attempt N continue attempt N-1's conversation\nrather than rebuilding it -- needs no provider assumption at all, but\nattempts deliberately reset the workspace, so that is a bigger change.\nDECAY PROBE COMPLETED, and its tail is unexplained.\n\n    [1m..20m]  keyed 5169/5226   unkeyed 0\n    [30m]      keyed    0/5226   unkeyed 0\n    [45m]      keyed    0/5226   unkeyed 0\n    [60m]      keyed    0/5226   unkeyed 0\n    [90m]      keyed    0/5226   unkeyed 0\n\nIt never recovered. Every probe re-sends the same prefix WITH the key,\nso each should re-prime and the next should hit -- which is exactly what\nthe re-prime experiment showed happening within seconds. Instead the key\nwent cold at 30 minutes and stayed cold for an hour of continued keyed\ntraffic.\n\nSo there are now three measured behaviours:\n  1. keyed prefixes hit reliably at short range        (reproduced many times)\n  2. only the MOST-RECENTLY-USED prefix is matched     (reproduced deterministically)\n  3. a key can stop hitting entirely and not recover   (observed once, unexplained)\n\n(1) and (2) are solid. (3) contradicts a simple model of (1) and (2) and\nhas n=1. It may be backend rotation, a per-key lifetime, or the unkeyed\ncontrol call in the probe interfering.\n\nBEARING ON THE RE-PRIME FIX: the fix targets a gap of seconds between\nattempts, which sits well inside where behaviour (1) holds, and the\nreproduction was unambiguous. But (3) means none of this should be\ndepended on -- a re-prime must be best-effort, cost nothing when it\nmisses, and the tier budget must still be sized as though the cache is\nabsent. Anything that only works when the cache hits will fail in\nwhatever regime (3) represents.\nWHOLE-RUN COST BREAKDOWN, comms/hamlib 2026-09-04, 600K tier.\nFour attempts, agent_fixed, 451,738 billable total:\n\n   A  turns   prompt_in     out   billable  cache%   worst turn\n   1     30   1,540,378  12,112     80,797   95.5%   32,146 (T1)\n   2     30   1,563,631  23,907     67,348   97.2%    6,348 (T8)\n   3     30   1,750,607  55,743    215,730   90.9%   83,111 (T25)\n   4     27   1,337,447  25,546     87,863   95.3%   33,432 (T1)\n\nEvery cache miss in the run:\n\n   triage T1  in=16296 (cached=0)  billable=18817\n   A1.T1      in=32058 (cached=0)  billable=32146   cold start\n   A3.T1      in=32928 (cached=0)  billable=33019   cold start\n   A3.T25     in=78758 (cached=0)  billable=83111   <- mid-attempt\n   A4.T1      in=33264 (cached=0)  billable=33432   cold start\n\nThree cold starts at ~32-33k = ~99k, 22% of the run, spent purely\nre-establishing a prefix. Add the T25 eviction and roughly 40% of the\nrun's billable went to cache behaviour.\n\nNEW, AND IT EXTENDS THIS BEAD: A3.T25 is a MID-ATTEMPT eviction. This\nbead says auto-caching within an attempt is real -- \"it is why we see\n99% hits between consecutive turns of one attempt\". That holds on\naverage (90.9-97.2% here) but it is not guaranteed: turn 25 of 30, on\nan unbroken conversation, came back cached=0 and re-billed the whole\n78,758-token prefix. One turn, 83,111 billable, 39% of that attempt.\n\nSo the exposure is not only \"each attempt pays a cold start\". Any turn\ncan drop, and the later it drops the more it costs, because the prefix\nit re-bills has grown. A 30-turn attempt is 30 chances to lose the\naffinity coin-flip, and prompt_cache_key is what would stop it.\n\nNote also A2, the only attempt with NO cold-start miss -- it inherited\nA1's warm cache and cost 67,348 against A1's 80,797. Net of cold\nstarts A2 is actually the more expensive of the two (67,348 vs\n48,651), so the apparent saving was cache luck, not attempt economics.\nWorth remembering when reading any per-attempt cost comparison: with a\n32k cold start on a 600k budget, cache noise is larger than most\neffects being measured.\nVERIFIED ON HARDWARE 2026-09-04, comms/hamlib. Closing.\n\nThis bead named its own test: \"check that attempt 2 turn 1 reports a\nlarge cached_tokens instead of 0. That single number is the whole\nbead.\" It passed:\n\n  A1.T1  in=32058  (cached=0)      billable=32146   expected, first request\n  A2.T1  in=32867  (cached=31985)  billable=989     <- the test, passed\n  A3.T1  in=32928  (cached=0)      billable=33019\n  A4.T1  in=33264  (cached=0)      billable=33432\n\nImplementation is complete: 5d4be30 on master, deployed, 13 tests in\ntests/test_llm_prompt_cache.py. Both traps handled -- _merge_call_kwargs\nmerges extra_body rather than clobbering the reasoning payload, and both\nparameters go through extra_body because SDK 1.70.0 rejects them as\nkwargs. provider=meta, llm.backend=auto, so the SDK path is taken and\nthe key is on the wire.\n\nBUT THE BENEFIT IS PARTIAL, ~1 HIT IN 3 RETRIES. When it lands it is\nemphatic (33,019 -> 989 billable, 97%). Two of three retries still paid\na full cold start, seconds after the previous attempt ended. Three\nthings narrow it: the misses are TOTAL, not partial, so it is routing\nrather than prefix divergence -- a divergence would still hit the shared\n~32k of system+payload; timing is not it, the gaps were seconds against\na decay probe that held 98.9% through 20 minutes; and nothing of ours\nwas competing, no other LLM traffic between 17:51 and 18:21.\n\nThis is consistent with poly-b05, which is already closed: its revert\nfound a BYTE-IDENTICAL repeat missing too. Whatever causes the miss is\nprovider-side routing the key does not reliably control. There is no\nfurther code fix available on our side, which is why this closes rather\nthan staying open.\n\nKNOWN DEVIATION, recorded not actioned: the DESIGN above says \"one key\nper role, e.g. polytropos-patch\". The code ships polytropos-{model}, and\nall three roles (triage, patch, chat) are configured to the same model,\nso they share one key and can evict each other under an interleaved\nfeeder. Not fixed because poly-b05's byte-identical-repeat result says\nprefix contention is not the mechanism, so separating the keys would not\nbuy what this bead is about. Revisit only if a measurement implicates\nrole contention specifically.\n\nNEW FAILURE MODE, see the note above: the cache can drop MID-attempt.\nA3.T25, turn 25 of 30 on an unbroken conversation, came back cached=0\nand re-billed 78,758 tokens for 83,111 billable. This bead assumed\nwithin-attempt caching was reliable (\"99% hits between consecutive turns\nof one attempt\"). It is not.\n\nCONSEQUENCE, and where the remaining value is: if each attempt is an\nunavoidable ~33k dice roll, the lever is FEWER ATTEMPTS. This run paid\n98,597 in cold starts plus the 83,111 eviction -- 40% of its 451,738\nbillable -- because it needed four attempts, all four of which died on\nthe TURN cap with ~150k of budget unspent. Raising max_turns is the\nactionable follow-on.","owner":"github@dragonflybsd.org","priority":1,"status":"closed","title":"the prompt cache is unreachable across requests: no prompt_cache_key is sent, so every attempt re-pays its cold context","updated_at":"2026-09-04T20:27:31Z"}
{"_type":"issue","close_reason":"Closed","closed_at":"2026-09-04T15:15:39Z","comment_count":0,"created_at":"2026-09-02T18:53:37Z","created_by":"Fred [bot]","dependency_count":0,"dependent_count":0,"description":"`github.deliver` drives a single shared `delivery.clone_dir` through\ncheckout -> apply -> commit -> push -> restore. The Accept endpoint is a\nsync FastAPI route, so uvicorn runs each request in its own threadpool\nthread. Nothing serialises them. Two Accepts a few seconds apart overlap\nin the clone.\n\nObserved 2026-09-02. Clone reflog:\n\n    18:45:47  checkout: master -> agentic/irc-ircd-ratbox-2026Q3-...\n    18:45:48  commit: irc/ircd-ratbox: fix build failure on DragonFly\n              <- 18:45:50.93 math_arpack-ng@openmpi Accept refuses here\n    18:45:53  reset: moving to HEAD          } restore_to_base\n    18:45:54  checkout: ...ratbox -> master  }\n\nbundle_review_requests rows bracket it: id=19 arpack-ng create_failed at\n18:45:50.937 with\n\n    GitWrongBranch: clone is on branch 'agentic/irc-ircd-ratbox-2026Q3-...',\n    expected base_branch 'master'\n\nand id=20 ircd-ratbox created at 18:45:54.294. The push is the slow leg\n(~5s) and is the window.\n\nThe refusal is the guard working, not the bug. `prepare_clean_branch`\nasserting clean+on-base is the only thing that stopped arpack-ng's diff\nbeing committed onto ircd-ratbox's branch and force-with-lease pushed\nthere, silently corrupting an already-accepted PR. The bug is that a\nshared mutable resource has no mutex.\n\nRecovery today is a plain retry once the clone is idle: the finally-block\nrestore does run, so the clone returns to base on its own.","design":"Serialise deliveries on the clone. Candidate shapes, simplest first:\n\n(a) module-level threading.Lock keyed by clone_dir in the git driver.\n    Correct for today's deployment: one uvicorn process, no --workers.\n    Smallest diff. Does not cover a second process (a CLI-driven\n    delivery, or a future worker count > 1).\n\n(b) fcntl.flock on a lockfile inside the clone, wrapping github.py's\n    whole prepare -> push -> restore span, bounded wait so a hung\n    delivery reports \"another delivery holds the clone, retry\" rather\n    than blocking the request thread. Covers any process. clone_dir is\n    already documented as dedicated to delivery, so it is the natural\n    mutex key.\n\n(c) queue accepts and deliver from one worker. Largest change; only\n    worth it if delivery grows other shared state.\n\nNot yet chosen -- pick the simplest that actually holds. Whichever wins,\nthe lock must span the restore, not just the prepare: the window that\nbit us was between commit and restore.","id":"poly-lt1","issue_type":"bug","labels":["concurrency","delivery"],"notes":"Implemented: one module-level threading.Lock in orchestrator, held\nacross the whole provider.create_review_request call.\n\nChosen over the file lock originally sketched, on facts: deliver() has\nexactly one caller (the Accept route) and uvicorn.run() is invoked\nwithout `workers`, so every delivery is a thread in one process. A\ncross-process lock would defend against a process that cannot exist\ntoday.\n\nBounded wait, not blocking: anyio's default thread limiter is 40\n(verified in the installed anyio), so an unbounded wait behind one hung\ndelivery would park request threads until the tracker stops answering.\nWaits delivery.git_timeout -- an existing setting, no new knob and no\nenv var -- which is 60s against a measured 7s healthy delivery.\n\nThe timeout raises DeliveryError from inside deliver()'s existing try,\nso a busy clone records the same create_failed row as any other\nprovider failure, with a message saying to retry.\n\nLock spans the HTTP calls too, necessarily: restore_to_base runs in\ncreate_review_request's finally, i.e. after the PR is created, so the\nclone is still on the feature branch during those calls.\n\nTests: tests/test_delivery_clone_lock.py (6). The last one reproduces\nthe incident against real git -- two deliveries started on a barrier\nagainst one real clone -- and fails 5/5 runs with the lock removed.\n\nResiduals, deliberately not coded for:\n- If the tracker process dies mid-delivery the lock dies with it and\n  the clone can be left on the feature branch. That is a crash, not a\n  race; the next Accept refuses, which is the safe direction.\n- If anyone adds uvicorn `workers` or a second delivering process, the\n  in-process lock stops being sufficient. The assumption is stated in\n  the comment above the lock.\nREVISITED 2026-09-04. The mutex described in NOTES is present, correct,\nand working. What is still hurting is a different problem underneath it,\nand the bead title no longer describes it.\n\nMEASURED, the 2026-09-04 11:14-11:16 accept burst:\n\n  delivered ok      12\n  clone-timeout     14\n\nGaps between consecutive SUCCESSFUL deliveries, in seconds:\n\n  6.7  7.7  6.6  7.1  6.8  7.0  7.0  8.2  7.5  7.5  7.1\n\nThat is the lock doing exactly its job: one delivery at a time, ~7s\neach, no interleaving, no GitWrongBranch. The original race is fixed.\n\nWHAT ACTUALLY FAILED\n\nNot a race. Queueing past a deadline. _clone_locked waits\ndelivery.git_timeout (60s) and then raises. At ~7s per delivery the\nclone serves roughly eight in that window; an operator accepting ~26 in\none sitting pushes the rest past 60s and every one of those is recorded\nas create_failed. The first failure lands at 11:15:12.699, ~60s after\nthe burst began, and the 14 failures interleave with ongoing successes\nat 11:15:15 / 11:15:22 / 11:15:30 -- the signature of a queue draining\nwhile latecomers time out, not of contention.\n\nSO THE RECORD IS WRONG\n\n`create_failed` means the provider was asked and refused. Here the\nprovider was never asked: the delivery did not fail, it was never\nattempted. That mislabelling is what stranded 14 accepted bundles with\nno PR (poly-86t) and made an operator think 14 fixes had been rejected\nupstream.\n\nTHE REAL CONSTRAINT\n\nDelivery is synchronous inside the Accept request and serialised, so\naccept latency is O(queue depth x 7s) with a 60s cliff. Two consequences\nbeyond the failures:\n\n  - Each waiting accept parks a Starlette threadpool thread. anyio's\n    default limiter is 40 (the figure this bead's own NOTES cite as the\n    reason the wait is bounded). A 26-accept burst holds ~26 of them.\n  - Raising delivery.git_timeout is not the fix: it is shared with the\n    git subprocess timeouts one layer down, so loosening the lock wait\n    also loosens those.\n\nCANDIDATE FIXES, given the mutex is already in place\n\n  (a) A separate, larger bound for the lock wait only. Smallest change,\n      does not conflate with git_timeout. Still parks request threads\n      and still has a cliff, just a further-away one.\n  (b) Stop recording a busy clone as create_failed. It is a distinct\n      outcome -- not attempted -- and with poly-86t's retry now shipped\n      the operator has an exit. Honest record, no throughput gain.\n  (c) Make delivery asynchronous: Accept records the intent, a poller\n      delivers. This is exactly the verify_requests shape already in\n      the codebase (tracker INSERTs a row, runner polls and acts), and\n      poly-fij notes that shape also lets a remote runner serve it.\n      Removes the cliff and the parked threads entirely. Costs Accept's\n      inline delivery outcome, which the UI currently surfaces.\n\n(c) is the one that actually removes the failure mode; (a)+(b) together\nare the cheap version that stops the mislabelling and moves the wall.\nNot choosing here -- the decision changes Accept's contract.\nCLOSING. The race is fixed and now verified on production data.\n\nThe threading.Lock recorded in the earlier note is present\n(orchestrator.py:63) and demonstrably working: across the 2026-09-04\naccept burst, successful deliveries are spaced 6.7 7.7 6.6 7.1 6.8 7.0\n7.0 8.2 7.5 7.5 7.1 seconds apart -- strictly one at a time, no\ninterleaving, and no GitWrongBranch since 2026-09-02.\n\nOne change landed here today, because it belonged to this bead's\nmechanism: the wait had been borrowing delivery.git_timeout, which\nbounds a single git subprocess. They are different quantities that\nhappened to share the number 60. Split out as\ndelivery.clone_wait_timeout (300s), so the queue an operator can build\nby accepting in bulk went from about eight deep to about forty, without\nloosening every git call to get there.\n\nWhat this bead is NOT about, and is now poly-vxx: a clone that is busy\npast that bound is still recorded as create_failed -- a provider failure\nthe provider never saw -- and delivery is still synchronous inside the\nAccept request, so the wall moved rather than went away.","owner":"github@dragonflybsd.org","priority":1,"status":"closed","title":"Two concurrent Accepts race on the one delivery clone; the loser fails with GitWrongBranch","updated_at":"2026-09-04T15:15:39Z"}
{"_type":"issue","comment_count":0,"created_at":"2026-09-02T18:02:29Z","created_by":"Fred [bot]","dependencies":[{"created_at":"2026-09-02T20:02:29Z","created_by":"Fred [bot]","depends_on_id":"poly-3sq","issue_id":"poly-451","metadata":"{}","type":"related"},{"created_at":"2026-09-09T17:26:06Z","created_by":"Fred [bot]","depends_on_id":"poly-88k","issue_id":"poly-451","metadata":"{}","type":"parent-child"}],"dependency_count":0,"dependent_count":0,"description":"Two worklist ports have failed identically in every run: the patch job\nrefuses before the agent is invoked, so the port cannot be fixed by the loop at\nall.\n\n  patch refused: could not compose <origin> before starting. The compose tree\n  is shared across jobs, so proceeding would work against whatever the previous\n  job left there. Underlying error:\n  extra: {\"rc\": 2}\n\nREPRODUCTIONS\n\nFour, across two runs about eight hours apart, on exactly the two worklist\nports that have no overlay.dops in DeltaPorts:\n\n  databases/libvalkey   run 1 and run 2\n  sysutils/ucored       run 1 and run 2\n\nEvery other worklist port with an existing overlay passed this preflight. The\ncorrelation is with \"no overlay\", not with load: run 2 happened with the queue\notherwise idle, after concurrent build activity had been stopped.\n\nSEQUENCE\n\n  1. triage runs normally and classifies the failure\n  2. triage bootstraps a `type=port` header overlay (new_port) because none\n     exists -- \"proceeding to patch\"\n  3. the patch job's preflight calls materialize_dports (steps.py, \"Compose\n     this one origin BEFORE the agent touches anything\")\n  4. that returns rc=2 and the job refuses by design: \"a compose that did not\n     work means we do not know what is on disk\"\n\nThe refusal is correct behaviour given a failed compose. The bug is that the\ncompose fails at all, deterministically, for a port whose overlay the harness\nitself just created.\n\nWHAT THE COMPOSE ACTUALLY SAYS\n\nReproduced by hand, and the two ports do not fail the same way:\n\n  sysutils/ucored      rc=2   [fail] preflight_validate: changed=0 skipped=0\n                              warnings=0 errors=1\n  databases/libvalkey  rc=1   Traceback ... io.open(self, mode, buffering,\n                              encoding, errors, newline)\n\nSo there is at least a validation failure and an unhandled exception behind the\nsame refusal, and neither reaches the operator: the message interpolates\n`stderr_tail or error`, both empty, so \"Underlying error:\" prints nothing and\nonly rc survives into extra.\n\nThe bootstrapped overlay is a bare header with no ops:\n\n  port databases/libvalkey\n  type port\n  reason \"Step 48 bootstrap: deterministic header; patch authors the body\"\n  target @any\n\nNOT THE MISLABELLING BEAD\n\npoly-e75 / poly-jg9 cover the verdict: any harness refusal is recorded as\nagent_gave_up, with no patch.md and no patch_audit.json, which reads as \"the\nagent tried and could not fix it\". That is a separate defect from this one and\nthey are independently fixable:\n\n  fix only the label  -> these ports stay permanently unfixable, honestly named\n  fix only this       -> they get a real patch attempt, while provider outages\n                         keep being blamed on the agent\n\nRELATED\n\npoly-3sq -- bootstrap overlay stubs left behind in the shared DeltaPorts\ncheckout when a job does not land. Same bootstrap path; these refusals are one\nway a job fails to land. 38 such untracked stubs were counted in the checkout\ntoday, and they block `dev-env update` from fast-forwarding it.","id":"poly-451","issue_type":"bug","notes":"DIAGNOSIS CORRECTED 2026-09-03 by validating the description against the\ncode. The description's central claim -- \"the compose fails at all,\ndeterministically, for a port whose overlay the harness itself just\ncreated\" -- is WRONG, and the two different rc values it reports were a\nred herring.\n\nCompose does not fail on these ports. Run right now, through the exact\ncode path the preflight uses (worker.materialize_dports, which is just\n`reapply <origin>`):\n\n    sysutils/ucored      ok=True rc=0\n    databases/libvalkey  ok=True rc=0\n\nBoth succeed. Nothing about the bootstrapped header is unacceptable to\ncompose.\n\nTHE ACTUAL MECHANISM: the overlay is not there when the patch job looks.\n\n  PORTS_DIR      = /work/DeltaPorts   (a SYMLINK)\n  PORTS_MAIN_DIR = /work/ports-main   (the shared checkout)\n\n  1. Triage has NO worktree. _checkout_bundle_branch_for_job has exactly\n     one call site, runner.py, and it passes job_type=\"patch\". So triage\n     runs with /work/DeltaPorts -> ports-main.\n  2. _ensure_overlay_or_abort writes the header with\n     worker.put_file(env, \"/work/DeltaPorts/ports/<origin>/overlay.dops\")\n     -- landing in ports-main, UNTRACKED and uncommitted.\n  3. The patch job calls create_job_worktree, which does\n     `git worktree add` a fresh tree from the base branch and then\n     _point_ports_link repoints /work/DeltaPorts at that worktree.\n  4. `git worktree add` does not carry untracked files. The overlay the\n     harness just wrote is not in the new tree.\n  5. materialize_dports composes a port with no overlay -> fails ->\n     preflight refuses.\n\nVerified, not inferred:\n  - the ports link currently resolves to ports-main (which is exactly\n    why a manual reapply succeeds and the real job does not)\n  - git status in ports-main shows the bootstrapped overlays untracked:\n        ?? ports/databases/libvalkey/\n        ?? ports/misc/libuuid/\n        ?? ports/sysutils/ucored/\n  - 42 untracked stubs in that checkout now\n\nSO poly-3sq IS THE SAME BUG, seen from the other end. The stubs are not\n\"left behind when a job does not land\"; they are orphaned by design at\nstep 3 of every single bootstrap, because the writer and the consumer\nlook at different trees. Fixing this fixes the accumulation too.\n\nAlso corrected: the description says this reproduces on \"exactly the two\nworklist ports that have no overlay.dops\". The population is wider --\nmisc/libuuid hit it tonight as a third. It is every port with no\noverlay, i.e. every port the bootstrap path exists to serve.\n\nWHY THE ORIGINAL HAND-REPRODUCTION SHOWED rc=2 AND rc=1: not\nreinvestigated, and no longer load-bearing. Those runs were made before\nthe poly-cpd compose fix landed. The rc values in the description should\nnot be treated as evidence of anything.\nIMPLEMENTED. The patch job now establishes its own overlay state, which\nis the same principle the compose preflight already states: \"each\nattempt responsible for its own starting state instead of trusting an\nordering that spans two modules\".\n\n  worker.ensure_bootstrap_overlay(env, origin) -- writes the\n  deterministic header if the tree PORTS_DIR points at right now has no\n  overlay. Idempotent, so a no-op for every port that never needed a\n  bootstrap. Called from the patch preflight between assert_port_clean\n  and materialize_dports.\n\n  worker.BOOTSTRAP_REASON / worker.read_status_type -- moved out of\n  runner so triage and the preflight emit a byte-identical header from\n  one source. Read through runner's existing function-level `worker`\n  import; a module-level one would have been the only such import in\n  that file.\n\nDELIBERATELY BEST-EFFORT. The first cut refused the job when the\nbootstrap failed, and four runner-parity tests caught it: that adds a\nSECOND refusal path for a condition the compose already gates, so an\nunreachable env would start failing with a different message than the\none operators know. It now records what happened\n(patch_overlay_bootstrapped / patch_overlay_bootstrap_failed) and lets\nthe compose arbitrate. Those four tests then passed unmodified, which is\nthe signal the design was wrong rather than the tests.\n\nTests: tests/test_patch_bootstrap_overlay.py (6) plus 4 ordering tests\nin test_patch_preflight_compose.py. Mutation-verified both ways --\nremoving the preflight call fails 3, and removing the overlay_dops guard\n(so it clobbers real overlays) fails 1. Suite 2972 passed.\n\nSTILL OPEN, and unchanged by this: triage keeps writing its bootstrap\ninto the shared checkout, where it stays untracked and orphaned. That is\npoly-3sq's accumulation (42 stubs counted now, up from 38). This fix\nmakes the orphan harmless rather than fatal; whether triage should stop\nwriting entirely is the poly-3sq decision, not this one.\n\nNOT YET VERIFIED ON HARDWARE. The three ports that hit this tonight\n(databases/libvalkey, sysutils/ucored, misc/libuuid) are the real test\nand need a deploy first.\nVERIFIED ON HARDWARE 2026-09-03, after deploying a41d8bb and re-raising\nthe three ports that hit this.\n\nAll three success criteria met:\n\n  patch_preflight_compose_failed   0   (was 3 of 3)\n  patch_overlay_bootstrapped       3   (\"re-established type=port\n                                        bootstrap overlay in this job's\n                                        worktree\")\n  llm_turns on the patch job       21 / 30 / 49   (was 0 / 0 / 0)\n\nThe model had never once been invoked for any of these ports across\nthree prior runs. It now is.\n\n  databases/libvalkey   agent_gave_up (never invoked) -> agent_fixed, 21 turns\n  misc/libuuid          agent_gave_up (never invoked) -> agent_fixed, 30 turns\n  sysutils/ucored       agent_gave_up (never invoked) -> agent_budget_exhausted, 49 turns\n\nBoth fixes carry the full artifact set -- analysis/changes.diff,\nanalysis/patch.md, analysis/rebuild_proof.json -- so they are real\nattempts, not empty terminal states. Both are unverified and sit in\n\"Needs verify\" for an operator.\n\nucored is not a regression: it got a genuine attempt and ran out of\nrunway at 49 turns, which is the tier budget problem measured on\ndevel/nspr the same night (120k funds ~1.4 attempts against a nominal\nmax_iterations of 4), not a failure of this fix.\n\nWorklist moved: Needs verify 6 -> 8, Needs a decision 19 -> 17.\n\nTwo of the ports the worklist had been reporting as three failed agent\nattempts each were fixable on the first real attempt.","owner":"github@dragonflybsd.org","priority":1,"status":"open","title":"the bootstrapped overlay is written to the shared checkout, then the patch job composes in a fresh worktree that does not have it","updated_at":"2026-09-03T00:35:08Z"}
{"_type":"issue","close_reason":"A failed verification now leaves three durable records: analysis/verification.log uploaded by the orchestrator, and verification_exit_code + verification_reason persisted on the bundle so the page can say what happened without fetching an artifact. The reason is composed from apply-and-build's own stage exits and stderr tail. The summary is gated on verification_status instead of verification_at, so a failed bundle stops claiming it was verified.","closed_at":"2026-09-05T20:47:29Z","comment_count":0,"created_at":"2026-09-02T16:44:07Z","created_by":"Fred [bot]","dependencies":[{"created_at":"2026-09-06T01:55:35Z","created_by":"Fred [bot]","depends_on_id":"poly-0e02","issue_id":"poly-9az","metadata":"{}","type":"relates-to"},{"created_at":"2026-09-02T18:44:06Z","created_by":"Fred [bot]","depends_on_id":"poly-8ni","issue_id":"poly-9az","metadata":"{}","type":"discovered-from"}],"dependency_count":0,"dependent_count":1,"description":"Verification is the only failure path in the pipeline that keeps no\nevidence. An operator can see THAT verify failed and never why.\n\nWHAT IS STORED\n\nPOST /api/bundles/<id>/verification writes three columns: verification_status,\nverification_at, verification_applied_diff_sha256. There is no column for a\nreason, an exit code, or a log reference.\n\n`dsynth_exit` IS accepted in the request body -- documented as \"optional, kept\nfor audit\" -- and is then forwarded only into the bundle_verified SSE event\npayload. It is never persisted, so nothing can query or render it.\n\nThe endpoint docstring names the missing piece itself:\n\n  \"The dsynth log itself is not stored here -- Slice 3 may upload it as a\n   bundle artifact (e.g. analysis/verification.log) via the artifact-store\n   endpoint independently.\"\n\nThat slice never landed.\n\nWHY THE EVIDENCE IS GONE BY THE TIME ANYONE LOOKS\n\nTwo perishable copies, no durable one:\n\n- dev-env apply-and-build writes work/artifacts/apply-and-build-<origin>.log\n  and verify-fix deletes it on success. Sampling every env after two failed\n  verifies found none present.\n- dsynth's per-port log under work/dsynth/logs/ is overwritten by the next\n  build of that origin.\n\nEstablishing why devel/libunwind failed verification took an ssh session and a\nread of a log the next build would have destroyed. It had to be done twice.\nThe answer -- check-sanity rejecting the port over ineffective options helpers,\nnothing to do with the fix under test -- was worth having: it was poly-cpd, and\nthe loop had been patching the wrong file for five bundles because of it.\n\nA CONTRADICTION IN THE SUMMARY\n\n_bundle_summary.html renders\n\n  {% if bundle.verification_at %}\n  <span><span class=\"k\">verified</span> {{ bundle.verification_at }}</span>\n  {% endif %}\n\nverification_at is set on BOTH outcomes -- the endpoint writes it whether ok is\ntrue or false. So a failed bundle shows the red \"verify failed\" pill from\nfix_status() and, two lines away, \"verified 2026-09-02T13:38:13\". The wrong one\nlooks authoritative because it carries a timestamp.\n\nSHAPE OF THE FIX\n\n- Upload the apply-and-build log as analysis/verification.log on failure. The\n  endpoint already names the path; the artifact store already takes uploads.\n- Persist the exit code and a short reason next to verification_status so the\n  bundle page can say what happened without fetching an artifact.\n- Gate the summary line on verification_status, not verification_at, so a\n  failed verification stops claiming it was verified.\n\nVerification is the gate an operator accepts a fix on. It is the one place\nwhere \"it failed, work out why yourself\" is most expensive.","id":"poly-9az","issue_type":"bug","notes":"IMPLEMENTED 2026-09-05. All three parts of the bead's fix shape.\n\n1. DURABLE LOG. verify_fix uploads the apply-and-build log as\nanalysis/verification.log on failure only, before POSTing the verdict.\nBest-effort: a failed upload must not lose the verdict, so it warns and\ncarries on. The endpoint's docstring had named this exact path as\nsomething 'Slice 3 may upload'; it never landed, which is why a failed\nverify kept no evidence at all.\n\n2. EXIT CODE AND REASON PERSISTED. Two new bundles columns\n(verification_exit_code, verification_reason) plus MIGRATIONS entries,\nverified against a database created before them. dsynth_exit was already\naccepted and documented as 'kept for audit', then forwarded only into the\nSSE payload -- accepted and dropped. The reason is READ OFF the\napply-and-build result rather than guessed: it reports which stage\nrefused (git apply / reapply / dsynth) and a stderr tail, so\n_failure_reason composes 'dsynth exited 1: check-sanity: ineffective\noptions helper' -- which is exactly the answer that cost an ssh session\nto find. Bounded at 2000 chars server-side; the column holds a reason,\nnot a log.\n\n3. THE SUMMARY NO LONGER CONTRADICTS ITSELF. _bundle_summary.html gated\non verification_at, which the endpoint writes on BOTH outcomes, so a\nfailed bundle showed the red 'verify failed' pill and 'verified\n<timestamp>' two lines apart. Now gated on verification_status, and the\nfailed branch shows the exit code inline.\n\nALSO: made _put_artifact an injectable hook on run_verify_fix, matching\n_get_json/_get_bytes/_post_json. Without it the existing failure-path\ntests would have attempted real HTTP to the fake tracker URL.\n\nNine tests: four on the orchestrator (uploads on failure, uploads nothing\non success, a failed upload keeps the verdict, the reason names the\nstage), three on the endpoint (exit code and reason persisted, reason\nbounded, no false 'verified' in the summary), plus the two updated.","owner":"github@dragonflybsd.org","priority":1,"status":"closed","title":"a verify failure records no reason anywhere, and the bundle summary still says 'verified' when it failed","updated_at":"2026-09-05T20:47:29Z"}
{"_type":"issue","comment_count":0,"created_at":"2026-09-02T13:01:22Z","created_by":"Fred [bot]","dependencies":[{"created_at":"2026-09-02T15:01:21Z","created_by":"Fred [bot]","depends_on_id":"poly-8ni","issue_id":"poly-qkp","metadata":"{}","type":"discovered-from"},{"created_at":"2026-09-09T17:26:02Z","created_by":"Fred [bot]","depends_on_id":"poly-zze","issue_id":"poly-qkp","metadata":"{}","type":"parent-child"}],"dependency_count":0,"dependent_count":0,"description":"poly-8ni gave the agent dsynth_test so it could reach the acceptance\ngate. It does. The refusal is then discarded, because reporting it depends on\nthe model emitting a Patch Log it may never reach.\n\nOBSERVED\n\ndevel_libunwind-20260902-124214Z, first run after poly-8ni deployed:\n\n  turn 19  dsynth_build  rebuild_ok=true\n  turn 22  dsynth_test   rebuild_ok=false   <- gate refused\n  turn 23  dsynth_log\n  turns 24-30  get_file / grep / list_dir   <- diagnosing the refusal\n  turn 30 = max_tool_turns (patch.py)\n\nThe attempt hit the turn cap mid-investigation and never emitted a final\nresponse. analysis/patch.md is 205 characters of mid-reasoning. The\nproof-block orphan rescue in attempt_loop then lifted rebuild_ok=true from\nthe turn-19 dsynth_build result and synthesized\n\n  {\"rebuild_ok\": true, \"source\": \"tool_result\"}\n\nso the bundle resolved agent_fixed with no record anywhere that the gate had\nrefused it. Verify will refuse it again, for the same defect, with the loop's\nown evidence already discarded.\n\nWHY THE PROMPT IS NOT ENOUGH\n\nThe patch prompt asks the agent to name a gate refusal in Patch Log and stop.\nThat instruction can only run if the model gets a turn to write the report.\nHere the gate landed at turn 22 of 30 and the refusal was ambiguous enough to\ninvestigate -- reasonable behaviour, since the agent cannot tell immediately\nwhether the failure is its own. Any instruction whose execution competes with\nthe turn budget will be dropped exactly when the news is most interesting.\n\nThe harness does not have this problem. The dsynth_test result is in the tool\ntrace; nothing needs to be remembered or re-derived.\n\nFIX DIRECTION\n\nRecord the gate outcome harness-side, next to rebuild_ok:\n\n- track dsynth_test results in the tool loop the way rebuild_ok_seen already\n  tracks dsynth_build\n- carry it into the proof dict on both paths -- the model-authored proof block\n  and the synthesized orphan-rescue one -- so analysis/rebuild_proof.json\n  gains a gate field that is present regardless of what the model wrote\n- surface it in the tool-trace summary so the next attempt's payload shows the\n  gate refused, not just that a build passed\n\nThen the prompt instruction becomes a nicety rather than the mechanism.\n\nA prompt guard capping post-gate investigation is worth adding too, but on its\nown it only narrows the window; it does not close it.\n\nNOTE ON SCOPE\n\nThis does not change what agent_fixed means -- see poly-8ni for why requiring\na green gate is not yet safe. It makes a refusal visible to the operator and\nto the next attempt.","id":"poly-qkp","issue_type":"bug","owner":"github@dragonflybsd.org","priority":1,"status":"open","title":"a dsynth_test refusal is lost when the attempt hits the turn cap, so an observed gate failure still reports agent_fixed","updated_at":"2026-09-02T13:01:22Z"}
{"_type":"issue","comment_count":0,"created_at":"2026-09-02T12:16:26Z","created_by":"Fred [bot]","dependencies":[{"created_at":"2026-09-02T14:16:26Z","created_by":"Fred [bot]","depends_on_id":"poly-cpd","issue_id":"poly-8ni","metadata":"{}","type":"discovered-from"},{"created_at":"2026-09-09T17:26:00Z","created_by":"Fred [bot]","depends_on_id":"poly-zze","issue_id":"poly-8ni","metadata":"{}","type":"parent-child"}],"dependency_count":0,"dependent_count":0,"description":"The agent's success oracle and the acceptance gate run different\nbuilds, so a bundle can be resolved agent_fixed and then fail verification for\na defect the loop had no way to observe.\n\nMECHANISM\n\nPatch loop: worker.dsynth_build runs `dsynth build` directly with -S -y (its\ndocstring says dbuild is intentionally not used). rebuild_ok comes from that\nexit status, and rebuild_ok is what marks a bundle agent_fixed.\n\nVerify gate: dev-env cli.py apply_and_build step 3 runs `dtest`, i.e.\n`dsynth test`. Per helpers.py that \"exercises the extra Q/A phases (stage-qa,\ncheck-plist, the test target) AND force-rebuilds by removing any existing\npackage first\". It also runs with DEVELOPER=1.\n\nDragonFly's Mk/bsd.sanity.mk gates its whole DEV_ERROR block on\n`.if defined(DEVELOPER)`. So a class of Makefile defects is fatal under the\ngate and invisible to the loop.\n\nFIRST CONFIRMED INSTANCE\n\ndevel/libunwind-20260902-103058Z. Patch agent: rebuild_ok=true, one attempt,\nresolution agent_fixed. Verify: two runs, both `dsynth stage exit=1`, both\ndying at check-sanity:\n\n  The following options helpers are incorrectly set after bsd.port.options.mk\n  and are ineffective: TESTS_CONFIGURE_OFF TESTS_CONFIGURE_ON\n\nThe underlying Makefile defect is poly-cpd (mk eval appended after the options\ninclude) and is pre-existing -- the agent's diff only adds a static patch file\nand one `file materialize` line. The build dies before the patched file is ever\ncompiled, so the fix was neither proved nor disproved.\n\nWHY THE AGENT CANNOT SEE IT\n\nNo agent tool runs `dsynth test`. The 18-tool surface has dsynth_build and\ndsynth_log only. The loop cannot reach the gate it is judged by, and the\nverify failure is not fed back into the bundle, so a re-attempt starts from the\nsame blind spot and would produce the same green result.\n\nFIX DIRECTIONS\n\n- Set DEVELOPER=1 in the agent's dsynth_build. Cheapest; surfaces DEV_ERRORs\n  during the loop. Does not cover the QA phases or the force-rebuild.\n- Add a dsynth_test tool and require it as the final proof for a fix the agent\n  wants to call done. Closes the gap properly; every attempt gets more\n  expensive, and stage-qa/check-plist failures become the agent's problem.\n- At minimum, write the verify failure back onto the bundle so a re-attempt\n  sees why acceptance was refused instead of re-deriving a green build.\n\nWorth deciding deliberately: making the loop's build identical to the gate is\nnot obviously right -- the cheap build is what makes iteration affordable.\nSurfacing the delta may be better than closing it.\n\nALSO\n\ndev-env cli.py apply_and_build's docstring says it runs `dbuild ORIGIN`; the\ncode runs `dtest`. Stale doc, same function.","id":"poly-8ni","issue_type":"bug","owner":"github@dragonflybsd.org","priority":1,"status":"open","title":"patch loop proves fixes with dsynth build but acceptance gates on dsynth test, so DEVELOPER/QA defects pass the loop and fail verify","updated_at":"2026-09-02T12:16:26Z"}
{"_type":"issue","comment_count":1,"comments":[{"author":"Fred [bot]","created_at":"2026-09-09T15:28:32Z","id":"bb05515a-a310-5023-89d2-c4c54c564f3e","issue_id":"poly-jg9","text":"poly-e75 closed into this: same defect, measured 2026-08-31 on 4 jobs (429/500/504/504)."}],"created_at":"2026-09-02T03:50:48Z","created_by":"Fred [bot]","dependencies":[{"created_at":"2026-09-02T05:50:48Z","created_by":"Fred [bot]","depends_on_id":"poly-3aa","issue_id":"poly-jg9","metadata":"{}","type":"related"},{"created_at":"2026-09-02T05:50:48Z","created_by":"Fred [bot]","depends_on_id":"poly-4av","issue_id":"poly-jg9","metadata":"{}","type":"related"},{"created_at":"2026-09-09T17:28:10Z","created_by":"Fred [bot]","depends_on_id":"poly-so8j","issue_id":"poly-jg9","metadata":"{}","type":"parent-child"}],"dependency_count":0,"dependent_count":0,"description":"`agent_gave_up` is recorded whether the agent gave up or the harness never\nreached the model. The two are indistinguishable afterwards, and the second\ncase is the common one.\n\nMEASURED 2026-09-02 across the full tracker history (136 bundles):\n\n  resolution            with analysis/patch.md   without\n  agent_fixed                    18                 0\n  agent_gave_up                   3                32\n  agent_budget_exhausted          1                 8\n\nArtifact retention is NOT the explanation. agent_fixed bundles have their\npatch.md on every day in the window, including the oldest (20260829). The\nabsence is specific to the give-up verdicts, and spans every day:\n20260830 0/14, 20260831 1/9, 20260901 0/8.\n\nA genuine give-up is a real agent output: the patch prompt asks for\n`## Rebuild Status: gave-up` plus a next-step recommendation in Patch Log, and\nthat lands in analysis/patch.md. A bundle marked agent_gave_up with no patch.md\nnever got that far.\n\nOBSERVED DIRECTLY tonight. archivers_lz4json-20260902-002241Z was marked\nagent_gave_up at 03:49. Its entire history is provider rate limiting - four\npatch attempts, each ending `Harness patch failed ... Error code: 429`. The\nagent did real work (make_patch, get_file, dupe, edit_file, genpatch) and never\nonce concluded anything. Its patch.md and patch_audit.json return\n`{\"error\": \"artifact not found\"}`.\n\nWHY IT MATTERS\n\nThe resolution is the only durable record of what happened. Anything reasoning\nabout agent quality reads it:\n\n- An operator triaging the queue sees \"the agent gave up on lz4json\" and\n  concludes the port is hard. It is not; the provider was down.\n- Any claim of the form \"the agent fails on N% of ports\" is unfounded, because\n  the denominator mixes model failure with infrastructure failure.\n- poly-3aa sampled the agent_fixed side and is unaffected, but the same style of\n  analysis applied to the give-up side would be measuring the provider.\n\nThis is adjacent to poly-4av (a transient provider error kills the job) but it\nis a different defect. poly-4av is about the job dying; this is about the\ndeath being misattributed to the agent in the permanent record.\n\nPROPOSED\n\nSeparate \"the agent concluded it could not fix this\" from \"we could not run the\nagent\". A distinct terminal resolution for infrastructure failure - provider\nerror, env broken, harness crash - would do it. The evidence to decide is\nalready present at the point the resolution is written: a give-up that produced\nno patch.md is not a give-up.\n\nDo not backfill silently. The 32 existing rows should be treated as unknown\nrather than relabelled, since the underlying cause is no longer recoverable for\nmost of them.\n","id":"poly-jg9","issue_type":"bug","labels":["agent","tracker"],"notes":"SECOND LIVE CONFIRMATION 2026-09-02 06:21.\n\nmath_xspread-20260902-043007Z resolved agent_gave_up. Its analysis/patch.md and\nanalysis/patch_audit.json both return {\"error\": \"artifact not found\"}. Its\nhistory is the same as lz4json's: triage completed, then patch attempts cut off\nby HTTP 429, never reaching a rebuild.\n\nTwo different ports, same night, same false verdict. Running count is 33 of 36\nagent_gave_up bundles with no patch.md, against 18 of 18 agent_fixed that have\none.\nSCOPE REFINEMENT 2026-09-02 06:30. Distinguishing misnamed from merely\nevidence-free, so the fix targets the right rows.\n\n  resolution               with report   without   name honest?\n  agent_fixed                  18           0      yes\n  agent_gave_up                 3          33      NO - asserts the agent decided\n  agent_budget_exhausted        1           8      partly - budget is a harness\n                                                   condition, and running out\n                                                   mid-attempt explains a missing\n                                                   report\n  triage_failed                 0           4      yes - says triage failed, does\n                                                   not claim the agent concluded\n                                                   anything\n\nThe defect is specifically agent_gave_up. It is the only verdict that attributes\na decision to the model while carrying no evidence the model ever spoke. 33 of 36.\n\ntriage_failed and agent_budget_exhausted are evidence-free too, but they name a\nharness condition rather than an agent judgement, so they mislead far less. They\ndo not need renaming; agent_gave_up does.\n\nNew data point: net-mgmt_ipv6mon-20260902-043028Z resolved triage_failed at 06:29\nstraight after `Harness triage failed ... Error code: 429`. Its analysis/triage.md\nreturns {\"error\": \"artifact not found\"}. Correctly named, and the honest contrast\nwith lz4json and xspread on the same night is what makes the agent_gave_up naming\nindefensible.\nTHIRD CONFIRMATION 2026-09-02 08:27. security_libfido2-20260902-043053Z\nresolved agent_gave_up after `Harness patch failed ... Error code: 429`.\nanalysis/patch.md returns {\"error\": \"artifact not found\"}.\n\nThree ports in one night - archivers/lz4json, math/xspread, security/libfido2 -\nall marked as the agent giving up, none of which reached the model's conclusion.\nRunning count 34 of 37, against 18 of 18 agent_fixed with a report.","owner":"github@dragonflybsd.org","priority":1,"status":"open","title":"A provider outage is recorded as agent_gave_up, so the verdict cannot be trusted","updated_at":"2026-09-02T08:28:00Z"}
{"_type":"issue","comment_count":0,"created_at":"2026-09-02T00:12:51Z","created_by":"Fred [bot]","dependencies":[{"created_at":"2026-09-02T02:12:50Z","created_by":"Fred [bot]","depends_on_id":"poly-3aa","issue_id":"poly-0j4","metadata":"{}","type":"related"},{"created_at":"2026-09-09T17:27:44Z","created_by":"Fred [bot]","depends_on_id":"poly-783c","issue_id":"poly-0j4","metadata":"{}","type":"parent-child"},{"created_at":"2026-09-02T02:12:50Z","created_by":"Fred [bot]","depends_on_id":"poly-9u2","issue_id":"poly-0j4","metadata":"{}","type":"related"}],"dependency_count":0,"dependent_count":0,"description":"poly-9u2 closed the diff channel: the patch payload no longer carries a prior\nattempt's changes.diff, its ops JSON, or any fenced block. Verified against the\nthree real prior libunwind bundles - no ```diff, no ```json, no\n\"mk target set\", no ${REINPLACE_CMD}, no MK heredoc.\n\nThe recipe still arrives. It travels as prose, and triage is what carries it.\n\nMEASURED on devel_libunwind-20260901-234836Z, the first run after poly-9u2\ndeployed. Triage's Suggested Fix reconstructs the whole fix without ever seeing\na diff:\n\n  \"two REINPLACE_CMD substitutions against ${WRKSRC}/tests/test-ptrace.c that\n   insert #elif defined(__DragonFly__) branches ahead of both #error sites\n   (SYSCALL case: a comment noting DragonFly uses INSTRUCTION mode and this\n   branch is unreachable; Singlestep case: a ptrace(PT_STEP, ...) call\n   mirroring FreeBSD's behavior), and add a post-patch target that invokes\n   dfly-patch. The exact expression text is already validated in the three\n   prior bundles' diffs - the patch agent should reproduce that change, not\n   invent a new form.\"\n\nThat names the defect exactly: the comment-only SYSCALL branch is what makes\nthe binary hang, and post-patch invoking dfly-patch is what double-runs a\nnon-idempotent sed. The final sentence is reproduced verbatim from the\n224859Z triage.\n\nTWO MECHANISMS, both in dportsv3/agent/context.py.\n\n1. PriorTriagesSection renders prior analysis/triage.md WHOLE. A prior\n   triage.md contains that round's own Suggested Fix, so the recipe\n   propagates triage -> triage -> patch and never touches a diff. This was a\n   deliberate choice in poly-9u2 - prior triage reasoning looked like the\n   record worth keeping, and fence-stripping was scoped to patch.md. The\n   choice was wrong for one subsection.\n\n2. rebuild_proof.json defeats its own relabelling. poly-9u2 retitled it\n   \"Rebuild Proof (compile-only)\", but the JSON body still reads\n   \"rebuild_ok\": true, and triage quoted it three times as proof the fix is\n   validated - \"all three with \\\"rebuild_ok\\\": true\". The key name is the\n   claim; the title above it does not survive being quoted.\n\nNote what triage asserts: that the expression text \"is already validated in\nthe three prior bundles' diffs\". It no longer has those diffs. It is citing\nevidence it cannot see, reconstructed from prose, and raising its confidence\non it.\n\nPROPOSED\n\na. Do not render a prior triage's ## Suggested Fix. It is the one subsection\n   addressed to the patch agent rather than a record of what was found. Keep\n   Classification, Platform, Root Cause, Evidence, Notes.\nb. Stop emitting a raw rebuild_ok key into any prompt. Render the proof as a\n   compiled: yes/no line built host-side, or drop the artifact from the\n   section - the same fact is already in the audit summary, labelled.\nc. Consider whether patch.md's Patch Log should be summarised rather than\n   quoted. It says \"added two REINPLACE_CMD commands to dfly-patch target for\n   test-ptrace.c, added post-patch target to invoke dfly-patch\" - shape-level\n   but sufficient. Lower confidence on this one: it is also the narrative\n   that correctly flagged the fix was never persisting to DeltaPorts, which\n   is real diagnostic value. Do a and b first and re-measure.\n\nRelated: poly-9u2 (closed the diff channel), poly-3aa (the weak oracle that\nmakes rebuild_ok worth quoting in the first place).\n","id":"poly-0j4","issue_type":"bug","labels":["agent","payload"],"owner":"github@dragonflybsd.org","priority":1,"status":"open","title":"Triage carries the recipe in prose, so closing the diff channel did not break the loop","updated_at":"2026-09-02T00:12:51Z"}
{"_type":"issue","assignee":"Fred [bot]","close_reason":"Diff/ops-JSON/fenced-block channel is shut in both sections, deployed Sep 13, verified against the real libunwind bundles by poly-0j4. The prose residual is poly-0j4 (a)(b)(c).","closed_at":"2026-09-22T19:19:15Z","comment_count":0,"created_at":"2026-09-01T23:20:01Z","created_by":"Fred [bot]","dependencies":[{"created_at":"2026-09-02T01:20:01Z","created_by":"Fred [bot]","depends_on_id":"poly-3aa","issue_id":"poly-9u2","metadata":"{}","type":"related"},{"created_at":"2026-09-09T17:27:43Z","created_by":"Fred [bot]","depends_on_id":"poly-783c","issue_id":"poly-9u2","metadata":"{}","type":"parent-child"}],"dependency_count":0,"dependent_count":0,"description":"The patch payload hands the agent a prior attempt's full diff under a heading\nthat says it succeeded. The agent copies it. The copy compiles, gets stamped\nsuccess, and becomes the next run's exemplar.\n\nMEASURED. devel/libunwind produced five consecutive agent_fixed bundles on\n2026-09-01 whose changes.diff is byte-identical:\n\n  devel_libunwind-20260901-134302Z\n  devel_libunwind-20260901-194535Z\n  devel_libunwind-20260901-214812Z\n  devel_libunwind-20260901-222907Z\n  devel_libunwind-20260901-224859Z\n\nFour harness changes landed between them (poly-dq5, poly-et4, poly-j83,\npoly-6cj) and the last run used a different model on a different provider.\nOutput did not change. The tracker returns newest-first, so the three\nexemplars offered to the next patch job are now 100% this fix.\n\nThe fix is wrong: it writes a comment-only __DragonFly__ branch into\ntest-ptrace.c's SYSCALL switch. Every case in that switch must resume the\nchild, so the binary compiles and hangs. It also adds\n`post-patch: ${MAKE} dfly-patch`, which double-runs a non-idempotent sed\n(bsd.port.mk orders 700:post-patch before 880:dfly-patch).\n\nMECHANISM\n\n1. runner.py:2377-2385 takes the first three bundles from\n   port_bundle_history(origin). No filter on resolution, delivery, or whether\n   the fix was later found wrong. Recency is the only criterion.\n   Triage gets the same, capped at two (runner.py:2289-2297).\n\n2. PriorAttemptsSection (context.py:583) renders each one into the prompt:\n   - analysis/changes.diff, verbatim, in a fenced diff block, 6000 chars\n     (max_diff_chars)\n   - analysis/patch.md, whose required format opens with `## Rebuild Status`\n     (context.py:774) and carries a `## Patch Plan (JSON)` block\n   - patch_audit.json summarised to `rebuild_ok=` per attempt plus\n     `last_rebuild_ok` (context.py:632-636)\n\n3. rebuild_ok is the whole oracle and means only that dsynth compiled the\n   port. attempt_loop.py:328-336 synthesises the proof from a bare tool\n   result when the model never claimed it. Nothing in the pipeline can\n   observe a runtime hang.\n\nSo the agent reads, under \"Prior Attempts\", a complete working diff directly\nbeneath the words Rebuild Status: success. Copying it is correct reasoning\nfrom the evidence supplied. Kimi-k3 said so explicitly: \"the exact expression\ntext is already validated in the two prior bundles' diffs - the patch agent\nshould reproduce that change, not invent a new form.\"\n\nWHY IT COMPOUNDS\n\nAttempt N copies attempt N-1. It compiles, because it compiled last time. It\nis stamped success and exported as attempt N+1's exemplar. A stronger\nreasoner weights the success label more heavily, not less, so better models\nlock in harder.\n\nThis is why the four prior interventions missed. Playbooks are advice; the\npayload carries the answer. Banning code in the triage prompt (poly-6cj) does\nnothing when the diff arrives via PriorAttemptsSection instead.\n\nPROPOSED CHANGE\n\nPrior attempts should carry what was tried and what happened, not a copyable\nrecipe labelled success.\n\na. Drop the fenced changes.diff block from PriorAttemptsSection.\nb. Close the second channel: patch.md's `## Patch Plan (JSON)` can hold the\n   same ops verbatim. Dropping only changes.diff leaves it open.\nc. State the inference that is free at render time: this port is being\n   patched again, therefore every attempt listed below failed to resolve it,\n   whatever its Rebuild Status says. The payload currently asserts the\n   opposite.\nd. Relabel rebuild_ok in the audit summary and the lifted Rebuild Status\n   line: compiled, correctness unverified.\n\nKeep the narrative. History is genuinely useful - it is how the 224859Z\ntriage spotted that the fix was absent from the tree the build ran against.\nThe defect is the copyable diff plus the success label, not history itself.\n\nVALIDATION\n\nRerun devel/libunwind. Five runs across two models produced one output; this\nchanges exactly one input. Expect a different wrong fix rather than a correct\none - that is still the result we need, because we have never observed what\nthe agent does when it is not handed an answer.\n\nRELATED\n\npoly-3aa covers the weak oracle itself (rebuild_ok cannot tell a fix from a\nsilencing). This bead is the separate defect of re-serving that oracle's\noutput as a validated exemplar. Fixing this one does not need the oracle\nfixed first.\n","id":"poly-9u2","issue_type":"bug","labels":["agent","payload"],"notes":"IMPLEMENTED, not yet deployed. dportsv3/agent/context.py.\n\nBoth PriorAttemptsSection (patch flow) and PriorTriagesSection (triage\nflow) carried the defect; the triage one was missed in the original\nwrite-up and is where the 224859Z triage got the diff it told the patch\nagent to reproduce.\n\n• changes.diff renders through _summarize_diff: per-file name and\n  +/- tally, never diff content. max_diff_chars (6000 patch / 3000\n  triage) deleted - it only existed to bound a body no longer emitted.\n• patch.md and the legacy patch.log render through\n  _strip_fenced_blocks, which elides the ## Patch Plan (JSON) block\n  carrying the ops. The legacy patch_plan.json entry is dropped\n  outright - it is nothing but the recipe.\n• Section headers now state that the port is failing now so no listed\n  attempt left it working, and that a success status means the port\n  compiled, not that the change was correct.\n• rebuild_ok relabelled compiled / last_attempt_compiled everywhere it\n  reaches a prompt. Titles: Files Changed, Rebuild Proof\n  (compile-only), Legacy Rebuild Status (compile-only).\n• Two fallback paths found in review that reopened the channel:\n  _tool_trace_summary dumped 2000 raw chars of tool_trace.jsonl when no\n  line parsed (tool args carry put_file/edit_file bodies), and\n  rebuild_proof.json rendered uncapped though it is model-authored.\n  Both bounded.\n• prompts.py: ## Suggested Fix now asks for the state the file must end\n  up in rather than the operation, and the clause \"because that decides\n  the form of the fix\" is gone - it invited mechanism-picking.\n\nVALIDATED against the real 224859Z artifacts: diff body, plan JSON and\nproof JSON all absent from the rendered section.\n\nKNOWN GAP, measured on the same render. The prose in patch.md's Patch\nLog still describes the mechanism closely enough to reconstruct it:\n\"extended dfly-patch with two REINPLACE_CMD ops inserting #elif\ndefined(__DragonFly__) branches at both #error sites, and added a\npost-patch hook\". Stripping that would also remove the narrative this\nsection exists to carry - the same report is what flagged that the fix\nwas never persisting to DeltaPorts. Left in deliberately; the rerun\nwill show whether the weaker form still drives an exact copy.\n\nTests: tests/test_prior_attempts_are_not_exemplars.py (16 cases),\nplus the two payload parity tests updated. 2920 pass.\n\nCORRECTION to the first line above: it is deployed. The install at\n/usr/local/lib/polytropos (Sep 13 19:36) carries _summarize_diff and\n_strip_fenced_blocks in context.py. 'not yet deployed' stopped being true\non Sep 13.\n\nStill open only for the KNOWN GAP: whether the weaker prose form in\npatch.md's Patch Log still drives an exact copy. That needs a rerun to\nanswer, not a change.\n\n---\n\nCLOSED 2026-09-22. Scope complete and verified; the residual belongs to\npoly-0j4.\n\nCode review against the four proposed changes, all present in both\nPriorAttemptsSection and PriorTriagesSection:\n  a. changes.diff -> _summarize_diff (context.py:129, used :652 and :937);\n     max_diff_chars gone.\n  b. patch.md/patch.log -> _strip_fenced_blocks (:163, used :650/:924/:946);\n     patch_plan.json dropped outright.\n  c. section headers state the inference (:632-633, :900).\n  d. compiled= / last_attempt_compiled (:825,:829,:873); titles Rebuild Proof\n     (compile-only) :646 and Legacy Rebuild Status (compile-only) :949.\nprompts.py Suggested Fix asks for end state, not operation; the\n'because that decides the form of the fix' clause is absent.\ntests/test_prior_attempts_are_not_exemplars.py 16/16 pass.\n\nThe rerun this bead was held open for happened: poly-0j4 measures\ndevel_libunwind-20260901-234836Z, the first run after deploy, and confirms\nthis bead's channel is shut - \"no diff, no json, no mk target set, no\nREINPLACE_CMD, no MK heredoc\". The KNOWN GAP is answered yes: the weaker\nprose form does still drive the copy.\n\nThat gap and two others are poly-0j4's scope, still live in the code:\n  - context.py:644 renders analysis/triage.md with transform None, so a\n    prior triage's own Suggested Fix propagates whole  -> poly-0j4 (a)\n  - context.py:645 renders rebuild_proof.json whose body still carries\n    \"rebuild_ok\": true                                 -> poly-0j4 (b)\n  - patch.md Patch Log prose                           -> poly-0j4 (c)","owner":"github@dragonflybsd.org","priority":1,"started_at":"2026-09-01T23:22:55Z","status":"closed","title":"The patch payload offers a prior attempt's diff as a worked example, so the agent copies it","updated_at":"2026-09-22T19:19:15Z"}
{"_type":"issue","assignee":"Fred [bot]","close_reason":"Landed 5584af7 (Sep 2), deployed. No notes were ever written on the bead; the commit body carries the record.","closed_at":"2026-09-14T18:16:21Z","comment_count":0,"created_at":"2026-09-01T22:22:09Z","created_by":"Fred [bot]","dependencies":[{"created_at":"2026-09-09T17:27:43Z","created_by":"Fred [bot]","depends_on_id":"poly-783c","issue_id":"poly-6cj","metadata":"{}","type":"parent-child"}],"dependency_count":0,"dependent_count":0,"description":"The patch agent is told to apply triage's Suggested Fix and not to\nre-investigate, and triage is told to produce a \"Concrete DeltaPorts-style\nfix plan\". So triage writes the change and the patch agent transcribes it.\nNothing anywhere tells the patch agent that triage can be wrong.\n\nMEASURED 2026-09-01/02, devel/libunwind, three consecutive runs on\ndifferent bundles (134302Z, 194535Z, 214812Z). All three produced a\nbyte-identical fix. Its content came from triage's Suggested Fix, which\ncarried the literal REINPLACE_CMD lines including a comment-only branch\nand a statement missing its semicolon, and ended:\n\n  \"Keep the fix in the existing overlay style (REINPLACE_CMD at\n   post-patch) rather than adding a static patch file.\"\n\nThe third run had the corrected toolchain-autoconf playbook loaded and\ntriage's playbook count raised from 1 to 5, and produced the same fix, so\nthis is not a playbook-content problem.\n\nTHE THREE PLACES\n\nprompts.py PATCH_SYSTEM: \"Apply triage's Suggested Fix first. It's a\nconcrete starting hypothesis. Don't burn turns re-investigating what's\nalready in the Triage Summary.\" The one hedging word is cancelled by the\nnext sentence.\n\ncontext.py AutomationContextSection: \"Apply it first. Only explore further\nif the suggested fix has already been tried (check Prior Attempts) or\ndoesn't work.\" Those are the only two exits, and neither fires for a\nsuggestion that builds green and is still wrong - which is exactly this\ncase. There is no branch for \"it builds and is the wrong change\".\n\nprompts.py TRIAGE_SYSTEM: \"## Suggested Fix / Concrete DeltaPorts-style\nfix plan.\" Asked for something concrete, it emits applicable code.\n\nReinforcing it: every playbook is flows:[triage, patch], so triage sees\nall of them, including five error-* entries carrying 14 paste-ready\nREINPLACE_CMD and dops blocks. It is shown recipes and asked for a\nconcrete plan.\n\nSearched for any counterweight: nothing in prompts.py, context.py or the\nplaybooks tells the patch agent triage may be wrong. The only\n\"you may disagree\" line is in TRIAGE_SYSTEM, about disagreeing with an\noperator - the other direction.","design":"1. PATCH_SYSTEM reframes triage as an initial investigation, not an\n   instruction: it read the log and the port metadata, never opened the\n   source, never touched the tree, never ran a build, and the patch agent\n   can do all three. Usually right about what failed, can be wrong about\n   what to do. Neither wave it away nor transcribe it - confirm its\n   reading against the source, then choose the form. Saying it was wrong\n   in the Patch Log is explicitly welcome.\n\n   The anti-thrash intent is kept. The old line existed because\n   exploration burned budget (net-mgmt/ipv6mon: 3.5M tokens into the turn\n   cap), so the new wording still says start from it rather than from\n   nothing.\n\n2. The Automation Context gains the missing exit: explore further if it\n   has been tried, if it doesn't work, or if reading the source shows it\n   would be the wrong change - naming the case the build cannot report.\n\n3. TRIAGE_SYSTEM asks for the direction in prose: which file, what must\n   become true, why, and what kind of file it is, since that decides the\n   form. No patch bodies, no REINPLACE_CMD, no dops ops, no ready-to-paste\n   snippets, with the reason stated - triage cannot see the source or run\n   a build, so anything in applicable form is copied without being\n   checked. It is also told the playbook recipes are addressed to the\n   patch agent: cite the pattern, do not transcribe it.\n\nThe ## Suggested Fix heading is unchanged; the runner's parsers key off\nit. tests/test_triage_is_not_authoritative.py pins the contract by\nsubstance, with whitespace collapsed so a re-wrap is not a failure.\n\nNOT DONE: the exemplar loop. The payload still carries prior agent_fixed\nresults with \"Rebuild Status: success\" and their full diffs, so a fix that\ncompiles and is wrong is offered to the next run as precedent. Separate\nissue; related to poly-3aa.","id":"poly-6cj","issue_type":"bug","owner":"github@dragonflybsd.org","priority":1,"started_at":"2026-09-01T22:22:25Z","status":"closed","title":"Triage writes the fix and the patch agent transcribes it: nothing says triage can be wrong","updated_at":"2026-09-14T18:16:21Z"}
{"_type":"issue","assignee":"Fred [bot]","close_reason":"Landed 5a8f8d7, deployed. Toolchain detection no longer reads the unmaterialized bundle path, and toolchain-autoconf.md's inverted advice was corrected in the same commit. No residual stated.","closed_at":"2026-09-14T18:16:20Z","comment_count":0,"created_at":"2026-09-01T21:31:32Z","created_by":"Fred [bot]","dependencies":[{"created_at":"2026-09-09T17:27:47Z","created_by":"Fred [bot]","depends_on_id":"poly-783c","issue_id":"poly-j83","metadata":"{}","type":"parent-child"}],"dependency_count":0,"dependent_count":0,"description":"Every toolchain-*.md playbook - 10 of the 17 - is silently dropped from\nevery payload, for every port.\n\ndetect_toolchains(port_dir) reads <port_dir>/Makefile from the\nfilesystem. Both call sites (runner.py, in build_triage_payload and\nbuild_patch_payload) pass `bundle_dir / \"port\"`. That directory is only\npresent when the bundle was materialized to disk, and the patch path does\nnot materialize it - steps.py says so outright: \"Patch doesn't materialize\nthe bundle into a tempdir (unlike triage)\". The read finds nothing and the\ndetector returns an empty set.\n\nAn empty set is not an error. The selector treats it as \"no toolchain\ncontext known\", every toolchain-tagged entry fails its trigger, and the\npayload still looks populated because the untagged entries are all still\nthere. The playbooks_selected activity row reported only included/skipped\ncounts, so there was no way to see it from the outside either.\n\nThe payload renders that very Makefile without trouble. read_bundle_text\ntries bundle_dir first and then falls back to artifact_get(bundle_id,\nrelpath); detection had no such fallback. One reader has it, the other\ndoes not.\n\nMEASURED 2026-09-01, devel/libunwind bundle devel_libunwind-20260901-194535Z.\n  logged: triage included=1 skipped=16 / patch included=5 skipped=12\n  reproducing the selector locally, those counts match exactly\n    role=triage cls=None          toolchains=[] -> 1\n    role=patch  cls=compile-error toolchains=[] -> 5\n  and with the tags the Makefile actually carries\n    role=patch  cls=compile-error toolchains=[autoconf] -> 6\n  The bundle's own port/Makefile has both:\n    USES=  compiler:c11 cpe libtool pkgconfig\n    GNU_CONFIGURE= yes\n  Feeding it straight to the detector returns\n    ['autoconf', 'c', 'libtool', 'pkg-config']\n  and the same call against a missing directory returns []. The detector\n  is correct; the path it is handed does not exist.\n\nNo on-disk bundle directory exists under the evidence tree at all, so this\nis not specific to one port or one run.","design":"playbooks.detect_toolchains_from_makefile(text) infers the same tags from\nthe Makefile's text, so a caller holding bundle contents through the\nartifact store can detect without a directory. Both payload builders now\ntry the directory first and fall back to\nread_bundle_text(bundle_dir, bundle_id, \"port/Makefile\").\n\nOnly the Makefile signals (USES=, GNU_CONFIGURE, USE_GMAKE) are reachable\nthat way. The file-presence signals (configure.ac, CMakeLists.txt,\nmeson.build, Cargo.toml) need a source tree, and a bundle only ever\ncarries port/* - Makefile, distinfo, pkg-descr, pkg-plist - so nothing is\nlost that a bundle could have provided.\n\nThe playbooks_selected row now names the detected toolchains, or \"none\".\nThe failure was invisible because an empty set and a genuine no-match\nlooked identical in telemetry; the count alone could not distinguish them.\n\nA test asserts the directory and text paths agree, so the fallback cannot\ndrift into a second implementation.","id":"poly-j83","issue_type":"bug","notes":"toolchain-autoconf.md item 2 corrected in the same change.\n\nIt told the agent that a dops `mk target set dfly-patch` body with\nREINPLACE_CMDs \"is durable across upstream regenerations\" while a static\ndragonfly/patch-*.c \"works but rots on every version bump\". That is\nbackwards for hand-written source, and it is the direct opposite of\nerror-prefer-dops-over-static-patches Step 2, which says a whole-line\nsubstitution silently no-ops when upstream edits the line while a patch\nfails loudly.\n\nIt was harmless while the playbook was dead. Fixing the detection makes it\nlive, so it had to be corrected in the same commit rather than left to\nland as newly-active wrong advice.\n\nRewritten to route by target kind rather than by symptom: hand-written\nsource (.c, .h, configure.ac, Makefile.am) takes a static patch; generated\noutput (configure, Makefile.in, libtool m4) takes a dops op, or better,\npatch the autotools source and let it regenerate. It now points at\nerror-prefer-dops-over-static-patches for the full decision instead of\nrestating a rule that can drift out of agreement with it.\n\nKept general - no reference to any particular port. Item 1's REINPLACE for\nconfig.sub/config.guess is consistent with the corrected rule (those are\nvendored generated files) and was left alone. The other nine toolchain\nplaybooks were surveyed for the same inversion; only this one had it.\ntoolchain-gmake.md already says upstream patches are the durable form.\n\nVerified after the edit: the file still parses, priority 100,\ntoolchains=('autoconf',), est_tokens 832, and it fires for an autoconf\nport. Suite 2898 passed.","owner":"github@dragonflybsd.org","priority":1,"started_at":"2026-09-01T21:31:46Z","status":"closed","title":"Toolchain playbooks never fire: detection reads a directory the bundle path does not materialize","updated_at":"2026-09-14T18:16:20Z"}
{"_type":"issue","assignee":"Fred [bot]","close_reason":"Landed 3e4aa60, deployed. bundle_branch_dropped fired 268 times in runner.log. The stale-branch-with-commits case was considered and rejected in the notes.","closed_at":"2026-09-14T18:16:14Z","comment_count":0,"created_at":"2026-09-01T19:10:17Z","created_by":"Fred [bot]","dependencies":[{"created_at":"2026-09-09T17:26:08Z","created_by":"Fred [bot]","depends_on_id":"poly-88k","issue_id":"poly-et4","metadata":"{}","type":"parent-child"}],"dependency_count":0,"dependent_count":0,"description":"Step 30 slice 4 states the contract plainly, in\ntests/test_bundle_branch_drop_per_job.py:\n\n  \"Patch end (either outcome) -> branch is dropped (changes.diff was\n   already captured at patch success; on failure the branch's state is\n   moot).\"\n\nThe branch is never dropped. Two bugs stacked, and the second hides the\nfirst.\n\nBUG 1 - the drop helper does not drop.\nrunner.py:3147, inside _drop_bundle_branch_for_job, whose own docstring\nsays \"drop the bundle's branch at terminal job end\":\n\n    # Keep the branch: a bundle's convert->patch chain shares it, and a\n    # later job in the chain still wants its commits. Only the worktree\n    # goes.\n    result = worker.destroy_job_worktree(env, bundle_id, \"patch\")\n\ndestroy_job_worktree's drop_branch defaults to False. The keep-the-branch\nrationale was pasted from destroy_job_worktree into the drop helper and\ndrop_branch=True was never passed, so only the worktree is removed.\n\nBUG 2 - the success signal reads a key that is never set.\nThe helper gates its \"bundle_branch_dropped\" activity row on\nresult.get(\"removed\"). destroy_job_worktree returns dropped_branch, never\nremoved, so the row never fires. Neither does the failure row, because\nok is True. The operation is silent in both directions.\n\nWHY THE TESTS STAYED GREEN.\ntest_drop_helper_logs_success_when_removed stubs destroy_job_worktree with\na lambda taking **kw and returning a canned {\"ok\": True, \"removed\": True}.\nIt pins the logging, never the argument, and supplies the \"removed\" key\nthe real worker does not.\n\nMEASURED 2026-09-01 in env 2026Q3:\n  activity rows  bundle_branch_checkout      36\n                 bundle_branch_dropped        0\n                 bundle_branch_drop_failed    0\n  bundle branches in ports-main              70\n    with commits of their own                 0\n    based on a stale master                  53\n\nNot one branch carries a commit, so the \"a later job still wants its\ncommits\" rationale is earning nothing. They are 70 stale pointers.\n\nCONSEQUENCE, measured on devel/libunwind.\nRe-running bundle devel_libunwind-20260830-160338Z reused\nbundle/devel_libunwind-20260830-160338Z, still pinned at ed604d3b3b6\nwhere it was cut on 08-30 (\"branch: Created from master\", one reflog\nentry, zero commits). master is at 04ebd5953f3, ten commits ahead.\ncreate_job_worktree(kind=\"patch\") reuses an existing branch as-is - there\nis no fast-forward on that path, only kind=\"verify\" recuts with -B.\nchanges.diff is `git diff <base>` from inside that worktree, so it\nreported the agent's real libunwind fix PLUS every file master had gained\nsince, as reversions:\n\n  ports/devel/binutils/diffs/Makefile.diff        (the gcc12 unblock)\n  ports/devel/gamin/overlay.dops                  (merged, PR #1574)\n  ports/graphics/vulkan-loader/overlay.dops       (merged, PR #1571)\n  ports/multimedia/svt-av1/{overlay.dops,patch-*} (merged, PR #1572)\n  ports/www/w3m/overlay.dops                      (merged, PR #1573)\n  special/Mk/diffs/@2026Q3/bsd.default-versions.mk.diff  (default to gcc12)\n\nRecorded agent_fixed. Applying it would revert four merged PRs, the\nbinutils change that unblocked gcc12, and the gcc12 default.\n","design":"1. Pass drop_branch=True in _drop_bundle_branch_for_job and replace the\n   pasted keep-the-branch comment with the slice 4 rationale. This is the\n   whole fix for bug 1; the contract and the helper's name already say\n   drop.\n\n2. Gate the activity row on dropped_branch, the key destroy_job_worktree\n   actually returns, so the operation stops being silent.\n\n3. Make the test assert the argument instead of only the logging: capture\n   the kwargs the stub receives and assert drop_branch is True. As written\n   it also supplies a \"removed\" key the real worker never returns, which is\n   what let bug 2 through.\n\nDeliberately NOT rebasing or fast-forwarding a reused branch. ports-main's\nmaster is itself moved by `dev-env update --force`, so base is not a fixed\npoint and a re-run's diff would depend on when the env was last updated; a\nrebase that conflicts in an unattended runner stalls the job mid-operation.\nDropping at patch end makes a re-run cut a fresh branch from the current\nbase, which is correct by construction and needs no conflict handling.\n\nWorth having as an independent backstop: refuse a changes.diff whose files\nfall outside the bundle's own origin. It would have caught the libunwind\ncase loudly rather than recording agent_fixed. Tracked separately in\npoly-3aa.\n","id":"poly-et4","issue_type":"bug","notes":"Implemented in the commit refs'ing this bead. Suite 2890 passed.\n\nCORRECTION to this bead's design. I first made the drop unconditional,\nmatching the slice 4 docstring. That was wrong. Git history shows\ntest_patch_drop_keeps_the_bundle_branch (\"must not delete the bundle\nbranch\") arrived in 5cc4931, the B1 per-job-worktree change, while the\n\"Patch end -> branch is dropped\" docstring dates from the initial import\n3414779. Keeping is the newer, deliberate decision, and convert does\ncommit to the branch (overlay.dops creation), so dropping unconditionally\nwould have broken the convert->patch chain. Reverted, implemented the\nconditional instead.\n\nWhat landed:\n- destroy_job_worktree gains drop_branch_if_unused. It asks git for\n  rev-list --count base..branch and deletes only at 0, so a branch with\n  commits is never touched. Skips entirely when the base cannot be\n  resolved, since there is no safe answer then.\n- _drop_bundle_branch_for_job passes it. drop_branch itself is still never\n  passed on the patch path, so test_patch_drop_keeps_the_bundle_branch\n  holds unchanged.\n- Both drop helpers now gate their activity row on dropped_branch instead\n  of the absent \"removed\".\n- The two stubs that returned a canned \"removed\" key now return\n  dropped_branch, and a new test asserts drop_branch_if_unused=True is what\n  gets passed. Four worker-level tests cover empty / non-empty / no-flag /\n  unknown-base.\n- The stale module docstring now describes the actual contract.\n\nEnv swept: 70 empty bundle branches deleted, 0 failed, 0 remaining, none\nwere held by a worktree. master unchanged at 04ebd5953f3.\n\nNot done: a stale branch that DOES carry commits is still reused as-is.\nRebasing it was considered and rejected - ports-main's master is moved by\ndev-env update --force, so base is not a fixed point, and a rebase\nconflict would stall an unattended job mid-operation. The changes.diff\norigin guard in poly-3aa is the backstop for that case.\nThird review pass on the implementation. Four more issues, all folded in\nbefore pushing.\n\n1. The drop signal was the bare token DROPPED, sniffed out of the shell\n   chain's stdout - a chain that also runs `worktree remove` and\n   `worktree prune`. `worktree prune -v` prints lines like\n   \"Removing worktrees/<name>: gitdir file points to non-existent\n   location\", so a path containing that word would have been read as a\n   successful drop. Now __POLY_BRANCH_DROPPED__, with a test that feeds\n   exactly that git chatter in and asserts dropped_branch stays False.\n\n2. branch -D changed to branch -d on the conditional path. rev-list has\n   already established the branch is fully merged into base, and -d makes\n   git enforce that independently: if the rev-list reasoning is ever\n   wrong, git refuses instead of destroying commits. -D stays on the\n   explicit drop_branch path, where forcing is the point.\n\n3. Removed a redundant `echo DROPPED` on the drop_branch path. That flag\n   already determines the return value, so the echo only polluted\n   stdout_tail.\n\n4. test_patch_drop_keeps_the_bundle_branch still passed but its prose\n   (\"Only the worktree is disposable\") no longer described what happens.\n   Rewritten to say what it actually pins: the patch path never *forces*\n   the branch away; whether an empty one is cleaned up is the worker's\n   call.\n\nChecked and found sound, no change needed:\n  - Nothing outside worker.py/runner.py references bundle branches, and\n    reconcile.py confirms the delivery diff is captured before removal,\n    so no downstream consumer needs the branch after the job.\n  - A failed branch delete short-circuits the && and the chain exits\n    non-zero, so ok=False and the drop_failed row fires. Failure is loud.\n  - A missing branch makes rev-list error, the || sets n=-1, and the\n    branch is kept. No spurious deletion.\n  - _resolve_bundle_base_branch runs before the worktree is removed and\n    reads PORTS_MAIN_DIR, whose HEAD worktrees never move.\n\nStill open, unchanged: convert failure is contractually supposed to drop\nthe branch but no call site does it, and a stale branch that carries\ncommits is still reused as-is.\n\nSuite 2891 passed.","owner":"github@dragonflybsd.org","priority":1,"started_at":"2026-09-01T19:10:29Z","status":"closed","title":"The bundle branch is never dropped: the drop helper does not pass drop_branch, and its success row reads a key that is never set","updated_at":"2026-09-14T18:16:14Z"}
{"_type":"issue","assignee":"Fred [bot]","close_reason":"Landed a82cce9 plus two review amendments, deployed. attempt_workspace_reset fired 399 times in runner.log. get_file's tab separator stays as a maybe-revisit, not open work.","closed_at":"2026-09-14T18:16:13Z","comment_count":0,"created_at":"2026-09-01T12:59:29Z","created_by":"Fred [bot]","dependencies":[{"created_at":"2026-09-09T17:27:45Z","created_by":"Fred [bot]","depends_on_id":"poly-783c","issue_id":"poly-dq5","metadata":"{}","type":"parent-child"},{"created_at":"2026-09-17T08:34:36Z","created_by":"Fred [bot]","depends_on_id":"poly-l18e.4","issue_id":"poly-dq5","metadata":"{}","type":"relates-to"}],"dependency_count":0,"dependent_count":0,"description":"The sanctioned way to fix a source file is\nmake_extract -> make_patch -> dupe -> edit -> genpatch -> install_patches.\nOn the second and every later attempt in a runner process, that flow\ncannot complete: genpatch stages its output where install_patches never\nlooks, and the WRKSRC it diffs still carries the previous attempt's\nedits. The agent's only remaining move is to hand-author the patch,\nwhich is the behaviour poly-n78 records and poly-7jw's guard now\nrefuses - so the attempt dead-ends with no legal path forward.\n\nMEASURED 2026-09-01. devel/libunwind, bundle\ndevel_libunwind-20260901-113146Z, job\n20260901-113257Z-2026Q3-devel_libunwind-970328-patch.job.\n4 attempts, 251 tool calls, 4,278,456 tokens, agent_gave_up at 12:50Z.\nStack: polytropos 5f7c781, model nvidia/nemotron-3-ultra-550b-a55b.\n\n1. GENPATCH STAGES WHERE INSTALL_PATCHES DOES NOT LOOK\n\ngenpatch has two staging conventions. On a _WRKSRC_CACHE hit the patch\nlands in WRKSRC under a WRKSRC-relative name; on a miss it lands in\n/work/genpatch-out/. install_patches only ever reads\n<writable>/work/genpatch-out.\n\n_WRKSRC_CACHE is process-lifetime. reset_attempt_caches (worker.py:1261)\nclears _READ_CACHE only. So the first make_extract in the runner process\npopulates it and every genpatch afterwards takes the cache-hit path.\n\nAttempt 3 walks into it exactly:\n  T13 genpatch                      ok, \"generated patch-tests_test-ptrace.c\"\n  T14 get_file /work/genpatch-out/  ok=false, \"no such file\"\n  T15 get_file <WRKSRC>/patch-...   ok - the patch is here\n  T16 install_patches               FileNotFoundError: missing patches\n\n2. WRKSRC AND THE DUPE BASELINE CARRY THE PREVIOUS ATTEMPT'S EDITS\n\nmake_extract runs `make extract`, which is a no-op once the extract\ncookie exists, so a later attempt inherits whatever the earlier one\nwrote. Attempt 4 read the file after make_extract and reported:\n\n  \"the upstream source already has the DragonFly support (lines 365-366\n   and 378-379). The patch is no longer needed.\"\n\nThose lines were attempt 1's edit. It then duped that file, so genpatch\ndiffed against a baseline that already contained the change. Attempt 4\nworked it out at T22:\n\n  \"the .orig file doesn't have the DragonFly branches at all, but the\n   current source has them in both cases (INSTRUCTION already had it,\n   SYSCALL I added). The genpatch only captured one hunk.\"\n\nAny patch generated this way is structurally incomplete.\n\n3. GENPATCH-OUT IS NEVER CLEARED\n\n/work/genpatch-out is shared by every job and nothing empties it. It\nstill held libunwind's malformed patch from 02:54Z nine hours later.\ninstall_patches with patches=None installs every patch-* found there\ninto whichever origin is current, so one job's leftover can be\ninstalled into an unrelated port.\n\n4. REGRESSION IN 5f7c781: THE TRAILING-NEWLINE REJECTION\n\n_validate_unified_diff refuses a patch that does not end with a\nnewline, and _reject_malformed_patch_write applies that to every\ndragonfly/patch-* write. 15 of the job's 19 put_file calls and both\ninstall_patches calls died on it:\n\n  attempt 3 T17-T23   7 consecutive put_file, all \"does not end with a newline\"\n  attempt 3 T26       install_patches, same\n  attempt 4 T22-T28   7 more, same\n\nThe model never appended the newline; it re-sent identical 415-byte\ncontent until the turn cap. The rule earns nothing to pay for that.\nRun over all 4199 dragonfly/patch-* in DeltaPorts it flags 6, and every\none is benign: devel/jwasm and x11/xfce4-taskmanager end mid-line;\ndevel/readline, shells/bash, net/openslp and net/hostapd210 have a\nfinal hunk one trailing-context line short at EOF, which patch(1)\naccepts. graphics/dcp2icc's own agent_fixed patch trips both rules and\nbuilt successfully. The genuinely broken artifact (libunwind's\n28-context-line hunk under an @@ -358,13 +358,16 @@ header) is caught by\nthe body-longer-than-declared and no-change-lines rules without it.\n\n5. GET_FILE'S SEPARATOR IS A TAB\n\nworker.py:550 renders f\"{n:6d}\\t{line}\". A tab-indented line comes back\nas number, TAB, then the file's own TAB, and nothing marks which is\nfurniture. edit_file then demands byte-exact reproduction of that\ndistinction. Across the job the model guessed \\t\\t, \\t, two spaces, six\nspaces, tab+six spaces, and once a literal backslash-t, before finding\nthe real \\t + two spaces at attempt 2 T11 and succeeding immediately.\nThe not-found error names no bytes, so each guess costs a turn.\n\nWHAT HELD\n\npoly-7jw's truncation fix held. The file went 10075 -> 10070 -> 10161\n-> 10166 bytes and was never truncated or emptied, and there was no\nwhole-file put_file on a source file.\n","design":"1. genpatch always stages into /work/genpatch-out, in addition to the\n   WRKSRC-relative copy the cache-hit path writes. One staging location\n   for install_patches to read; the canonical naming is preserved.\n\n2. Clear /work/genpatch-out at attempt start, so one attempt's output\n   cannot be installed by the next and a stale malformed file cannot\n   block an unrelated port. Belongs with reset_attempt_caches, which\n   attempt_loop already calls per attempt.\n\n3. Clean the port's WRKDIR at attempt start so make_extract really\n   re-extracts and dupe snapshots a pristine baseline.\n   _clean_port_workdir already exists and drops the caches.\n\n4. Drop the trailing-newline rejection from _validate_unified_diff.\n   Keep no-change-lines and body-longer-than-declared; tolerate a final\n   hunk short at EOF, which is the readline/bash/openslp/hostapd210/\n   dcp2icc shape.\n\n5. edit_file's not-found error returns the closest matching region of\n   the file verbatim, so the tab/space distinction is readable rather\n   than guessable. This is what aider's SearchReplaceNoExactMatch\n   already does (\"Did you mean to match some of these actual lines?\").\n","id":"poly-dq5","issue_type":"bug","notes":"Implemented in 7e6ce8a. Suite 2878 passed (51 cases in\ntests/test_worker_edit_file.py).\n\n1. genpatch now copies its cache-hit output into /work/genpatch-out as\n   well as leaving it in WRKSRC, so install_patches can find it whichever\n   staging path genpatch took.\n\n2. reset_attempt_workspace(env, origin) added and called from\n   attempt_loop at every attempt start, beside reset_attempt_caches. It\n   clears patch-* from genpatch-out and runs _clean_port_workdir so\n   make_extract really re-extracts and dupe snapshots a pristine\n   baseline. Both stages best-effort; the result is emitted as an\n   attempt_workspace_reset event. Side benefit: _clean_port_workdir\n   drops _WRKSRC_CACHE, which is what made the staging path\n   process-lifetime in the first place.\n   patch.run now forwards origin to attempt_loop.run - it accepted the\n   argument but dropped it, so the WRKDIR could not have been cleaned.\n\n3. Trailing-newline rejection removed from _validate_unified_diff. A\n   final hunk short at EOF is tolerated too, but only at EOF and only\n   when the shortfall is equal on both sides (i.e. trailing context, no\n   missing change line); short mid-file is still an error. Kept:\n   body-longer-than-declared, no-change-lines, bad body-line prefix,\n   change line past a satisfied hunk.\n   Re-run over all 4199 dragonfly/patch-* in DeltaPorts: 0 rejected,\n   down from 6. The libunwind artifact is still caught, now as\n   'declares 13 old / 16 new ... but the body carries more (16 / 16)'.\n\n4. edit_file returns nearest_line + nearest_text on a missed anchor,\n   found by scoring whitespace-normalised lines and returning the file's\n   real bytes. Tested against the exact anchor that failed on libunwind\n   (two tabs vs tab + two spaces): it returns the correct\n   '\\t  ptrace (...' region, and a round-trip test feeds nearest_text\n   straight back as old_string and the edit lands. When the region holds\n   tabs the message also names the get_file separator problem.\n\nNot done: get_file's separator is still a tab (worker.py:550). Changing\nit would alter every read the model has been trained on this session;\nnearest_text makes the ambiguity recoverable instead. Worth revisiting\nif anchors keep missing.\n\nNot committed by me beyond this: nothing pushed.\nRe-review found three issues in the first implementation; amended into\n7728159 before pushing.\n\n1. The WRKDIR was cleaned on EVERY attempt, including the first. That is\n   redundant and expensive: reset_port already runs _clean_port_workdir\n   as its first stage when a patch job ends (steps.py:1296 ->\n   worker.py:1992), so between jobs the WRKDIR is already pristine. The\n   real gap is only BETWEEN ATTEMPTS within a job. Cleaning on attempt 1\n   bought nothing and forced a full re-extract every time - painful on a\n   large port. Now gated on attempt_idx > 1, with attempt_idx threaded\n   from attempt_loop. genpatch-out is still cleared on every attempt: it\n   is shared across jobs, cheap to clear, and nothing else sweeps it.\n\n2. _nearest_region would return a region on a single incidental line\n   match. On a 24-line window that is not a near miss, and pointing the\n   model at an unrelated region is worse than staying quiet. Now requires\n   best_score * 3 >= span. Verified it still fires on the libunwind\n   shape (most lines right, indentation wrong) and declines a\n   contrived weak match.\n\n3. _validate_unified_diff carried a redundant hit_eof flag alongside the\n   i >= len(lines) test that already implied it. Removed; the two could\n   have drifted.\n\nSuite 2882 passed (55 cases). Corpus re-checked after the amendments:\n0 of 4199 rejected.\nThird review pass, tracing the live codepaths rather than the diff.\nTwo more issues found and folded in.\n\n4. reset_attempt_workspace dropped the materialize baseline. It calls\n   _clean_port_workdir, which pops _MATERIALIZE_STATE as well as\n   _WRKSRC_CACHE. That coupling is right for reset_port but wrong here:\n   _MATERIALIZE_STATE tracks ports/<origin>/ against the compose tree at\n   /work/artifacts/compose, and cleaning a WRKDIR under /work/obj does\n   not invalidate it. dsynth_build gates on that baseline\n   (worker.py:3078) and refuses with blocked_by=stale_compose when it is\n   missing, so every retry would have burned a turn on a needless\n   materialize_dports. Now snapshotted and restored around the clean,\n   confined to this function so reset_port is untouched. Safe because\n   dsynth_build still re-checks _port_subtree_hash, so a baseline that\n   has genuinely gone stale is caught there.\n\n5. The attempt_workspace_reset event reached tool_trace.jsonl but not\n   the activity log: PatchEventDispatcher.__call__ appends every event\n   then routes by type, and there was no branch for this one. The reset\n   deletes staged patches and wipes a WRKDIR - it needs to be visible in\n   the feed, which is where this whole investigation was done. Branch\n   added, reporting the count cleared and whether the wrkdir clean\n   succeeded.\n\nAlso checked and found sound, no change needed:\n  - genpatch staging block: paths/genpatch_out/patch_path/patch_basename\n    are all in scope at the insertion point.\n  - origin really does reach the reset: steps.py:1187 -> patch.run ->\n    attempt_loop.run -> reset_attempt_workspace.\n  - CRLF patches and blank context lines both validate clean; the\n    trailing \\r does not trip the body-prefix check.\n  - the dispatcher tolerates unknown event types without raising.\n\nSuite 2884 passed (57 cases).","owner":"github@dragonflybsd.org","priority":1,"started_at":"2026-09-01T12:59:48Z","status":"closed","title":"The regenerate-a-patch flow dead-ends: genpatch stages where install_patches never looks","updated_at":"2026-09-14T18:16:13Z"}
{"_type":"issue","assignee":"Fred [bot]","close_reason":"Landed 5f7c781, deployed. edit_file has 1492 calls in runner.log. Residual revert_file was declared out of scope in the notes.","closed_at":"2026-09-14T18:16:12Z","comment_count":0,"created_at":"2026-09-01T10:19:10Z","created_by":"Fred [bot]","dependencies":[{"created_at":"2026-09-09T17:27:44Z","created_by":"Fred [bot]","depends_on_id":"poly-783c","issue_id":"poly-7jw","metadata":"{}","type":"parent-child"}],"dependency_count":0,"dependent_count":0,"description":"put_file is the only write primitive the patch agent has. There is no\nline-oriented edit tool - no str_replace, no replace-range. To change one\nline of a source file the agent must re-emit the entire file verbatim,\nand on multi-kilobyte sources it does not do that reliably.\n\nMEASURED 2026-09-01, devel/onetbb and devel/libunwind.\n\n  devel/onetbb, test/common/memory_usage.h (real size 6771 bytes):\n    dupe memory_usage.h                       correct order\n    put_file memory_usage.h (1247 bytes)      truncated 6771 -> 1247\n    genpatch ok\n    install_patches installed=0 FAIL          nothing to install\n    put_file memory_usage.h (6771 bytes)      restored\n\n  same job, test/common/utils_concurrency_limit.h (19229 bytes):\n    put_file utils_concurrency_limit.h.patch (1133 bytes)\n                                              a .patch file written INTO WRKSRC\n    put_file dragonfly/patch-test_common_memory__usage.h (0 bytes)\n                                              a ZERO-BYTE patch into the overlay\n    put_file utils_concurrency_limit.h (47 bytes)    truncated 19229 -> 47\n    put_file utils_concurrency_limit.h (19229 bytes) restored\n\n  devel/libunwind, tests/test-ptrace.c (8017 bytes):\n    put_file test-ptrace.c (730 bytes)\n    put_file test-ptrace.c (0 bytes)          emptied the file\n\nTHE CHAIN\n\n  whole-file rewrite truncates or corrupts the source\n  -> genpatch diffs against a mangled baseline\n  -> empty or garbage diff, install_patches installed=0 FAIL\n  -> agent falls back to hand-authoring the patch file with put_file\n  -> malformed hunk\n\nThe libunwind fallback patch that resulted has 28 context lines and ZERO\nadditions or deletions, under a header claiming @@ -358,13 +358,16 @@.\npatch(1) rejects it. gcc12 produced the same class of artifact earlier\n(header claimed 8 lines, body supplied 5).\n\nRELATION TO poly-n78\n\n  poly-n78 records the same visible symptom - hand-edited patch files -\n  but its cause note attributes it to make_extract being dead (poly-cx6,\n  now closed), which made dupe/genpatch physically unavailable. That is\n  not this. Here make_extract works and dupe/genpatch ARE called; they\n  still produce nothing usable because the file they diff has been\n  corrupted by put_file first. Fixing n78's \"prefer genpatch\" does not\n  help when genpatch's input is already wrong.\n\nWHY IT MATTERS\n\n  Of 13 bundles resolved on 2026-09-01, the 5 that hand-authored a\n  dragonfly/patch-* file produced 4 give-ups. Every fix that landed was\n  expressible as an mk / REINPLACE_CMD op - a sed expression, no file\n  reproduction required. Every give-up involved trying to edit a real\n  source file. devel/libunwind's one successful fix (2026-08-30) used\n  REINPLACE inside `mk target set dfly-patch` for exactly this reason.\n\nFIX DIRECTION\n\n  Add a line-oriented edit tool: replace-range or str_replace with an\n  expected-context guard, so editing a source file never requires\n  re-emitting it. That removes the truncation class entirely.\n\n  Secondary, cheap, and worth having regardless: nothing currently acts\n  on the loud signals. `install_patches installed=0 FAIL` and a 0-byte\n  patch file are both written and ignored. A generated patch should be\n  rejected before an attempt may claim success unless its hunk counts\n  match its body, it contains at least one non-context line, and it ends\n  with a newline.","id":"poly-7jw","issue_type":"bug","notes":"Implemented.\n\nedit_file(path, old_string, new_string, expected_sha256?, replace_all?)\nworker.py, registered in tools.py. Anchored replace: unique match\nrequired, zero and ambiguous both fail rather than guess. Reuses\nput_file's write guards plus the line-number check on BOTH strings, so\na numbered anchor says why instead of a bare no-match. Returns new\nsha256 + a context window so confirming an edit costs no extra turn.\nNon-UTF-8 is directed back to put_file base64.\n\ninstall_patches hardened, both loud signals from the bead:\n- empty genpatch-out now ok=False, not the old ok=True/installed=[]\n- every candidate structurally validated before any copy; the batch is\n  refused whole rather than half-installed\n\n_validate_unified_diff: hunk header counts vs body, at least one +/-\nline, trailing newline, body-line prefixes. Regression-tested against\nthe measured libunwind signature (header 13->16 over 28 context lines,\nzero changes) and the gcc12 class (header claims 8, body supplies 5).\n\nGuidance updated so the model reaches for it: prompts.py (WRKSRC edit\nstep, overlay.dops write loop, discipline list) and playbooks\nflow-patch.md + error-dragonfly-source-patches.md. runner.py activity\nsummarizer prints edit_file's RESULTING size — the same signal that\nmade the truncations visible.\n\ntests/test_worker_edit_file.py, 25 cases. Suite 2852 passed.\n\nNot done, deliberately out of scope: revert_file (restore from the\n.orig dupe already writes). Without it a bad write is still terminal.\nRe-review found four defects in the first implementation; all fixed.\n\n1. _validate_unified_diff over-consumed. The hunk-body loop read on to\n   the next header, so a blank separator line or a trailing newline at\n   EOF was folded into the hunk's tally and a VALID patch was rejected.\n   Now it stops as soon as the header's own counts are satisfied.\n\n2. That stop then under-detected: trailing body lines past the declared\n   counts were never examined ('-a/+b/+c' under a header claiming 1/1\n   passed). Added an explicit check for a +/- line immediately after a\n   satisfied hunk.\n\n3. edit_file wrote with a plain write_bytes. A write dying partway\n   leaves a truncated file - the exact failure the tool exists to\n   prevent. Now temp-file + atomic replace, mode carried over.\n\n4. THE IMPORTANT ONE. The validation was only on install_patches, but\n   that is not the path the malformed patch took. The bead says the\n   agent 'falls back to hand-authoring the patch file with put_file' -\n   straight into ports/<origin>/dragonfly/, never through\n   install_patches, and nothing was refusing it. Added\n   _reject_malformed_patch_write, wired into both put_file and\n   edit_file. Scoped to dragonfly/patch-* basenames so whole-file\n   'file materialize' sources under dragonfly/ are not parsed as diffs.\n\nVALIDATED AGAINST THE REAL CORPUS. Ran the validator over all 4199\ndragonfly/patch-* files in the DeltaPorts checkout: 6 flagged, every one\npre-existing and genuinely malformed, none agent-written.\n  devel/readline patch-terminal.c and shells/bash patch-lib_readline_\n  terminal.c - header @@ -580,7 +580,7 @@ over a 6/6 body; the closing\n  context line is missing from both.\n  net/openslp, net/hostapd210 - same class.\n  devel/jwasm, x11/xfce4-taskmanager - file ends mid-line, no newline.\n0.14% flag rate on a corpus that predates the guard, and the guard only\ngoverns new writes, so it does not touch these.\n\nAlso corrected: the missing-newline message claimed patch(1) would\nreject the hunk. patch(1) commonly tolerates it - the message now says\nwhat the signal actually means for a freshly written patch.\n\nThree placeholder fixtures in test_worker_guardrails.py used\n'@@ -1 +1 @@' as stand-in patch content; they test the\nMakefile.DragonFly authoring lock, not diff validity, so they now carry\na real one-line diff.\n\ntests/test_worker_edit_file.py now 34 cases. Suite 2861 passed.\nFifth defect, found and fixed: replace_all had no type coercion.\n\nIt is the only boolean in the whole tool registry, and dispatch passes\nLLM arguments through untouched. Models do emit the STRING \"false\" for\na boolean; `not \"false\"` is False in Python, so that would have\nsilently turned a single anchored replace into a whole-file one - the\nopposite of what this tool is for. _as_bool now accepts real booleans\nand the common string spellings and refuses anything else rather than\nguessing.\n\nSuite 2865 passed (38 cases in test_worker_edit_file.py).\n\nput_file is NOT removed. 18 tools registered, both write primitives\npresent. put_file keeps its role (create a file, write base64/binary);\nonly its description changed, plus the malformed-patch guard on its\ntext path.\nFollow-on: poly-dq5. The libunwind verification run for this bead\ngave up, and the dominant cause was the trailing-newline rejection added\nhere in 5f7c781 - it blocked 15 of 19 put_file calls and both\ninstall_patches calls across attempts 3 and 4. The truncation fix itself\nheld (file never truncated, no whole-file put_file on a source). The\nnewline rule is removed under poly-dq5.","owner":"github@dragonflybsd.org","priority":1,"started_at":"2026-09-01T10:44:37Z","status":"closed","title":"put_file whole-file writes truncate source files, so genpatch diffs a corrupted baseline","updated_at":"2026-09-14T18:16:12Z"}
{"_type":"issue","close_reason":"Fixed in b5c9c5e (engine: stop the commit from reverting a patch it never saw). patch.apply now calls txn.flush_pending() before shelling out, so the subprocess sees staged edits and commit has no stale buffer to write back. Two regression tests added; the mk-before-patch one fails without the fix reproducing the exact binutils signature. Suite 2827 green. Verified on the real tree: devel/binutils composes with --with-lib-path and without the FreeBSD CONFIGURE_TARGET, devel/gdb's patch lands, controls devel/jna and lang/gcc13 unregressed. End to end, lang/gcc12 then built SUCCESS in 00:54:22 (27 packages, 0 failed) after failing at the stage1 libgcc_s.so link at 4:27 every prior attempt.","closed_at":"2026-08-31T23:39:54Z","comment_count":0,"created_at":"2026-08-31T20:27:15Z","created_by":"Fred [bot]","dependency_count":0,"dependent_count":0,"description":"compose reports the op applied (applied=N errors=0) while the patched\ncontent is absent from the output tree. 48 of the 84 overlays that patch\na Makefile are affected.\n\nMECHANISM, read off the code\n\n  engine/fsops.py    FileTransaction buffers writes in _writes: dict and\n                     read_text() returns the buffered copy when present\n  engine/apply.py    txn = FileTransaction(...) is created once (:262)\n                     and committed once, after the op loop (:498)\n  engine/apply.py    _exec_patch_apply starts with `_ = txn` (:63) and\n                     shells out to patch(1) with cwd=port_root, mutating\n                     the file directly on disk\n\nSo for an overlay whose ops touch the same Makefile:\n\n  1. `mk ...` reads Makefile from disk, stages modified text in _writes\n  2. `patch apply` edits Makefile on disk, transaction knows nothing\n  3. txn.commit() writes the staged (pre-patch) text over the file\n\nThe patch is reverted by the commit. patch(1) exited 0, so the op row is\nrecorded as applied and no error or warning is emitted.\n\nORDER DEPENDENT, not merely co-presence\n\nAn mk op AFTER patch apply is harmless: read_text() falls through to disk\nand picks up the patched content. Only an mk op that stages a write\nBEFORE patch apply runs loses it.\n\n  mk op before patch apply, patch clobbered   48\n  mk op after  patch apply, survives           3\n  patch apply with no mk op at all            33\n\nREPRODUCTION, verified 2026-08-31 on @2026Q3\n\n  devel/binutils   mk target set dfly-patch, then patch apply\n                   -> composed Makefile keeps the block the diff deletes\n                      (.if ARCH == x86_64 / CONFIGURE_TARGET=\n                      x86_64-portbld-freebsd) and lacks the line it adds\n                      (--with-lib-path=/lib:/usr/lib).\n                      The dfly-patch target IS present, i.e. the mk op won.\n\n  devel/gdb        same shape. Composed Makefile still has\n                   x86_64-portbld-freebsd and still says \"for FreeBSD\",\n                   both of which its Makefile.diff rewrites. Its five mk\n                   ops all landed.\n\n  lang/gcc13       CONTROL: patch apply with no mk op.\n                   binutils@native present in the composed Makefile.\n\nThe diff is not at fault. Applying devel/binutils' Makefile.diff by hand\nwith compose's own command line, passing the diff to -i, exits 0 and\nproduces exactly the intended output:\n\n  patch --batch --forward -V none -r - -p0 -i DIFFPATH\n\nIMPACT\n\ndevel/binutils is the reason lang/gcc12 cannot build. Without\n--with-lib-path and with CONFIGURE_TARGET left at\nx86_64-portbld-freebsd, the ports ld is a FreeBSD-targeted linker and\nrejects /usr/lib/libc.so with \"file in wrong format\", killing gcc12 at\nthe stage1 libgcc_s.so link. The reference DPorts tree that avalon builds\nhas both changes applied, which is why the same port version built there.\nNo change inside ports/lang/gcc12 can route around it.\n\nFIX DIRECTIONS\n\nEither route patch.apply through the transaction (read the subject via\ntxn.read_text, run patch on that text, stage_write the result), or have\nit flush/invalidate the transaction's entry for any path it touches so\nthe commit does not overwrite it. A cheap interim guard: make the planner\nreject or warn when an overlay has an mk op and a patch apply against the\nsame subject, since today that combination silently drops the patch.\n\nWhatever the fix, an op whose effect does not reach the output must not\nbe reported as applied.\n","id":"poly-bqo","issue_type":"bug","notes":"CODE RE-READ 2026-08-31. Mechanism confirmed line by line; no claim in the\ndescription changed. Added detail and one more control.\n\nengine/apply.py\n  :262  txn = FileTransaction(dry_run=dry_run) — one per port\n  :265+ ordered_ops = @any ops first, then target ops, then the rest;\n        within a group, plan order = overlay.dops file order\n  :498  txn.commit() sits AFTER the op loop, inside the try. Called once.\n        The only other calls are txn.rollback() on error paths.\n  :63   _exec_patch_apply opens with `_ = txn`, then subprocess patch(1)\n        with cwd=port_root. Nothing it does is visible to the transaction.\n\nengine/fsops.py\n  read_text()  returns self._writes[path] when staged, else path.read_text()\n  commit()     iterates _writes and does NamedTemporaryFile + replace(path).\n               Unconditional: no mtime check, no re-read, no merge. Whatever\n               is in the buffer wins.\n\nengine/executors/mk_ops.py\n  Every executor ends in txn.stage_write(path, updated), and path is always\n  _resolve_path(context.port_root, None, default=\"Makefile\") (:84 and the\n  same call in each op). So mk ops can only ever collide with\n  `patch apply diffs/Makefile.diff` — the conflict class is exactly Makefile.\n\nFULL SWEEP over every patch apply op in the tree, not just Makefile ones\n(subject = the diff basename, files_X.diff -> files/X):\n\n  patch apply ops total                                        133\n    staged write to the same subject BEFORE the patch (lost)    48   all Makefile\n    staged write to the same subject only AFTER  (survives)      3\n    no competing staged write at all                            82\n\ntext line-* ops stage writes too, but no overlay stages one against a\nsubject it later patches, so pkg-plist/pkg-message patches are unaffected\ntoday. The exposure is latent for them, not active.\n\nTHIRD CONTROL — order dependence proven empirically, devel/jna\n  Its overlay runs `patch apply diffs/Makefile.diff` FIRST and\n  `mk target set dfly-patch` LAST. Composed Makefile contains all three:\n    S/x86_64/x86-64/ in ANT_ARCH        (patch)   present\n    dragonflybsd-${ANT_ARCH}.jar        (patch)   present\n    dfly-patch:                         (mk)      present\n  Same two op kinds as binutils and gdb, opposite order, patch survives.\n  That is the ordering claim, demonstrated rather than reasoned.\n\nSo the three cases line up exactly with the model:\n  mk before patch  -> patch lost      devel/binutils, devel/gdb\n  mk after  patch  -> patch kept      devel/jna\n  no mk at all     -> patch kept      lang/gcc13\nCAUSAL PROOF, single-variable experiment on devel/binutils itself.\n\nThe controls (jna vs binutils/gdb vs gcc13) were different ports, so they\nonly established correlation. This changes one variable on one port.\n\nTook devel/binutils/overlay.dops and moved the `mk target set dfly-patch`\nblock from before `patch apply diffs/Makefile.diff` to after it. Nothing\nelse touched: same diff, same six materialize ops, same target, same\nfreebsd-ports tree, composed to an empty scratch dir both times.\n\n  mk BEFORE patch apply (as committed)\n    --with-lib-path                     absent\n    CONFIGURE_TARGET=x86_64-portbld-freebsd   still present\n    dfly-patch:                         present\n\n  mk AFTER patch apply (reordered)\n    --with-lib-path                     present\n    CONFIGURE_TARGET=x86_64-portbld-freebsd   gone\n    dfly-patch:                         present\n\nBoth runs reported ops=8 applied=8 errors=0.\n\nOrder is the only input that differed, so the staged-write-overwrites-\npatch mechanism is established, not inferred. It also rules out the\ncompeting explanation that the binutils diff simply fails to apply under\ncompose for some unrelated reason: the same diff applies cleanly in the\nreordered run.\n\nThe overlay was restored to its committed content afterwards (git status\nclean for that path) and the scratch trees were removed.\n\nPractical note for whoever fixes this: reordering an overlay is a valid\nper-port workaround today, but it is a trap — the correct behaviour must\nnot depend on op order within a dops file, and 48 overlays currently\ndepend on it silently.","owner":"github@dragonflybsd.org","priority":1,"status":"closed","title":"patch apply writes to disk outside the FileTransaction, so a preceding mk op silently reverts it","updated_at":"2026-08-31T23:39:54Z"}